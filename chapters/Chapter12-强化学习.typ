#import "@local/ysz_tools:0.1.0": *

#show: conf.with(
  title: "shabi",
)

== 强化学习基本设置 (Basic Settings of RL Problems)

=== 强化学习的直观理解与应用场景

强化学习 (Reinforcement Learning, RL) 致力于解决智能体 (Agent) 在与环境 (Environment) 的不断交互中学习最优决策序列的问题。

*典型应用场景*：
- 机器人控制（如 Boston Dynamics 的机器狗、双足机器人行走）
- 游戏 AI（如吃豆人、Backgammon 双陆棋、机器人足球赛）

*核心直觉（种西瓜的例子）*：
强化学习的过程类似于种西瓜。在瓜苗种下后，我们面临状态的变化和动作的选择。
- *动作 (Action)*：浇水或不浇水。
- *状态转移 (State Transition)*：缺水、健康、溢水、或凋亡。这些状态的变化往往带有随机性。
- *奖赏 (Reward)*：过程中可能只得到小的负反馈（如消耗水资源记为 $-1$），只有在最后收获好西瓜时才得到极大的正反馈（如 $+1$）。或者由于错误操作导致凋亡得到极大的惩罚（如 $-100$）。

这种“试错 (Trial-and-error)”和“延迟奖赏 (Delayed Reward)”正是强化学习面临的核心挑战。


=== 马尔可夫决策过程 (Markov Decision Process, MDP)

强化学习的基础数学框架是马尔可夫决策过程。

#definition[马尔可夫决策过程][
MDP 是对强化学习环境交互过程的严格数学抽象。它*假设环境的下一个状态仅依赖于当前状态和当前动作*，而与更早的历史状态无关（即马尔可夫性）。
]


1. *智能体与环境闭环*：机器（智能体）观察到当前的环境状态 $x$，执行动作 $a$，环境根据该动作转移到新状态，并反馈给机器一个标量奖赏 $r$。
2. *四元组定义*：一个 MDP 可以用一个四元组来完整描述：$ E = chevron.l X, A, P, R chevron.r $
   - *状态空间 $X$*：机器能够感知到的所有可能状态的集合，$x in X$。
   - *动作空间 $A$*：机器能够执行的所有可能动作的集合，$a in A$。
   - *转移概率 $P$*：$P: X times A times X -> bb(R)$。表示在状态 $x$ 执行动作 $a$ 后，转移到下一个状态 $x'$ 的概率。
   - *奖赏函数 $R$*：$R: X times A times X -> bb(R)$ 或简写为 $R: X times X -> bb(R)$。表示发生状态转移时环境给予的反馈信号。

3. *策略的数学表达*：在给定状态下执行的动作。
   - *确定性策略*：直接将状态映射到唯一动作，$ pi: X -> A $此时动作可以直接写为 $a = pi(x)$。
   - *随机性策略*：将状态映射到动作的概率分布，$ pi: X times A -> bb(R) $这意味着在状态 $x$ 下采取某动作 $a$ 依据的是一个概率值。
4. *交互轨迹*：
   从初始状态 $x_0$ 开始，智能体根据策略 $pi$ 选择动作 $a_0$，环境反馈奖赏 $r_1$ 并转移到 $x_1$。以此类推，形成一条长度为 $T$ 的交互序列：
   $ chevron.l x_0, a_0, r_1, x_1, a_1, r_2, ..., x_(T-1), a_(T-1), r_T, x_T chevron.r $


=== 优化目标：累积奖赏 (Cumulative Reward)

#remark[强化学习的根本目标不是追求某一步的即时奖赏最大化，而是寻找一个最优策略 $pi^*$，使得长期累积奖赏的期望值达到最大。]

在不同的任务设定下，累积奖赏有不同的计算方式：
1. *$T$ 步累积奖赏 (Finite-horizon)*：适用于有限步数结束的任务。目标是最大化这 $T$ 步内奖赏的平均期望：
   $ bb(E) [ 1/T sum_(t=1)^T r_t ] $
2. *折扣累积奖赏 (Infinite-horizon Discounted)*：适用于可能无限进行下去的任务。
   
   引入折扣因子 $gamma in (0, 1)$，使得越远未来的奖赏权重越低，既体现了近期奖赏的确定性，也保证了总和级数的收敛：
   $ bb(E) [ sum_(t=0)^(+oo) gamma^t r_(t+1) ] $


=== 强化学习与监督学习的本质对比

在基础设置的最后，我们需要厘清强化学习与传统机器学习的边界：
- *监督学习（分类器/回归器）*：模型 $f$ 的输入是示例（Features），输出是标记（Labels）。训练数据直接提供了明确的“正确答案”（Ground Truth），模型通过最小化误差进行学习。
- *强化学习*：策略 $pi$ 的输入是状态，输出是动作。环境中 *不存在* 明确的“正确动作”标记，只有等到最终结果揭晓才能反思之前动作（通过*试错*尝试不同的动作），观察环境给出的延迟标量反馈（即奖赏 $r$）来间接评估和优化策略。

#note[在某种意义上而言，强化学习可被认为是具有“延迟标记能力”的监督学习。]


== K-摇臂赌博机 (K-armed Bandit)

在本节中，我们通过一个简化的单状态模型来深入探讨强化学习中最核心的权衡问题。

=== 问题定义与背景

#definition[K-摇臂赌博机 (K-Armed Bandit)][
  K-摇臂赌博机是一个只有*单个状态、$K$ 个动作*（即摇臂）的强化学习模型，每个摇臂的奖赏服从某个期望未知的分布。
  
  在每个时刻从 $K$ 个摇臂中选择一个并按下，根据该摇臂背后的未知概率分布立即产生一个奖赏 $r$，不涉及状态转移。该问题的目标是通过多次尝试，最大化累积奖赏。
]


在 K-摇臂赌博机中，智能体面临着经典的 *探索-利用窘境 (Exploration-Exploitation dilemma)*：
- *利用 (Exploitation)*：按下目前已知平均奖赏最高的摇臂，以获取即时高收益。
- *探索 (Exploration)*：尝试那些目前表现一般甚至较差的摇臂，以发现潜在的更高收益摇臂，防止陷入局部最优。

=== $epsilon$-贪心算法

这是解决探索-利用问题最简单直观的方法。

#algorithm[$epsilon$-贪心算法][
  在每一次选择动作时：
  - 以 $1 - epsilon$ 的概率进行“利用”：选择当前平均奖赏最大的摇臂，即 $a = arg max_k Q(k)$。
  - 以 $epsilon$ 的概率进行“探索”：从所有 $K$ 个摇臂中以均匀概率随机选择一个动作。
]

#note[
  算法中的 $Q(k)$ 记录了当前摇臂 $k$ 的平均奖赏。
  
  随时间减小 $epsilon$ 的值（如 $epsilon = 1/sqrt(t)$）可以使系统在初期充分探索，在后期趋于稳定利用。
]

=== Softmax 策略 (Boltzmann 探索)

$epsilon$-贪心算法在探索时对所有摇臂一视同仁，而 Softmax 策略则根据摇臂的估计价值来分配探索概率。

#definition[Softmax 选择机制][
  基于每个摇臂的当前平均奖赏估计 $Q(k)$，按下第 $k$ 个摇臂的概率 $P(k)$ 定义为：
  $ P(k) = (e^(Q(k)/tau)) / (sum_(i=1)^K e^(Q(i)/tau)) $
]

*关键参数：温度 $tau$ (Temperature)*：
- *高温度 ($tau -> +infinity$)*：各摇臂选中的概率趋于相等，系统表现为完全随机探索。
- *低温度 ($tau -> 0$)*：高奖赏摇臂被选中的概率极大增加，系统表现为趋向于完全利用。

这种机制使得智能体在探索时会更有倾向性地尝试那些“看起来还不错”的摇臂，而不是完全盲目地尝试。

== 有模型学习 (Model-based Learning)
首先探讨一种理想情况——即所谓的“白盒”环境。设强化学习的环境是完全已知的。具体而言，就是马尔可夫决策过程 (MDP) 的四元组 $E = chevron.l X, A, P, R chevron.r$ 对智能体来说是透明的。智能体清楚地知道在状态 $x$ 下执行动作 $a$ 后，转移到下一个状态 $x'$ 的精确概率 $P_(x -> x')^a$，以及能够获得的准确奖赏 $R_(x -> x')^a$。#note[
  既然环境的运转规律已经完全被掌握，强化学习就不再需要像赌博机那样去“试错”。此时的问题完全转化为一个通过规划和计算来寻找最优策略 $pi^*$ 的动态规划问题。
]

=== 值函数的定义 (Value Functions)

#remark[  
  如果说“策略 $pi$” 是决定智能体在特定状态下*该怎么做*，那么“值函数”就是用来评估智能体处于某个特定状态（或执行某个特定动作）*到底有多好*。它衡量的是在未来长期交互中所能获得累积奖赏的期望值，是评价不同策略优劣的“量尺”。
]


为了全面评估，我们需要定义两种不同的值函数：

1. *状态值函数 $V^pi (x)$*：评估这个状态有多好。表示从状态 $x$ 开始，一直按照策略 $pi$ 行动，所能获得的累积奖赏期望。
  根据累积奖赏类型的不同，可分为：
  - *$T$ 步累积状态值函数*：
    $ V_T^pi (x) = bb(E)_pi [ 1/T sum_(t=1)^T r_t | x_0 = x ] $
  - *折扣累积状态值函数*：
    $ V_gamma^pi (x) = bb(E)_pi [ sum_(t=0)^(+oo) gamma^t r_(t+1) | x_0 = x ] $

2. *状态-动作值函数 $Q^pi (x,a)$*：评估在给定状态下的动作有多好。表示从状态 $x$ 开始，*第一步先强制执行动作 $a$*，然后再继续按照策略 $pi$ 行动，所能获得的累积奖赏期望。
  - *$T$ 步累积动作值函数*：
    $ Q_T^pi (x,a) = bb(E)_pi [ 1/T sum_(t=1)^T r_t | x_0 = x, a_0 = a ] $
  - *折扣累积动作值函数*：
    $ Q_gamma^pi (x,a) = bb(E)_pi [ sum_(t=0)^(+oo) gamma^t r_(t+1) | x_0 = x, a_0 = a ] $

=== 贝尔曼等式

#remark[
  直接计算长期的累积期望非常困难。贝尔曼等式揭示了强化学习中最深刻的动态规划思想：*当前状态的价值，等于“即时获得的奖赏”加上“下一个状态的后续价值”*。它巧妙地将一个长远未来的累积过程，转化为一个只关心当前步与下一步的简单递归方程。
]
Bellman Equation:
$ V_T^pi (x) = sum_(a in A) pi(x,a) sum_(x' in X) P_(x -> x')^a ( 1/T R_(x -> x')^a + (T-1)/T V_(T-1)^pi(x') ) $

将长度为 $T$ 的累积奖赏拆分：
$ V_T^pi (x) &= bb(E)_pi [ 1/T sum_(t=1)^T r_t | x_0 = x ] \
            &= bb(E)_pi [ 1/T r_1 + (T-1)/T dot 1/(T-1) sum_(t=2)^T r_t | x_0 = x ] $

接着，利用全概率公式，将第一步所有可能采取的动作 $a$（根据策略 $pi(x,a)$ 采样）以及随后可能转移到的下一个状态 $x'$（根据转移概率 $P_(x -> x')^a$）展开：
#mitex(`V_T^{\pi} (x) = \sum_{a \in A} \underbrace{\pi(x, a)}_{\text{选动作的概率}} \sum_{x' \in X} \underbrace{P^a_{x \to x'}}_{\text{转移概率}} \cdot \mathbb{E}[f|x_0=x, a_0=a, x_1=x']`)

$V_T^pi (x) = sum_(a in A) pi(x,a)times\   sum_(x' in X) P_(x -> x')^a ( 1/T R_(x -> x')^a + (T-1)/T bb(E)_pi [ 1/(T-1) sum_(t=1)^(T-1) r_t | x_0 = x' ] )$

注意到公式右侧期望部分恰好就是下个状态 $x'$ 的 $T-1$ 步状态值函数，于是得到原式.


同理，折扣累积奖赏的贝尔曼等式可以推导为：
$ V_gamma^pi (x) = sum_(a in A) pi(x,a) sum_(x' in X) P_(x -> x')^a ( R_(x -> x')^a + gamma V_gamma^pi(x') ) $

而状态-动作值函数 $Q$ 可以直接写为给定动作后的期望：
$ Q_gamma^pi (x,a) = sum_(x' in X) P_(x -> x')^a ( R_(x -> x')^a + gamma V_gamma^pi(x') ) $

=== 最优策略与最优值函数

最优策略是在所有状态下都能获得最大累积奖励的策略：
$ pi^* = arg max_pi sum_(x in X) V^pi (x) $

在最优策略下，对于任意状态 $x$，最优状态值函数 $V^*(x)$ 必然等于在当前状态下采取能带来最高期望价值的动作对应的 $Q$ 值。这被称为贝尔曼最优等式 (Bellman Optimality Equation)：
$ V_gamma^*(x) = max_(a in A) sum_(x' in X) P_(x -> x')^a ( R_(x -> x')^a + gamma V_gamma^*(x') ) $
$ V^*(x) = max_(a in A) Q^(pi^*)(x,a) $

基于上述推导，如果我们已经知道了最优的值函数，那么最优策略就只需在每一步贪心地选择使得 $Q(x,a)$ 最大的动作：#note[这不是废话吗，每一步选最好的动作。]
$ pi'(x) = arg max_(a in A) Q^pi (x,a) $

=== 策略迭代算法 (Policy Iteration)

结合对值函数的评估和对策略的贪心改进，我们便可以得到有模型学习中的核心算法框架。

#algorithm[策略迭代 (Policy Iteration)][
  策略迭代通过“策略评估”和“策略改进”两个阶段的交替，不断优化策略，直至收敛。
  
  *输入*：MDP 四元组 $E = chevron.l X, A, P, R chevron.r$，累积奖赏参数 $T$。
  
  *过程*：
  1. *初始化*：对所有的状态 $x in X$，初始化值函数 $V(x)=0$，并设定一个均匀分布的随机初始策略 $pi(x,a) = 1/|A(x)|$。
  2. *策略评估 (Policy Evaluation)*：在当前策略 $pi$ 下，根据贝尔曼等式迭代计算值函数。对于有限步数 $T$，循环更新 $V'(x)$ 直至 $t=T$；
     $ V'(x) = sum_(a in A) pi(x,a) sum_(x' in X) P_(x -> x')^a ( 1/t R_(x -> x')^a + (t-1)/t V(x') ) $
  3. *策略改进 (Policy Improvement)*：使用最新计算得到的值函数，提取出对每个动作的 $Q(x,a)$ 估计，并贪心地选出最好的动作，更新策略：
     $ pi'(x) = arg max_(a in A) Q(x,a) $
  4. *终止判定*：检查新策略 $pi'$ 是否与旧策略 $pi$ 完全相同。若没有任何状态的动作发生改变，说明策略已经是最优的，跳出循环；否则令 $pi = pi'$，回到第 2 步重新评估。
  
  *输出*：最优策略 $pi^*$。
]

== 免模型学习 (Model-free Learning)

在有模型学习中，我们依赖于对环境动态特性（转移概率 $P$ 和奖赏函数 $R$）的完全掌握。然而，现实世界中的大多数问题都过于复杂，我们往往面对的是一个“黑盒”环境。

#definition[免模型学习 (Model-free Learning)][
  当环境的转移概率 $P$ 和奖赏函数 $R$ 未知时，智能体无法通过内部的马尔可夫推演来计算值函数。此时，智能体必须通过在环境中实际执行动作、收集真实交互轨迹（Trajectory）来积累经验，并直接根据经验数据来估算值函数并优化策略。
]

=== 蒙特卡洛方法 (Monte Carlo Methods)

通过大数定律，用样本的经验平均值来逼近真实的数学期望。

#remark[蒙特卡洛方法][既然无法直接算出某个状态-动作对 $(x,a)$ 的期望累积奖赏，那就在环境中多跑几次完整的游戏（Episode）。把每次经过状态 $x$ 并执行动作 $a$ 后得到的实际总奖赏记录下来，最后取平均值，作为对 $Q(x,a)$ 的无偏估计。
]

1. *轨迹采样*：在环境 $E$ 中执行策略 $pi$ 产生一条完整轨迹：
   $ angle.l x_0, a_0, r_1, x_1, a_1, r_2, ..., x_(T-1), a_(T-1), r_T, x_T angle.r $
2. *计算回报 (Return)*：对于轨迹中的时刻 $t$，其向后的平均累积奖赏可以表示为：
   $ R = 1/(T-t) sum_(i=t+1)^T r_i $
3. *增量式更新*：为了避免保存所有历史数据，我们采用增量更新的方法。设 $text("count")(x_t, a_t)$ 为状态-动作对被访问的次数，则新的估计值为：
   $ Q(x_t,a_t) = (Q(x_t,a_t) times text("count")(x_t,a_t) + R) / (text("count")(x_t,a_t) + 1) $

在蒙特卡洛控制中，为了保证持续的探索，我们通常将更新后的值函数结合 $epsilon$-贪心法来提取新策略。根据产生样本的策略与待优化的策略是否相同，分为两类：

- *同策略 (On-policy)*：评估和改进的是*同一个*策略（即使用 $epsilon$-贪心策略生成轨迹，同时也优化这个 $epsilon$-贪心策略）。
- *异策略 (Off-policy)*：评估和改进的是目标策略 $pi$（通常是确定性的贪心策略），但生成数据的行为策略（Behavior Policy）是另一个具有探索性的策略（如 $epsilon$-贪心）。
  为了修正分布差异，异策略蒙特卡洛必须引入*重要性采样 (Importance Sampling)*。如果行为策略在第 $j$ 步选择动作的概率为 $p_j$，那么目标策略的回报需要通过累积概率比率进行加权：
  $ R = 1/(T-t) sum_(i=t+1)^T ( r_i times product_(j=i)^(T-1) 1/p_j ) $

#remark[
  同策略的核心是“边做边学”，评估当前正在执行的带有探索性的策略；异策略的核心是“看别人做来学自己”，将探索过程与目标策略的优化解耦，能学习到更为纯粹的最优策略。
]

=== 时序差分学习 (Temporal Difference, TD)

蒙特卡洛方法的一个致命弱点是：必须等待一个完整的轨迹结束（即走到终点状态 $x_T$）才能计算回报并更新价值。在连续任务或步数极长的任务中，这种延迟是不可接受的。

#remark[时序差分学习][
  *核心概念*：时序差分结合了蒙特卡洛的“免模型采样”思想和动态规划的“自举 (Bootstrapping)”思想。它不必等待回合结束，而是在每走一步后，利用*下一步的价值估计*来更新*当前步的价值估计*。
]

*STEP-BY-STEP DETAILS*：

我们可以将原本蒙特卡洛的增量更新公式：
$ Q_(t+1)(x,a) = Q_t(x,a) + 1/(t+1) (R_("真实回报") - Q_t(x,a)) $

替换为只走一步的 TD 更新公式：
$ Q(x,a) = Q(x,a) + alpha (r + gamma Q(x', a') - Q(x,a)) $
- $alpha$：学习率（更新步长）。
- $r + gamma Q(x', a')$：被称为 *TD 目标 (TD Target)*，它是根据下一步的即时奖赏和下一步的估计价值拼凑出的当前真实价值的近似。
- $r + gamma Q(x', a') - Q(x,a)$：被称为 *TD 误差 (TD Error)*，代表了我们估计的偏差。

#note[
  时序差分方法由于每步都能更新，收敛速度通常远快于蒙特卡洛方法，是现代强化学习最基石的算法族。
]

=== Sarsa 算法与 Q-learning 算法

基于 TD 思想的动作价值控制算法，同样分为同策略和异策略两个经典分支：Sarsa 和 Q-learning。

#algorithm[Sarsa 算法 (同策略时序差分控制)][
  Sarsa 的名称来源于它在更新时需要用到五元组：当前状态 $S$、当前动作 $A$、即时奖赏 $R$、下一状态 $S'$、下一动作 $A'$。
  
  *核心过程*：
  1. 智能体在状态 $x$ 依据 $epsilon$-贪心策略选择动作 $a$。
  2. 执行动作 $a$，获得奖赏 $r$，并转移到新状态 $x'$。
  3. *在状态 $x'$ 处，再次依据相同的 $epsilon$-贪心策略选择下一步动作 $a'$*。
  4. 利用实际选定的 $a'$ 进行价值更新：
     $ Q(x,a) = Q(x,a) + alpha(r + gamma Q(x', a') - Q(x,a)) $
  5. 贪心化当前策略 $pi(x) = arg max_(a'') Q(x, a'')$，步进到下一状态，重复循环。
]

#algorithm[Q-learning 算法 (异策略时序差分控制)][
  Q-learning 是强化学习中最著名的算法之一，它打破了必须用实际执行动作的价值来更新当前价值的限制。
  
  *核心过程*：
  1. 智能体在状态 $x$ 依据 $epsilon$-贪心策略（行为策略）选择执行动作 $a$。
  2. 执行动作 $a$，获得奖赏 $r$，并转移到新状态 $x'$。
  3. *在更新 $Q(x,a)$ 时，它不去管智能体下一步实际会走哪步，而是直接假设智能体会采取最优策略，即直接取状态 $x'$ 下所有可能动作的 $Q$ 值最大者*：
     $ Q(x,a) = Q(x,a) + alpha(r + gamma max_(a'') Q(x', a'') - Q(x,a)) $
  4. 贪心化目标策略 $pi(x) = arg max_(a'') Q(x, a'')$，步进到下一状态，重复循环。
]

#remark[
  Sarsa 是保守的，它在更新时会考虑策略本身的探索性带来的潜在风险；而 Q-learning 是激进的，无论当前如何探索，它始终在逼近那个绝对最优的确定性策略 $pi^*$。
]