#import "@local/ysz_tools:0.1.0": *

#show: conf.with(
  title: "shabi",
)

== 强化学习基本设置 (Basic Settings of RL Problems)

=== 强化学习的直观理解与应用场景

强化学习 (Reinforcement Learning, RL) 致力于解决智能体 (Agent) 在与环境 (Environment) 的不断交互中学习最优决策序列的问题。

*典型应用场景*：
- 机器人控制（如 Boston Dynamics 的机器狗、双足机器人行走）
- 游戏 AI（如吃豆人、Backgammon 双陆棋、机器人足球赛）

*核心直觉（种西瓜的例子）*：
强化学习的过程类似于种西瓜。在瓜苗种下后，我们面临状态的变化和动作的选择。
- *动作 (Action)*：浇水或不浇水。
- *状态转移 (State Transition)*：缺水、健康、溢水、或凋亡。这些状态的变化往往带有随机性。
- *奖赏 (Reward)*：过程中可能只得到小的负反馈（如消耗水资源记为 $-1$），只有在最后收获好西瓜时才得到极大的正反馈（如 $+1$）。或者由于错误操作导致凋亡得到极大的惩罚（如 $-100$）。

这种“试错 (Trial-and-error)”和“延迟奖赏 (Delayed Reward)”正是强化学习面临的核心挑战。


=== 马尔可夫决策过程 (Markov Decision Process, MDP)

强化学习的基础数学框架是马尔可夫决策过程。

#definition[马尔可夫决策过程][
MDP 是对强化学习环境交互过程的严格数学抽象。它*假设环境的下一个状态仅依赖于当前状态和当前动作*，而与更早的历史状态无关（即马尔可夫性）。
]


1. *智能体与环境闭环*：机器（智能体）观察到当前的环境状态 $x$，执行动作 $a$，环境根据该动作转移到新状态，并反馈给机器一个标量奖赏 $r$。
2. *四元组定义*：一个 MDP 可以用一个四元组来完整描述：$ E = chevron.l X, A, P, R chevron.r $
   - *状态空间 $X$*：机器能够感知到的所有可能状态的集合，$x in X$。
   - *动作空间 $A$*：机器能够执行的所有可能动作的集合，$a in A$。
   - *转移概率 $P$*：$P: X times A times X -> bb(R)$。表示在状态 $x$ 执行动作 $a$ 后，转移到下一个状态 $x'$ 的概率。
   - *奖赏函数 $R$*：$R: X times A times X -> bb(R)$ 或简写为 $R: X times X -> bb(R)$。表示发生状态转移时环境给予的反馈信号。

3. *策略的数学表达*：在给定状态下执行的动作。
   - *确定性策略*：直接将状态映射到唯一动作，$ pi: X -> A $此时动作可以直接写为 $a = pi(x)$。
   - *随机性策略*：将状态映射到动作的概率分布，$ pi: X times A -> bb(R) $这意味着在状态 $x$ 下采取某动作 $a$ 依据的是一个概率值。
4. *交互轨迹*：
   从初始状态 $x_0$ 开始，智能体根据策略 $pi$ 选择动作 $a_0$，环境反馈奖赏 $r_1$ 并转移到 $x_1$。以此类推，形成一条长度为 $T$ 的交互序列：
   $ chevron.l x_0, a_0, r_1, x_1, a_1, r_2, ..., x_(T-1), a_(T-1), r_T, x_T chevron.r $


=== 优化目标：累积奖赏 (Cumulative Reward)

#remark[强化学习的根本目标不是追求某一步的即时奖赏最大化，而是寻找一个最优策略 $pi^*$，使得长期累积奖赏的期望值达到最大。]

在不同的任务设定下，累积奖赏有不同的计算方式：
1. *$T$ 步累积奖赏 (Finite-horizon)*：适用于有限步数结束的任务。目标是最大化这 $T$ 步内奖赏的平均期望：
   $ bb(E) [ 1/T sum_(t=1)^T r_t ] $
2. *折扣累积奖赏 (Infinite-horizon Discounted)*：适用于可能无限进行下去的任务。
   
   引入折扣因子 $gamma in (0, 1)$，使得越远未来的奖赏权重越低，既体现了近期奖赏的确定性，也保证了总和级数的收敛：
   $ bb(E) [ sum_(t=0)^(+oo) gamma^t r_(t+1) ] $


=== 强化学习与监督学习的本质对比

在基础设置的最后，我们需要厘清强化学习与传统机器学习的边界：
- *监督学习（分类器/回归器）*：模型 $f$ 的输入是示例（Features），输出是标记（Labels）。训练数据直接提供了明确的“正确答案”（Ground Truth），模型通过最小化误差进行学习。
- *强化学习*：策略 $pi$ 的输入是状态，输出是动作。环境中 *不存在* 明确的“正确动作”标记，只有等到最终结果揭晓才能反思之前动作（通过*试错*尝试不同的动作），观察环境给出的延迟标量反馈（即奖赏 $r$）来间接评估和优化策略。

#note[在某种意义上而言，强化学习可被认为是具有“延迟标记能力”的监督学习。]


== K-摇臂赌博机 (K-armed Bandit)

在本节中，我们通过一个简化的单状态模型来深入探讨强化学习中最核心的权衡问题。

=== 问题定义与背景

#definition[K-摇臂赌博机 (K-Armed Bandit)][
  K-摇臂赌博机是一个只有*单个状态、$K$ 个动作*（即摇臂）的强化学习模型，每个摇臂的奖赏服从某个期望未知的分布。
  
  在每个时刻从 $K$ 个摇臂中选择一个并按下，根据该摇臂背后的未知概率分布立即产生一个奖赏 $r$，不涉及状态转移。该问题的目标是通过多次尝试，最大化累积奖赏。
]


在 K-摇臂赌博机中，智能体面临着经典的 *探索-利用窘境 (Exploration-Exploitation dilemma)*：
- *利用 (Exploitation)*：按下目前已知平均奖赏最高的摇臂，以获取即时高收益。
- *探索 (Exploration)*：尝试那些目前表现一般甚至较差的摇臂，以发现潜在的更高收益摇臂，防止陷入局部最优。

=== $epsilon$-贪心算法

这是解决探索-利用问题最简单直观的方法。

#algorithm[$epsilon$-贪心算法][
  在每一次选择动作时：
  - 以 $1 - epsilon$ 的概率进行“利用”：选择当前平均奖赏最大的摇臂，即 $a = arg max_k Q(k)$。
  - 以 $epsilon$ 的概率进行“探索”：从所有 $K$ 个摇臂中以均匀概率随机选择一个动作。
]

#note[
  算法中的 $Q(k)$ 记录了当前摇臂 $k$ 的平均奖赏。
  
  随时间减小 $epsilon$ 的值（如 $epsilon = 1/sqrt(t)$）可以使系统在初期充分探索，在后期趋于稳定利用。
]

=== Softmax 策略 (Boltzmann 探索)

$epsilon$-贪心算法在探索时对所有摇臂一视同仁，而 Softmax 策略则根据摇臂的估计价值来分配探索概率。

#definition[Softmax 选择机制][
  基于每个摇臂的当前平均奖赏估计 $Q(k)$，按下第 $k$ 个摇臂的概率 $P(k)$ 定义为：
  $ P(k) = (e^(Q(k)/tau)) / (sum_(i=1)^K e^(Q(i)/tau)) $
]

*关键参数：温度 $tau$ (Temperature)*：
- *高温度 ($tau -> +infinity$)*：各摇臂选中的概率趋于相等，系统表现为完全随机探索。
- *低温度 ($tau -> 0$)*：高奖赏摇臂被选中的概率极大增加，系统表现为趋向于完全利用。

这种机制使得智能体在探索时会更有倾向性地尝试那些“看起来还不错”的摇臂，而不是完全盲目地尝试。

== 有模型学习 (Model-based Learning)
首先探讨一种理想情况——即所谓的“白盒”环境。设强化学习的环境是完全已知的。具体而言，就是马尔可夫决策过程 (MDP) 的四元组 $E = chevron.l X, A, P, R chevron.r$ 对智能体来说是透明的。智能体清楚地知道在状态 $x$ 下执行动作 $a$ 后，转移到下一个状态 $x'$ 的精确概率 $P_(x -> x')^a$，以及能够获得的准确奖赏 $R_(x -> x')^a$。#note[
  既然环境的运转规律已经完全被掌握，强化学习就不再需要像赌博机那样去“试错”。此时的问题完全转化为一个通过规划和计算来寻找最优策略 $pi^*$ 的动态规划问题。
]

=== 值函数的定义 (Value Functions)

#remark[  
  如果说“策略 $pi$” 是决定智能体在特定状态下*该怎么做*，那么“值函数”就是用来评估智能体处于某个特定状态（或执行某个特定动作）*到底有多好*。它衡量的是在未来长期交互中所能获得累积奖赏的期望值，是评价不同策略优劣的“量尺”。
]


为了全面评估，我们需要定义两种不同的值函数：

1. *状态值函数 $V^pi (x)$*：评估这个状态有多好。表示从状态 $x$ 开始，一直按照策略 $pi$ 行动，所能获得的累积奖赏期望。
  根据累积奖赏类型的不同，可分为：
  - *$T$ 步累积状态值函数*：
    $ V_T^pi (x) = bb(E)_pi [ 1/T sum_(t=1)^T r_t | x_0 = x ] $
  - *折扣累积状态值函数*：
    $ V_gamma^pi (x) = bb(E)_pi [ sum_(t=0)^(+oo) gamma^t r_(t+1) | x_0 = x ] $

2. *状态-动作值函数 $Q^pi (x,a)$*：评估在给定状态下的动作有多好。表示从状态 $x$ 开始，*第一步先强制执行动作 $a$*，然后再继续按照策略 $pi$ 行动，所能获得的累积奖赏期望。
  - *$T$ 步累积动作值函数*：
    $ Q_T^pi (x,a) = bb(E)_pi [ 1/T sum_(t=1)^T r_t | x_0 = x, a_0 = a ] $
  - *折扣累积动作值函数*：
    $ Q_gamma^pi (x,a) = bb(E)_pi [ sum_(t=0)^(+oo) gamma^t r_(t+1) | x_0 = x, a_0 = a ] $

=== 贝尔曼等式

#remark[
  直接计算长期的累积期望非常困难。贝尔曼等式揭示了强化学习中最深刻的动态规划思想：*当前状态的价值，等于“即时获得的奖赏”加上“下一个状态的后续价值”*。它巧妙地将一个长远未来的累积过程，转化为一个只关心当前步与下一步的简单递归方程。
]
Bellman Equation:
$ V_T^pi (x) = sum_(a in A) pi(x,a) sum_(x' in X) P_(x -> x')^a ( 1/T R_(x -> x')^a + (T-1)/T V_(T-1)^pi(x') ) $

将长度为 $T$ 的累积奖赏拆分：
$ V_T^pi (x) &= bb(E)_pi [ 1/T sum_(t=1)^T r_t | x_0 = x ] \
            &= bb(E)_pi [ 1/T r_1 + (T-1)/T dot 1/(T-1) sum_(t=2)^T r_t | x_0 = x ] $

接着，利用全概率公式，将第一步所有可能采取的动作 $a$（根据策略 $pi(x,a)$ 采样）以及随后可能转移到的下一个状态 $x'$（根据转移概率 $P_(x -> x')^a$）展开：
#mitex(`V_T^{\pi} (x) = \sum_{a \in A} \underbrace{\pi(x, a)}_{\text{选动作的概率}} \sum_{x' \in X} \underbrace{P^a_{x \to x'}}_{\text{转移概率}} \cdot \mathbb{E}[f|x_0=x, a_0=a, x_1=x']`)

$V_T^pi (x) = sum_(a in A) pi(x,a)times\   sum_(x' in X) P_(x -> x')^a ( 1/T R_(x -> x')^a + (T-1)/T bb(E)_pi [ 1/(T-1) sum_(t=1)^(T-1) r_t | x_0 = x' ] )$

注意到公式右侧期望部分恰好就是下个状态 $x'$ 的 $T-1$ 步状态值函数，于是得到原式.


同理，折扣累积奖赏的贝尔曼等式可以推导为：
$ V_gamma^pi (x) = sum_(a in A) pi(x,a) sum_(x' in X) P_(x -> x')^a ( R_(x -> x')^a + gamma V_gamma^pi(x') ) $

而状态-动作值函数 $Q$ 可以直接写为给定动作后的期望：
$ Q_gamma^pi (x,a) = sum_(x' in X) P_(x -> x')^a ( R_(x -> x')^a + gamma V_gamma^pi(x') ) $

=== 最优策略与最优值函数

最优策略是在所有状态下都能获得最大累积奖励的策略：
$ pi^* = arg max_pi sum_(x in X) V^pi (x) $

在最优策略下，对于任意状态 $x$，最优状态值函数 $V^*(x)$ 必然等于在当前状态下采取能带来最高期望价值的动作对应的 $Q$ 值。这被称为贝尔曼最优等式 (Bellman Optimality Equation)：
$ V_gamma^*(x) = max_(a in A) sum_(x' in X) P_(x -> x')^a ( R_(x -> x')^a + gamma V_gamma^*(x') ) $
$ V^*(x) = max_(a in A) Q^(pi^*)(x,a) $

基于上述推导，如果我们已经知道了最优的值函数，那么最优策略就只需在每一步贪心地选择使得 $Q(x,a)$ 最大的动作：#note[这不是废话吗，每一步选最好的动作。]
$ pi'(x) = arg max_(a in A) Q^pi (x,a) $

=== 策略迭代算法 (Policy Iteration)

结合对值函数的评估和对策略的贪心改进，我们便可以得到有模型学习中的核心算法框架。

#algorithm[策略迭代 (Policy Iteration)][
  策略迭代通过“策略评估”和“策略改进”两个阶段的交替，不断优化策略，直至收敛。
  
  *输入*：MDP 四元组 $E = chevron.l X, A, P, R chevron.r$，累积奖赏参数 $T$。
  
  *过程*：
  1. *初始化*：对所有的状态 $x in X$，初始化值函数 $V(x)=0$，并设定一个均匀分布的随机初始策略 $pi(x,a) = 1/|A(x)|$。
  2. *策略评估 (Policy Evaluation)*：在当前策略 $pi$ 下，根据贝尔曼等式迭代计算值函数。对于有限步数 $T$，循环更新 $V'(x)$ 直至 $t=T$；
     $ V'(x) = sum_(a in A) pi(x,a) sum_(x' in X) P_(x -> x')^a ( 1/t R_(x -> x')^a + (t-1)/t V(x') ) $
  3. *策略改进 (Policy Improvement)*：使用最新计算得到的值函数，提取出对每个动作的 $Q(x,a)$ 估计，并贪心地选出最好的动作，更新策略：
     $ pi'(x) = arg max_(a in A) Q(x,a) $
  4. *终止判定*：检查新策略 $pi'$ 是否与旧策略 $pi$ 完全相同。若没有任何状态的动作发生改变，说明策略已经是最优的，跳出循环；否则令 $pi = pi'$，回到第 2 步重新评估。
  
  *输出*：最优策略 $pi^*$。
]

== 免模型学习 (Model-free Learning)

在有模型学习中，我们依赖于对环境动态特性（转移概率 $P$ 和奖赏函数 $R$）的完全掌握。然而，现实世界中的大多数问题都过于复杂，我们往往面对的是一个“黑盒”环境。

#definition[免模型学习 (Model-free Learning)][
  当环境的转移概率 $P$ 和奖赏函数 $R$ 未知时，智能体无法通过内部的马尔可夫推演来计算值函数。此时，智能体必须通过在环境中实际执行动作、收集真实交互轨迹（Trajectory）来积累经验，并直接根据经验数据来估算值函数并优化策略。
]

=== 蒙特卡洛方法 (Monte Carlo Methods)

通过大数定律，用样本的经验平均值来逼近真实的数学期望。

#remark[蒙特卡洛方法][既然无法直接算出某个状态-动作对 $(x,a)$ 的期望累积奖赏，那就在环境中多跑几次完整的游戏（Episode）。把每次经过状态 $x$ 并执行动作 $a$ 后得到的实际总奖赏记录下来，最后取平均值，作为对 $Q(x,a)$ 的无偏估计。
]

1. *轨迹采样*：在环境 $E$ 中执行策略 $pi$ 产生一条完整轨迹：
   $ angle.l x_0, a_0, r_1, x_1, a_1, r_2, ..., x_(T-1), a_(T-1), r_T, x_T angle.r $
   其中$x_i$是状态，$a_i$是策略，$r_i$是奖赏。
2. *计算回报 (Return)*：对于轨迹中的时刻 $t$，从当前时刻到任务结束，其向后的平均累积奖赏可以表示为：
   $ R = 1/(T-t) sum_(i=t+1)^T r_i $
3. *增量式更新*：为了避免保存所有历史数据，我们采用增量更新的方法。设 $text("count")(x_t, a_t)$ 为状态-动作对被访问的次数，则新的估计值为：
   $ Q(x_t,a_t) = (Q(x_t,a_t) times text("count")(x_t,a_t) + R) / (text("count")(x_t,a_t) + 1) $

在蒙特卡洛控制中，为了保证持续的探索，我们通常将更新后的值函数结合 $epsilon$-贪心法来提取新策略。根据产生样本的策略与待优化的策略是否相同，分为两类：

- *同策略 (On-policy)*：评估和改进的是*同一个*策略（即使用 $epsilon$-贪心策略生成轨迹，同时也优化这个 $epsilon$-贪心策略）。
 #algorithm[同策略蒙特卡洛方法][
  1. $Q(x, a) = 0, "count"(x, a) = 0, pi(x, a) = 1 / (|A(x)|)$;
  2. *for* $s = 1, 2, dots$ *do*
  3.  在 $E$ 中执行策略 $pi$ 产生轨迹: \
      $angle.l x_0, a_0, r_1, x_1, a_1, r_2, dots, x_(T-1), a_(T-1), r_T, x_T angle.r$;
  4.  *for* $t = 0, 1, dots, T - 1$ *do*
  5.   $R = 1 / (T - t) sum_(i=t+1)^T r_i$;
  6.   $Q(x_t, a_t) = (Q(x_t, a_t) times "count"(x_t, a_t) + R) / ("count"(x_t, a_t) + 1)$;
  7.  $ "count"(x_t, a_t) = "count"(x_t, a_t) + 1$;
  8.  *end for*
  9.  对所有已见状态 $x$: \
      $pi(x, a) = cases(
        arg max_(a') Q(x, a')\, & "以概率" 1 - epsilon,
        "以均匀概率从" A "中选取动作"\, & "以概率" epsilon
      )$
  10. *end for*
  *输出：* 策略 $pi$
  ]

- *异策略 (Off-policy)*：评估和改进的是目标策略 $pi$（通常是确定性的贪心策略），但生成数据的行为策略（Behavior Policy）是另一个具有探索性的策略（如 $epsilon$-贪心）。#note[
 $epsilon$贪心:
$
 pi^* (x,a) = cases(
  arg max_Q Q(x,a)"," quad &1-epsilon,
  "random," quad &"else"
 )
$
]



  为了修正分布差异，异策略蒙特卡洛必须引入*重要性采样 (Importance Sampling)*。如果行为策略在第 $j$ 步选择动作的概率为 $p_j$，那么目标策略的回报需要通过累积概率比率进行加权：
  $ R = 1/(T-t) sum_(i=t+1)^T ( r_i times product_(j=i)^(T-1) 1/p_j ) $

#remark[
  同策略的核心是“边做边学”，评估当前正在执行的带有探索性的策略；异策略的核心是“看别人做来学自己”，将探索过程与目标策略的优化解耦，能学习到更为纯粹的最优策略。
]

=== 时序差分学习 (Temporal Difference, TD)

蒙特卡洛方法的一个致命弱点是：必须等待一个完整的轨迹结束（即走到终点状态 $x_T$）才能计算回报并更新价值。在连续任务或步数极长的任务中，这种延迟是不可接受的。

#remark[时序差分学习][
  *核心概念*：时序差分结合了蒙特卡洛的“免模型采样”思想和动态规划的“自举 (Bootstrapping)”思想。它不必等待回合结束，而是在每走一步后，利用*下一步的价值估计*来更新*当前步的价值估计*。
]
我们已经有经典的更新公式
$ Q_(t+1)(x,a) = Q_t (x,a) + 1/(t+1) (R - Q_t (x,a)) $
有时候我们不能模拟完整的轨迹,只能通过单步模拟来更新值函数.记$x'$是下一步的状态,$a'$是下一步的动作.
$ Q(x,a) = Q(x,a) + alpha (r + gamma Q(x', a') - Q(x,a)) $
- $alpha$：学习率（更新步长）。
- $r + gamma Q(x', a')$：被称为 *TD 目标 (TD Target)*，它是根据下一步的即时奖赏和下一步的估计价值拼凑出的当前真实价值的近似。
- $r + gamma Q(x', a') - Q(x,a)$：被称为 *TD 误差 (TD Error)*，代表了我们估计的偏差。#note[
  时序差分方法由于每步都能更新，收敛速度通常远快于蒙特卡洛方法，是现代强化学习最基石的算法族。
]

=== Sarsa 算法与 Q-learning 算法

基于 TD 思想的动作价值控制算法，同样分为同策略和异策略两个经典分支：Sarsa 和 Q-learning。

#algorithm[Sarsa 算法 (同策略时序差分控制)][
  Sarsa 的名称来源于它在更新时需要用到五元组：当前状态 $S$、当前动作 $A$、即时奖赏 $R$、下一状态 $S'$、下一动作 $A'$。
  
  *核心过程*：
  1. 智能体在状态 $x$ 依据 $epsilon$-贪心策略选择动作 $a$。
  2. 执行动作 $a$，获得奖赏 $r$，并转移到新状态 $x'$。
  3. *在状态 $x'$ 处，再次依据相同的 $epsilon$-贪心策略选择下一步动作 $a'$*。
  4. 利用实际选定的 $a'$ 进行价值更新：
     $ Q(x,a) = Q(x,a) + alpha(r + gamma Q(x', a') - Q(x,a)) $
  5. 贪心化当前策略 $pi(x) = arg max_(a'') Q(x, a'')$，步进到下一状态，重复循环。
]

#algorithm[Q-learning 算法 (异策略时序差分控制)][
  Q-learning 是强化学习中最著名的算法之一，它打破了必须用实际执行动作的价值来更新当前价值的限制。
  
  *核心过程*：
  1. 智能体在状态 $x$ 依据 $epsilon$-贪心策略（行为策略）选择执行动作 $a$。
  2. 执行动作 $a$，获得奖赏 $r$，并转移到新状态 $x'$。
  3. *在更新 $Q(x,a)$ 时，它不去管智能体下一步实际会走哪步，而是直接假设智能体会采取最优策略，即直接取状态 $x'$ 下所有可能动作的 $Q$ 值最大者*：
     $ Q(x,a) = Q(x,a) + alpha(r + gamma max_(a'') Q(x', a'') - Q(x,a)) $
  4. 贪心化目标策略 $pi(x) = arg max_(a'') Q(x, a'')$，步进到下一状态，重复循环。
]

#remark[
  Sarsa 是保守的，它在更新时会考虑策略本身的探索性带来的潜在风险；而 Q-learning 是激进的，无论当前如何探索，它始终在逼近那个绝对最优的确定性策略 $pi^*$。
]









 #algorithm[异策略蒙特卡罗强化学习算法][
  1. $Q(x, a) = 0, "count"(x, a) = 0, pi(x, a) = 1 / (|A(x)|)$;
  2. *for* $s = 1, 2, dots$ *do*
  3.  在 $E$ 中执行策略 $pi$ 的 $epsilon$-贪心策略产生轨迹: \
     $<x_0, a_0, r_1, x_1, a_1, r_2, dots, x_(T-1), a_(T-1), r_T, x_T>$;
  4.  $p_i = cases(
       1 - epsilon + epsilon / |A|\, & a_i = pi(x),
       epsilon / |A|\, & a_i != pi(x)
     )$;
  5.  *for* $t = 0, 1, dots, T - 1$ *do*
  6.   $R = 1 / (T - t) sum_(i=t+1)^T (r_i times product_(j=i)^(T-1) 1 / p_j)$;
  7.   $Q(x_t, a_t) = (Q(x_t, a_t) times "count"(x_t, a_t) + R) / ("count"(x_t, a_t) + 1)$;
  8.   $"count"(x_t, a_t) = "count"(x_t, a_t) + 1$;
  9.  *end for*
  10.  $pi(x) = arg max_(a') Q(x, a')$
  11. *end for*
  *输出：* 策略 $pi$
]


== 值函数近似 (Value Function Approximation)

本节将探讨当强化学习面对复杂现实问题时，如何解决由于状态空间过大或连续而导致的“维度灾难”问题。

=== 表格型值函数的局限性

在前面的章节中，无论是动态规划、蒙特卡罗方法还是时序差分学习，我们都在一个前提下进行：假定状态空间和动作空间都是*离散且有限*的。

在这些场景下，值函数可以通过一个“表格”（Tabular value function）来精确记录。这意味着我们在内存中维护着一个查找表，表中的每一个条目精确对应一个状态 $x$ 或状态-动作对 $(x,a)$ 的价值。

#note[
  然而，现实世界中的状态往往是连续的（例如机器人的关节角度、自动驾驶的坐标与速度）。当状态空间 $X$ 变为连续空间（如 $X = bb(R)^n$）时，我们显然无法在内存中存下一个具有无限多条目的表格。即使进行离散化，状态数量也会随维度呈指数级爆炸，导致无法有效泛化。
]

=== 值函数近似的概念

为了解决上述困难，我们需要改变值函数的表达方式，引入值函数近似（Value Function Approximation）。

#definition[值函数近似 (Value Function Approximation)][
  不再试图精确记录每一个状态的独立价值，而是引入一个参数化的数学模型（如线性模型、神经网络等）来拟合价值空间的整体分布。通过学习模型的参数 $theta$，使得模型对于任意输入的状态 $x$，都能直接计算并输出近似的价值估计。
]

这种方法不仅极大地压缩了存储空间（从存储无限个状态值变为仅存储参数 $theta$），更重要的是赋予了模型*泛化能力*：算法可以通过学习经历过的状态，推断出那些从未见过的相似状态的价值。

=== 线性近似器 (Linear Approximator)

为简便起见，我们首先考虑最基础且易于分析的近似方法：线性近似。

对于状态值函数，我们将其表达为状态特征向量的线性函数：
$ V_theta (x)  = theta^top x $

其中，$x$ 是表示环境状态的特征向量，而 $theta$ 则是模型需要学习的参数向量。

*优化目标*：
为了找到最优的参数 $theta$，我们通常采用最小二乘误差（Mean Squared Error）来度量我们学到的近似值函数 $V_theta (x) $ 与环境中真实的值函数 $V^pi (x) $ 之间的差异。
其目标函数定义为：
$ epsilon_theta = bb(E)_(x ~ pi) [ (V^pi (x)  - V_theta (x) )^2 ] $


最经典的做法是使用梯度下降法（Gradient Descent）来迭代更新参数向量 $theta$。

在实际的免模型学习中，真实的 $V^pi (x) $ 是未知的。因此，算法通常会使用蒙特卡罗方法的回报 $R$ 或时序差分方法的 TD 目标 $(r + gamma V_theta(x'))$ 来作为真实价值的替代标签，从而计算梯度并更新 $theta$。


=== 状态-动作值函数近似

在缺乏环境动力学模型的控制任务中，我们需要评估的是状态-动作值函数 $Q(x,a)$。类似地，它的线性近似可以表示为：
$ Q_theta(x,a) = theta^top(x,a) $

这里，输入变成了状态和动作的联合特征向量。在假定动作空间是有限的离散空间时，可以使用One-hot encoding处理动作向量。例如，若系统总共有 $K$ 个可能动作，当前采取了第 $k$ 个动作，则动作特征可以表示为一个除第 $k$ 位为 1，其余全为 0 的向量：
$ a = (0, ..., 1, ..., 0) $

将该动作特征与状态特征进行拼接或张量积计算，即可作为线性模型的输入。

#remark[
  虽然线性近似简单高效，但其表达能力有限。在面对极其复杂的非线性环境时，通常需要引入更强大的非线性值函数近似技术，例如核方法（Kernel Methods）或者是现代深度强化学习（Deep Reinforcement Learning）中广泛使用的深度神经网络（如 DQN 中的神经网络）。
]

== 模仿学习 (Imitation Learning)

在经典的强化学习任务中，我们通常假设环境能够提供明确的奖赏函数 $R(x)$（例如游戏得分）。但在许多现实场景中（如自动驾驶、机器人抓取），人工设计一个完美反映任务目标的奖赏函数极其困难。

#definition[模仿学习 (Imitation Learning)][
  当环境奖赏函数难以定义时，模仿学习提供了一种替代方案：直接利用人类专家在环境中的决策记录（即专家范例）来进行学习，让机器模仿人类专家的行为模式。
]

=== 专家轨迹数据集 (Expert Demonstrations)

在模仿学习中，智能体不依赖环境奖赏，而是依赖一组由专家展示的优质轨迹数据集。

假设我们收集了 $m$ 条专家轨迹，构成数据集 $D$：
$D = {tau_1, tau_2, ..., tau_m}$

其中，每一条轨迹 $tau_i$ 记录了专家在完整回合中经历的“状态-动作”序列：
$tau_i = angle.l s_1^i, a_1^i, s_2^i, a_2^i, ..., s_(n_i+1)^i angle.r$
这里 $n_i$ 表示第 $i$ 条轨迹的步数。



=== 行为克隆 (Behavioral Cloning)



#remark[
  行为克隆直接将模仿学习问题退化为一个传统的监督学习问题。它不再考虑状态转移的长期影响，而是简单粗暴地学习一个从“状态”到“动作”的直接映射。
]



1. *数据展平与重组*：首先，将包含时序关系的轨迹数据集 $D$ 拆解为一个个独立的“状态-动作”对。此时，状态 $s$ 视为特征输入，专家采取的动作 $a$ 视为标签 (Label)：
   $D = {(s_1, a_1), (s_2, a_2), ..., (s_(sum_(i=1)^m n_i), a_(sum_(i=1)^m n_i))}$
2. *监督学习寻优*：
   - 如果动作空间是*离散*的（如走迷宫的上下左右），则训练一个*分类器* $pi(s)$，最小化交叉熵等分类损失。
   - 如果动作空间是*连续*的（如方向盘转角），则训练一个*回归器* $pi(s)$，最小化均方误差等回归损失。

#note[
  行为克隆实现简单且收敛快，但它的致命缺点是缺乏对长远状态转移的规划。一旦智能体在执行中因微小误差偏离了专家轨迹分布，就会进入从未见过的状态，导致误差随时间步不断累积甚至崩溃。
]



=== 逆强化学习 (Inverse Reinforcement Learning, IRL)

#remark[
  常规强化学习是“已知奖赏函数，求最优策略”；逆强化学习则是*“已知最优策略（专家轨迹），反推潜在的奖赏函数”*。
]

在 IRL 的标准设定中，我们通常假设真实的奖赏函数 $R(x)$ 可以被建模为状态特征向量 $x$ 的线性组合：
$R(x) = w^T x$，
其中，$w$ 是我们需要从专家数据中学习的未知权重向量。

我们的目标是寻找一个权重 $w^*$，使得在此奖赏函数下，专家策略的期望累积奖赏（即状态特征的期望加权和）显著优于任何其他随机采样的策略。

#algorithm[迭代式逆强化学习算法 (Iterative IRL)][
  该算法通过交替更新“奖赏函数权重”与“最优策略”来逼近专家的真实意图。
  
  *输入*：环境 $E$，状态空间 $X$，动作空间 $A$，专家轨迹数据集 $D = {tau_1, tau_2, ..., tau_m}$。
  
  *过程*：
  1. *计算专家特征期望*：从范例轨迹 $D$ 中计算出专家的状态加权和的均值向量 $overline(x)^*$（这代表了专家的平均表现水平）。
  2. *初始化*：随机初始化一个策略 $pi$。
  3. `for` $t = 1, 2, ...$ `do`：
     - 在环境中执行当前策略 $pi$，采样生成轨迹，并计算出该策略下的状态加权和均值向量 $overline(x)_t^pi$。
     - *更新奖赏权重 $w$*：#note[寻找一个新的权重 $w^*$，使得在此权重下，专家的表现尽可能大地拉开与当前已知所有策略 $pi_i$ (其中 $i=1...t$) 表现的差距。这是一个最大最小化优化问题。]
        $w^* = arg max_w min_(i=1)^t w^T (overline(x)^* - overline(x)_i^pi)$
        $text("s.t.") ||w|| <= 1$
     - *更新策略 $pi$*：在更新了奖赏函数的环境 $angle.l X, A, tilde(R)(x) = (w^*)^T x angle.r$ 中，利用任何常规强化学习算法（如有模型的值迭代、免模型的 Q-learning）重新求解出当前环境的最优策略，赋给 $pi$。
  7. `end for`
  
  *输出*：反推得到的奖赏函数 $R(x) = (w^*)^T x$ 以及最终收敛的最优策略 $pi$。
]

#remark[
  迭代式 IRL 算法本质上是一个生成对抗过程：奖赏函数试图区分专家轨迹和机器生成的轨迹（找出能最大化表现差距的 $w$），而策略优化的过程则试图生成更高质量的轨迹来最大化当前奖赏，直到机器的表现逼近专家的特征期望 $overline(x)^*$，无法被进一步区分。
]

== 基于人类反馈的强化学习 (RLHF)

在掌握了经典的强化学习理论后，本节将视角转向前沿，探讨强化学习在现代大语言模型（LLM，如 ChatGPT、Claude）中的核心应用形式——基于人类反馈的强化学习 (Reinforcement Learning from Human Feedback, RLHF)。

=== 强化学习基础概念回顾

在引入大模型对齐之前，我们先系统回顾强化学习的核心组件与智能体分类。

==== 状态、观测与动作空间
- *状态与观测*：环境的真实底层情况称为状态 (State, $S_t$)，而智能体实际能看到的信息称为观测 (Observation, $O_t$)。在完全可观测 (Fully Observed) 环境中 $O_t = S_t$；在部分可观测 (Partially Observed) 环境中，智能体需要根据历史序列 $H_t = o_1, a_1, r_1, ..., o_t, a_t, r_t$ 提取当前状态 $S_t = f(H_t)$。
- *动作空间 (Action Space)*：
  - *离散动作空间 (Discrete)*：动作数量有限，如围棋 (Go) 的 361 个落子点。
  - *连续动作空间 (Continuous)*：动作取值在连续区间内，如机器人的关节控制扭矩。

==== 策略与值函数
- *策略 (Policy)*：
  - 随机性策略 (Stochastic Policy)：$pi(a|s) = p(a_t=a|s_t=s)$。
  - 确定性策略 (Deterministic Policy)：$a^* = arg max_a pi(a|s)$。
- *值函数 (Value Function)*：引入折扣因子 $gamma$，回顾长期累积价值的期望：
  - 状态值函数：$V_pi (s) = bb(E)_pi [ sum_(k=0)^oo gamma^k r_(t+k+1) | s_t = s ]$
  - 状态-动作值函数：$Q_pi (s,a) = bb(E)_pi [ sum_(k=0)^oo gamma^k r_(t+k+1) | s_t = s, a_t = a ]$

==== 智能体分类
根据智能体内部优化的核心目标，可分为三类：
1. *基于值的智能体 (Value-based Agent)*：内部只维护值函数，通过贪心地选择值最大的动作来隐式生成策略（如 Q-learning）。
2. *基于策略的智能体 (Policy-based Agent)*：不维护值函数，直接参数化策略网络，输出动作的概率分布。
3. *Actor-Critic 智能体*：结合上述两者。Actor（演员）负责输出策略，Critic（评论家）负责评估值函数，Critic 辅助 Actor 更好、更稳地更新。


=== 大模型对齐挑战与 3H 原则

大语言模型（如基于 Transformer 架构的模型）本质上是“文字接龙”的概率模型，通过海量语料进行预训练。但这会导致模型可能输出有害、偏见或不符合人类价值观的内容。

#remark[AI 对齐与 3H 原则][
  
AI 对齐 (AI Alignment) 是指确保人工智能系统的目标和行为与人类的价值观、意图相一致。为了规范 LLM 的行为，OpenAI 等机构提出了模型回复必须遵循的 *3H 原则*：
  - *Helpfulness (有用性)*：模型应遵循用户的指令，提供有价值、能解决问题的信息。
  - *Honesty (诚实性)*：模型应基于事实回答，不能产生幻觉 (Hallucination) 或胡编乱造。
  - *Harmlessness (无害性)*：模型绝不能输出具有攻击性、歧视性或危险（如制造武器指南）的内容。
]

传统监督学习很难兼顾这种多维度的、模糊的偏好，这就促使了 RLHF 技术的诞生。


=== RLHF 核心流水线 (Pipeline)


RLHF 并非单一算法，而是一套将人类主观偏好转化为机器可优化目标的工程流水线。整个过程通常分为三个核心步骤。

==== 步骤 1：监督微调 (Supervised Fine-Tuning, SFT)
预训练模型具备了丰富的世界知识，但还不擅长“对话”。在这一步，我们收集高质量的人类问答对 $(x, y_1, y_2, ..., y_T)$，利用传统的交叉熵损失函数对模型进行监督微调。
*目的*：让模型学会遵循指令和基础的对话格式，此时得到的基础模型称为 *SFT 模型*。

==== 步骤 2：训练奖励模型 (Reward Model, RM)
既然无法用代码写出完美的奖赏函数 $R(x)$，我们就训练一个神经网络来充当“人类裁判”。
- *数据收集*：给定一个输入 $x$，让 SFT 模型生成多个不同的回答。
- *人类标注*：人类标注员根据 3H 原则，对这些回答进行好坏排序（人类偏好）。
- *模型训练*：训练一个奖励模型，输入文本，输出一个标量奖励值 $r$。使得 RM 的打分能够完美拟合人类的排序偏好。

==== 步骤 3：近端策略优化 (Proximal Policy Optimization, PPO) 闭环迭代

这是 RLHF 中数学和工程最复杂的环节，通常采用 Actor-Critic 架构的变体算法 PPO。

#remark[PPO 与四大模型协作][
  *核心概念*：在 RLHF 的最后阶段，系统并不是只有一个模型在运行，而是有 *四个模型* 在内存中同时协作：
  1. *策略模型 (Policy Model / Actor)*：需要被优化的主角，初始化为 SFT 模型，负责不断生成新的回答。
  2. *参考模型 (Reference Model)*：被冻结的原始 SFT 模型。用于计算 KL 散度惩罚，防止策略模型在追求高奖励时“走火入魔”，偏离正常人类语言分布。
  3. *奖励模型 (Reward Model)*：被冻结的裁判，对策略模型生成的回答给出环境奖赏 $r$。
  4. *评论模型 (Critic Model)*：预测当前状态（对话上下文）的价值，辅助 Actor 更新。
]

1. *采样阶段*：用户输入提示词 $x$，策略模型逐步生成回答 $y_t$。生成完毕后，奖励模型给出整句话的得分。同时，参考模型给出相同序列的概率分布，用于计算相对于初始分布的 KL 散度偏移。
2. *广义优势估计 (Generalized Advantage Estimation, GAE)*：
   - 评论模型输出每个时刻的价值预测。
   - 利用真实获得的回报 (Return) 与评论模型的预测值作差，计算出 *TD 误差 (TD Error)*。
   - GAE 结合多步 TD 误差，精确估算出当前动作的 *优势函数 (Advantage Function)*（即当前动作比平均表现好多少）。
3. *参数更新阶段*：基于计算出的优势函数，更新网络参数。此时涉及到三个联合优化的损失函数：
   - *PPO-clip 损失函数*：负责更新策略模型。利用 Clipping 机制限制每次策略更新的幅度，保证训练极其稳定，防止策略崩溃。
   - *均方差误差损失 (MSE Loss)*：负责更新评论模型，使其对价值的预测越来越准。
   - *语言模型损失 (LM Loss)*：可选，通常在目标函数中混入部分原始的预训练损失，确保模型不会在强化学习过程中遗忘基础知识。