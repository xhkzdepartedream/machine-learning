#import"../../notes.typ":*
#show:notes

= 深度学习
== 前馈神经网络
网络启发而发明的由神经元连接组成的网络状机器学习模型。前馈神经网络（feedforward neural network）或多层感知机(multilayer perceptron，MLP)）是最具代表性的神经网络，主要用于监督学习，如分类和回归。

前馈神经网络由多层神经元组成，层间的神经元相互连接，层内的神经元不连接。其信息处理机制是：前一层神经元通过层间连接向后一层神经元传递信号，因为信号是从前往后转递的，所以是“前馈的”信息处理网络。这里，神经元是对多个输入信号（实数向量）进行非线性转换产生一个输出信号（实数值）的函数，*整个神经网络是对多个输入信号（实数向量）进行多次非线性转换产生多个输出信号（实数向量）的复合函数*。每一个神经元的函数含有参数，神经网络的神经元的参数通过学习得到。当前馈神经网络的层数达到一定数量时（一般大于2)，又称为深度神经网络（deep neural network，DNN)。

*前馈神经网络学习算法是反向传播（backpropagation）算法*，是*随机梯度下降法*的具体实现。学习的损失函数通常在分类时是交叉熵损失，在回归时是平方损失，其最小化等价于极大似然估计。学习的正则化方法包括早停法（earlystopping)、暂退法（dropout）。

=== 前馈神经网络模型
神经元的本质是一个函数，以实数向量为输入，实数值为输出的非线性函数，表示多个输入信号到一个输出信号的非线性转换。

#definition[神经元][
  神经元是如下定义的非线性函数：

$ y = f(x_1, x_2, dots, x_n) = a(sum_(i=1)^n w_i x_i + b) $

或者写作

$ y = f(x_1, x_2, dots, x_n) = a(z), z = sum_(i=1)^n w_i x_i + b $

其中，$x_1, x_2, dots, x_n$ 是输入，取实数值；$y$ 是输出，取实数值；$z$ 是中间结果，又称作净输入（net input），也取实数值；$w_1, w_2, dots, w_n$ 是权重（weight），$b$ 是偏置（bias），也都取实数值；$z = sum_(i=1)^n w_i x_i + b$ 是仿射函数；$a(dot)$ 是特定的非线性函数，称为激活函数（activation function）。激活函数有多种形式，比如 S 型函数：

$ a(z) = 1 / (1 + e^(-z)) $
]

多层前馈神经网络或前馈神经网络是如下定义的非线性函数的复合函数。输入是 $x_i, i = 1, 2, dots, n$，输出是 $y_k, k = 1, 2, dots, l$。神经网络有 $s$ 层 $(s >= 2)$。第一层到第 $s-1$ 层是隐层。其他内容省略，整体上是多个函数的复合而已。

隐层神经元函数由两部分组成：仿射函数和激活函数。这里介绍常用的隐层激活函数，包括S型函数、双曲正切函数、整流线性函数。一个神经网络通常采用一种隐层激活函数。

1. *S 型函数 (Sigmoid Function)*
S 型函数（或 Logistic 函数）将输入映射到 $(0, 1)$ 区间，常用于早期的神经网络，但存在梯度消失问题。公式为：
$ sigma(z) = 1 / (1 + e^(-z)) $

2. *双曲正切函数 (Hyperbolic Tangent Function)*
双曲正切函数（tanh）形状类似于 S 型函数，但输出范围是 $(-1, 1)$，且以 0 为中心，收敛速度通常快于 S 型函数。公式为：
$ tanh(z) = (e^z - e^(-z)) / (e^z + e^(-z)) $

3. *整流线性函数 (Rectified Linear Unit, ReLU)*
ReLU 是目前深度学习中最常用的激活函数。它计算简单，且在正区间梯度恒为 1，有效缓解了梯度消失问题。公式为：
$ f(z) = max(0, z) = cases(z\, & z >= 0, 0\, & z < 0) $

输出层神经元函数由两部分组成：仿射函数和激活函数。输出层激活函数通常使用恒等函数、S型函数、软最大化函数。在回归、二类分类、多类分类、多标签分类中，激活函数有不同的形式。根据任务的不同（回归或分类），输出层神经元 ($s$ 层) 采用不同的激活函数 $g(dot)$。令 $h^((s-1))$ 为上一隐层的输出，则输出层的净输入为 $z = w^((s)T) h^((s-1)) + b^((s))$。

(1) 回归任务 (Regression)

输出层只有一个神经元，激活函数为恒等函数。输出即为线性预测值：
$ y = g(z) = z $

(2) 二类分类 (Binary Classification)

输出层只有一个神经元，激活函数为 S 型函数 (Sigmoid)。输出表示属于正类的后验概率：
$ P(y=1|x) = 1 / (1 + e^(-z)) $

(3) 多类分类 (Multi-class Classification)

输出层有 $l$ 个神经元，采用 *Softmax* 函数（软最大化函数）。第 $k$ 类别的概率为：
$ P(y_k=1|x) = e^(z_k) / (sum_(i=1)^l e^(z_i)) $
其中 $z_k$ 称为对数几率 (logit)。

(4) 多标签分类 (Multi-label Classification)

输出层有 $l$ 个神经元，每个神经元独立使用 S 型函数。这意味着一个样本可以同时属于多个类别：
$ P(y_k=1|x) = 1 / (1 + e^(-z_k)), quad k=1, 2, dots, l $

在前馈神经网络用于多类分类的预测阶段（非训练阶段），为了提高效率，通常 *省去 Softmax 激活函数的计算*。
直接选取净输入（对数几率）$z_k$ 最大的类别作为预测结果：
$ arg max_k P(y_k=1|x) arrow arg max_k z_k $
这是因为指数函数是单调递增的，比较 $z_k$ 的大小与比较概率的大小结果等价。

=== 反向传播算法

多层前馈神经网络（Multilayer Feedforward Neural Networks）通常具有极强的非线性映射能力。然而，由于网络中存在隐含层，我们无法直接观察到隐层神经元的“理想输出”，这使得早期的训练变得非常困难。

*反向传播算法 (Backpropagation Algorithm, BP)* 的提出解决了这一难题。其核心思想是：将训练集数据输入神经网络的输入层，经过隐含层逐层处理，最后传向输出层（*前向传播*）；如果输出层的实际输出与期望输出不符，则将误差沿着原来的连接通路返回（*反向传播*）。在反向传播过程中，通过 *链式法则 (Chain Rule)* 计算误差对各层权重的梯度，并修正各层神经元的权重，直到误差达到期望值或达到最大迭代次数。

BP 算法本质上是 *梯度下降法 (Gradient Descent)* 在多层复合函数上的应用。

#remark[
 BP 算法之所以被称为“反向传播”，是因为 *信号是正向流动的*（从输入到输出），而 *误差梯度是反向传播的*（从输出层回流至输入层），用来告诉前面的层：“你们哪里做错了，需要改多少”。
]


假设给定的训练数据集 $D = {(x_1, y_1), (x_2, y_2), dots, (x_N, y_N)}$，其中 $x_i in RR^d$，$y_i in RR^l$。
我们定义网络的损失函数（通常为均方误差）：
$ E_k = 1/2 sum_(j=1)^l (hat(y)_j^k - y_j^k)^2 $
其中 $hat(y)$ 是网络预测值，$y$ 是真实标签。

#algorithm[标准反向传播 (Standard BP) 算法][
 *输入：* 训练数据集 $D$；学习率 $eta$。 \
 *输出：* 连接权重 $w$ 和偏置 $b$。

 + *初始化*：在 $(0, 1)$ 范围内随机初始化网络中所有连接权重和偏置。
 + *重复下列步骤，直到满足停止条件*（如误差小于阈值）：
  + *对数据集中每一个样本 $(x_k, y_k)$：*
   + *前向传播*：
    将 $x_k$ 输入网络，计算每一层神经元的输入 $z$ 和输出 $a$，直到获得输出层结果 $hat(y)_k$。
   + *计算输出层梯度*：
    计算输出层神经元的误差项 $delta$（梯度的一部分）。
    $ g_j = hat(y)_j^k (1 - hat(y)_j^k) (y_j^k - hat(y)_j^k) $
   + *反向计算隐层梯度*：
    从输出层向后，计算隐层神经元的误差项 $e_h$。
    $ e_h = b_h (1 - b_h) sum_(j=1)^l w_(h j) g_j $
   + *更新参数*：
    根据计算出的梯度更新权重与偏置。
    $ w_(h j) arrow.l w_(h j) + eta g_j b_h $
    $ v_(i h) arrow.l v_(i h) + eta e_h x_i $
]

在上述算法中：
- 假设激活函数为 Sigmoid 函数 $sigma(z) = 1/(1+e^(-z))$，因为公式中出现了 $y(1-y)$ 形式的导数。
- $w_(h j)$ 表示隐层第 $h$ 个神经元到输出层第 $j$ 个神经元的权重。
- $v_(i h)$ 表示输入层第 $i$ 个神经元到隐层第 $h$ 个神经元的权重。
- $b_h$ 是隐层神经元的输出。


BP 算法的数学本质极其优美，它完全依赖于微积分中的 *链式法则*。为了简化推导，我们考虑一个包含一个隐层的神经网络，并针对 *单个样本* 进行误差分析。

*1. 符号定义*

- *输入*：$x in RR^d$
- *隐层权重*：$W^((1)) in RR^(q times d)$（$q$ 为隐层节点数）
- *隐层净输入*：$z^((1)) = W^((1)) x + b^((1))$
- *隐层输出*：$a^((1)) = sigma(z^((1)))$
- *输出层权重*：$W^((2)) in RR^(l times q)$
- *输出层净输入*：$z^((2)) = W^((2)) a^((1)) + b^((2))$
- *最终输出*：$a^((2)) = sigma(z^((2)))$
- *损失函数*：$E = 1/2 ||y - a^((2))||^2 = 1/2 sum_(k=1)^l (y_k - a_k^((2)))^2$

*2. 输出层权重的梯度*

我们需要计算损失函数 $E$ 对输出层权重 $w_(j k)^((2))$ 的偏导数（即权重 $w_(j k)$ 连接隐层节点 $j$ 和输出节点 $k$）。

根据链式法则：
$ (partial E) / (partial w_(j k)^((2))) = (partial E) / (partial a_k^((2))) dot (partial a_k^((2))) / (partial z_k^((2))) dot (partial z_k^((2))) / (partial w_(j k)^((2))) $

我们逐项计算：
+ *误差对输出的导数*：
 $ (partial E) / (partial a_k^((2))) = -(y_k - a_k^((2))) $
+ *输出对净输入的导数*（假设 Sigmoid 激活函数）：
 $ (partial a_k^((2))) / (partial z_k^((2))) = sigma'(z_k^((2))) = a_k^((2)) (1 - a_k^((2))) $
+ *净输入对权重的导数*：
 $ z_k^((2)) = sum_j w_(j k)^((2)) a_j^((1)) + b_k^((2)) arrow.double (partial z_k^((2))) / (partial w_(j k)^((2))) = a_j^((1)) $

定义输出层的 *误差项 (Error Term)* $delta_k^((2))$ 为：
$ delta_k^((2)) equiv (partial E) / (partial z_k^((2))) = -(y_k - a_k^((2))) dot a_k^((2)) (1 - a_k^((2))) $

则输出层权重的梯度为：
$ (partial E) / (partial w_(j k)^((2))) = delta_k^((2)) dot a_j^((1)) $

*3. 隐层权重的梯度（反向传播的核心）*

现在计算输入层到隐层的权重 $w_(i j)^((1))$ 的梯度。这一步需要将误差从输出层“反传”回来。

$ (partial E) / (partial w_(i j)^((1))) = sum_(k=1)^l ( (partial E) / (partial z_k^((2))) dot (partial z_k^((2))) / (partial a_j^((1))) ) dot (partial a_j^((1))) / (partial z_j^((1))) dot (partial z_j^((1))) / (partial w_(i j)^((1))) $

注意这里出现了一个求和符号 $sum_k$，因为隐层节点 $j$ 的输出 $a_j^((1))$ 影响了输出层 *所有* 的节点 $k$，所以误差必须从所有输出节点回传。

+ *回传误差*：
 已知 $(partial E) / (partial z_k^((2))) = delta_k^((2))$。
 且 $z_k^((2)) = sum_j w_(j k)^((2)) a_j^((1)) + b$，故 $(partial z_k^((2))) / (partial a_j^((1))) = w_(j k)^((2))$。
+ *隐层激活导数*：
 $(partial a_j^((1))) / (partial z_j^((1))) = sigma'(z_j^((1))) = a_j^((1)) (1 - a_j^((1)))$
+ *输入项*：
 $(partial z_j^((1))) / (partial w_(i j)^((1))) = x_i$

定义隐层的 *误差项* $delta_j^((1))$：
$ delta_j^((1)) equiv (partial E) / (partial z_j^((1))) = ( sum_(k=1)^l delta_k^((2)) w_(j k)^((2)) ) dot sigma'(z_j^((1))) $

则隐层权重的梯度为：
$ (partial E) / (partial w_(i j)^((1))) = delta_j^((1)) dot x_i $

#remark[
 *通用的更新规则：*
 无论是哪一层，权重的更新公式都遵循统一的形式：
 $ w arrow.l w - eta dot delta_("current") dot "Input"_("prev") $
 即：*（学习率）$times$（当前层误差项）$times$（上一层输入）*。
]


在深度神经网络中，BP 算法容易遇到严重的梯度问题。

观察隐层误差项的公式：
$ delta^((1)) = (W^((2)))^T delta^((2)) dot sigma'(z^((1))) $
如果网络很深，根据链式法则，梯度将包含多个 $sigma'(z)$ 的连乘。

1. *梯度消失 (Vanishing Gradient)*：
 Sigmoid 函数的导数 $sigma'(z) in (0, 0.25]$。如果网络有 10 层，梯度可能会乘以 $0.25^10 approx 10^(-6)$，导致底层权重几乎不更新，网络无法学习。
 *解决方案*：使用 ReLU 激活函数（正区间导数为 1），或使用 LSTM 等特殊结构。

2. *梯度爆炸 (Exploding Gradient)*：
 如果权重 $W$ 初始化得过大，连乘后梯度会指数级增长，导致权重更新幅度过大，网络震荡发散。
 *解决方案*：梯度裁剪 (Gradient Clipping)，层归一化 (Layer Normalization)。

