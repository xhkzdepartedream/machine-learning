#import "../../notes.typ":*
#show:notes

== Bagging 与 随机森林

*集成学习*（Ensemble Learning）通过构建并结合多个学习器来完成学习任务，通常能够获得比单一学习器显著优越的泛化性能。集成学习主要分为两大类：一类是利用基学习器之间的依赖关系进行串行生成的Boosting（如AdaBoost, GBDT）；另一类是利用基学习器之间的独立性进行并行生成的Bagging，以及其特例*随机森林*（Random Forest）。随机森林方法简单、易实现、计算开销小，在很多实际任务中展现出强大的性能，被誉为“代表集成学习技术水平的方法”。


=== Bagging (Bootstrap Aggregating)

Bagging 是并行式集成学习方法的典型代表。它的核心思想是通过*自助采样法*（Bootstrap Sampling）构建出具有差异性的训练数据集，分别训练基学习器，最后通过聚合策略（如多数投票或平均法）得到最终结果。Bagging 的主要目的是*降低方差*（Variance），从而提高模型的稳定性，能够有效减少过拟合。

#definition[自助采样 (Bootstrap Sampling)][
  给定包含 $m$ 个样本的数据集 $D$，我们对它进行采样产生数据集 $D'$：每次随机从 $D$ 中挑选一个样本，将其拷贝放入 $D'$，然后再将该样本放回初始数据集 $D$ 中，使得该样本在下次采样时仍有可能被采到。
  
  重复执行 $m$ 次后，我们就得到了一个包含 $m$ 个样本的数据集 $D'$。
]

#theorem[
  在自助采样中，初始数据集 $D$ 中约有 36.8% 的样本未出现在采样数据集 $D'$ 中。这些未使用的样本称为*袋外样本*（Out-of-Bag, OOB），可用于验证模型性能。
]

#proof[
  假设数据集 $D$ 中有 $m$ 个样本。每次采样选中某个特定样本的概率是 $1/m$，不被选中的概率是 $1 - 1/m$。
  经过 $m$ 次采样，该样本始终未被选中的概率为：
  $ p = (1 - 1/m)^m $
  取极限 $m -> infinity$:
  $ lim_(m->infinity) (1 - 1/m)^m = 1/e approx 0.368 $
]

基于自助采样，Bagging 的基本流程如下：

1.  *采样*：从原始训练集 $D$ 中使用 Bootstrap 方法采样 $T$ 次，得到 $T$ 个含 $m$ 个训练样本的采样集 $D_1, D_2, dots, D_T$。
2.  *训练*：基于每个采样集 $D_t$，训练出一个基学习器 $h_t (x)$。
3.  *聚合*：
    -   *分类任务*：使用*简单投票法*，即 $H(x) = arg max_(y) sum_(t=1)^T I(h_t (x) = y)$。
    -   *回归任务*：使用*简单平均法*，即 $H(x) = 1/T sum_(t=1)^T h_t (x)$。

Bagging 对于不稳定的基学习器（如不剪枝的决策树、神经网络）效果最好，因为这些模型对数据分布的微小变化非常敏感，而 Bagging 通过平滑这种敏感性来降低方差。

#remark[

虽然 Bagging 框架本身是通用的，但在实际应用（尤其是随机森林）中，基学习器的训练方法有非常具体的最佳实践。简单来说：通常使用 CART 决策树，且不进行剪枝。

*原因*：Bagging 主要用于降低*方差*。它最适合那些对数据分布敏感的、*不稳定*（Unstable）的算法。
- *不稳定*：训练数据稍微变动一点，模型结构就会发生剧烈变化。决策树就是典型的不稳定算法。
- *稳定*：如 k-NN、线性回归。对它们做 Bagging 效果很差，甚至可能因为重采样导致有效数据变少而起反作用。

这是随机森林训练中最反直觉的一点：*我们希望基学习器“过拟合”。*

在训练每个 $h_t (x)$ 时，通常采取以下策略：
- *完全生长（Fully Grown）*：让决策树一直分裂下去，直到叶子节点只有 1 个样本，或者纯度达到 100%。
- *不剪枝（No Pruning）*：传统决策树为了防止过拟合会进行剪枝，但在 Bagging/RF 中，我们通常*不做*剪枝。

*原理分析（偏差-方差权衡）*：
- *单棵树*：由于不剪枝，单棵树的复杂度很高，表现为*低偏差*（Low Bias），但是*高方差*（High Variance，即过拟合）。
- *集成后*：Bagging 的平均/投票机制可以极大地消除方差。
- *最终结果*：我们利用单棵树保证了整体的“低偏差”，利用聚合机制解决了“高方差”，从而得到了一个双优模型。
]

=== 随机森林 (Random Forest)

*随机森林*（Random Forest, RF）是 Bagging 的一个扩展变体。RF 在以决策树为基学习器构建 Bagging 集成的基础上，进一步在决策树的训练过程中引入了*随机属性选择*。

具体来说，传统决策树在选择划分属性时，是在当前节点的属性集合（假设有 $d$ 个属性）中选择一个最优属性；而在 RF 中，对基决策树的每个结点，先从该结点的属性集合中随机选择一个包含 $k$ 个属性的子集，然后再从这个子集中选择一个最优属性用于划分。

#note[随机森林的随机性来源][
  随机森林包含两个层面的随机性，这使得基学习器之间的相关性进一步降低：
  
  (1) *样本扰动*：通过 Bootstrap 采样，每个基决策树使用的训练集不同。
  
  (2) *属性扰动*：在分裂节点时，只考察特征的一个随机子集（通常取 $k = log_2 d + 1$ 或 $k = sqrt(d)$）。
]

#algorithm[随机森林算法][
  *输入*：训练集 $D = {(bold(x)_1, y_1), dots, (bold(x)_m, y_m)}$，属性集合 $A$，基学习器个数 $T$。
  
  *过程*：
  1.  For $t = 1, 2, dots, T$:
      -   对训练集 $D$ 进行 Bootstrap 采样，得到 $D_t$。
      -   基于 $D_t$ 训练一颗决策树 $h_t$。
      -   在树的每个节点分裂时：
          -   随机从所有属性 $A$ 中选择 $k$ 个属性构成子集 $A_k$。
          -   在 $A_k$ 中选择最优划分属性进行分裂。
          -   树生长到最大深度，通常*不剪枝*。
  2.  End For
  
  *输出*：集成模型 $H(x)$。
      -   分类：$H(x) = "MajorityVote"({h_t(x)}_(t=1)^T)$
      -   回归：$H(x) = 1/T sum_(t=1)^T h_t(x)$
]

*为什么随机森林比单纯的 Bagging 更好？*

这可以用方差的分解来解释。假设我们要计算 $T$ 个独立同分布的随机变量均值的方差。
如果变量完全独立，方差为 $sigma^2/T$。但在 Bagging 中，由于使用的是重叠的数据集，树之间存在正相关性。

设单棵树的方差为 $sigma^2$，任意两棵树之间的相关系数为 $rho$。则集成模型（$T$ 棵树均值）的方差为：

$ op("Var")(1/T sum h_i (x)) = rho sigma^2 + (1-rho)/T sigma^2 $

#proof[

假设我们有 $T$ 个随机变量（代表 $T$ 棵树的预测输出）$X_1, X_2, dots, X_T$。
我们要计算的是它们的均值 $overline(X) = 1/T sum_(i=1)^T X_i$ 的方差。

根据方差的性质，$op("Var")(a X) = a^2 op("Var")(X)$。这里 $a = 1/T$，所以：
$ op("Var")(overline(X)) = op("Var")(1/T sum_(i=1)^T X_i) = 1/T^2 op("Var")(sum_(i=1)^T X_i) $

展开求和项的方差：
$ op("Var")(sum_(i=1)^T X_i) = sum_(i=1)^T op("Var")(X_i) + sum_(i != j) op("Cov")(X_i, X_j) $

这里包含两部分：
1.  *方差项*（对角线元素）：共有 $T$ 个项。假设每棵树的方差相同，即 $op("Var")(X_i) = sigma^2$。
    $ sum_(i=1)^T op("Var")(X_i) = T sigma^2 $
2.  *协方差项*（非对角线元素）：共有 $T times T - T = T(T-1)$ 个项。
    根据相关系数定义 $rho_(i j) = op("Cov")(X_i, X_j) / (sigma_i sigma_j)$。
    假设所有树两两之间的相关系数均为 $rho$，且方差均为 $sigma^2$，则 $op("Cov")(X_i, X_j) = rho sigma^2$。
    $ sum_(i != j) op("Cov")(X_i, X_j) = T(T-1) rho sigma^2 $

将这两部分代回原式：
$ op("Var")(overline(X)) &= 1/T^2 [ T sigma^2 + T(T-1) rho sigma^2 ] \
&= 1/T sigma^2 + (T-1)/T rho sigma^2 \
&= 1/T sigma^2 + (1 - 1/T) rho sigma^2 \
&= 1/T sigma^2 + rho sigma^2 - 1/T rho sigma^2 \
&= rho sigma^2 + 1/T (1 - rho) sigma^2 $
]

#remark[
  在现实中，Tree 1 和 Tree 2 的相关性肯定不等同于 Tree 1 和 Tree 3。那为什么公式里用同一个 $rho$？

这里的 $rho$ 实际上代表的是 *“平均相关系数”* (Average Correlation)。理由如下：
]
-   *第一项* $rho sigma^2$：当 $T -> infinity$ 时，这一项保持不变。这意味着仅仅增加树的数量不能无限降低方差，瓶颈在于树之间的相关性 $rho$。
-   *第二项* $(1-rho)/T sigma^2$：随着 $T$ 增加，这一项趋向于 0。

Bagging 通过样本扰动降低了 $rho$。而随机森林通过*属性扰动*，强制让树长得“不一样”，进一步大幅降低了 $rho$，从而在第一项上取得了更好的减方差效果，虽然这可能会轻微增加单棵树的偏差（因为只能用部分特征），但整体泛化误差通常更低。



在分类问题中，除了硬投票（Hard Voting，直接看类别），随机森林也常用软投票（Soft Voting），即平均所有树对各类的预测概率，通常效果更好。

*OOB 误差估计示例*

由于 Bootstrap 的特性，我们可以直接利用袋外样本（OOB）来估计泛化误差，而无需专门划分验证集。

1.  对于每个样本 $(bold(x)_i, y_i)$，找出那些*没有*使用该样本进行训练的决策树集合，记为 $H_i^("oob")$。
2.  仅利用 $H_i^"oob"$ 中的树对 $bold(x)_i$ 进行预测，得到预测结果 $hat(y)_i^"oob"$。
3.  OOB 误差率计算为：
    $ E_("oob") = 1/m sum_(i=1)^m I(hat(y)_i^"oob" != y_i) $

这种估计在 $m$ 足够大时，与 $k$ 折交叉验证的结果非常接近。

=== 随机森林的优缺点分析

为了更全面地理解随机森林，我们将其性质总结如下：

*优点：*

1.  *准确性高*：在许多数据集上表现优异，通常不需要复杂的调参。
2.  *抗过拟合*：由于双重随机性（样本和特征），极大地降低了方差。
3.  *处理高维数据*：能够处理成千上万的输入变量，且不需要做特征选择（它自己会做）。
4.  *特征重要性*：训练完成后，可以给出哪些特征比较重要（Feature Importance）。
5.  *并行化*：树与树之间互不依赖，训练速度可以高度并行化，比 Boosting 快。

*缺点：*

1.  *模型解释性差*：相比于单棵决策树（白盒模型），几百棵树的集成变成了“黑盒”。
2.  *对噪声过大的数据*：如果数据噪声特别大，可能会陷入过拟合（虽然比单棵树好）。
3.  *计算资源*：需要存储大量的树模型，预测时需要遍历所有树，实时性要求极高的场景（微秒级）可能受限。

=== 代码实现：特征重要性评估

随机森林的一个重要副产物是特征重要性评估。主要有两种方法：

1.  *基于基尼指数（MDI）*：在训练过程中，记录每个特征在所有树的分裂节点上减少的杂质（Gini Impurity）总和。减少得越多，特征越重要。
2.  *基于 OOB 数据置换（MDA）*：
    -   用训练好的森林在 OOB 数据上计算误差 $E_0$。
    -   保持其他列不变，随机打乱 OOB 数据中特征 $j$ 的数值，再次计算误差 $E_j$。
    -   特征重要性 $"Imp"_j = E_j - E_0$。如果打乱特征 $j$ 后误差大幅上升，说明该特征很重要。

#algorithm[OOB 特征重要性计算 (MDA)][
  *输入*：训练好的随机森林 $"RF"$，袋外数据 $D_("oob")$。
  *输出*：特征重要性向量 $bold(I)$。

  1. 计算原始 OOB 准确率：$"acc"_0 = "Evaluate"("RF", D_("oob"))$
  2. For 每个特征 $j in {1, dots, p}$:
     - 复制 $D_("oob")$ 为 $D'_j$。
     - *随机打乱* $D'_j$ 中第 $j$ 列的数据顺序。
     - 计算打乱后的准确率：$"acc"_j = "Evaluate"("RF", D'_j)$
     - 计算重要性：$I_j = "acc"_0 - "acc"_j$
  3. End For
  4. 对 $bold(I)$ 进行归一化处理。
]

这为特征选择提供了一种非常实用的非参数统计方法。

#example[
  *任务定义*：
1.  *分类 ($y_c$)*：判断水果是否是“苹果”（1=是，0=否）。
2.  *回归 ($y_r$)*：预测水果的“甜度”（0-10）。

*数据集 $D$ (4 个样本)*：
#figure(
  table(
    columns: 5,
    inset: 8pt,
    align: center,
    [*ID*], [*特征1: 颜色 ($x_1$)*], [*特征2: 大小 ($x_2$)*], [*分类目标 ($y_c$)*], [*回归目标 ($y_r$)*],
    [1], [1 (红)], [1 (小)], [1 (是)], [7],
    [2], [1 (红)], [2 (大)], [1 (是)], [9],
    [3], [0 (绿)], [1 (小)], [0 (否)], [3],
    [4], [0 (绿)], [2 (大)], [0 (否)], [1]
  ),
  caption: "原始训练集"
)

*森林参数设置*：
-   树的数量 $T = 2$。
-   特征子集大小 $k = 1$ (为了演示特征扰动，强制每个节点只随机看 1 个特征)。
-   最大深度 = 2。
]

*任务一：分类问题 (Classification)*

我们使用 *Gini 指数* 作为分裂标准。

*第一步：构建第一棵树 (Tree 1)*

1.  *Bootstrap 采样*：
    假设随机采样得到 ID 序列：${1, 2, 2, 3}$。
    -   训练集 $D_1$: ${(红, 小, 1), (红, 大, 1), (红, 大, 1), (绿, 小, 0)}$。
    -   *注意*：样本 4 没有被选中，它是 *OOB 样本*。

2.  *根节点分裂*：
    -   *特征随机选择*：假设随机抽到了 *$x_1$ (颜色)*。
    -   *计算分裂*：
        -   左分支 (红): ${1, 2, 2}$ -> 类别 ${1, 1, 1}$。$"Gini" = 1 - (3/3)^2 - 0 = 0$ (纯度最高)。
        -   右分支 (绿): ${3}$ -> 类别 ${0}$。$"Gini" = 0$ (纯度最高)。
    -   *结论*：Tree 1 极其简单，规则为 "红色是苹果，绿色不是"。

*第二步：构建第二棵树 (Tree 2)*

1.  *Bootstrap 采样*：
    假设随机采样得到 ID 序列：${3, 4, 1, 4}$。
    -   训练集 $D_2$: ${(绿, 小, 0), (绿, 大, 0), (红, 小, 1), (绿, 大, 0)}$。
    -   *注意*：样本 2 是 OOB 样本。

2.  *根节点分裂*：
    -   *特征随机选择*：假设这次随机抽到了 *$x_2$ (大小)*。
    -   *计算分裂*：
        -   左分支 (小): ${3, 1}$ -> 类别 ${0, 1}$。
            $ "Gini"_("left") = 1 - (1/2)^2 - (1/2)^2 = 0.5 $
        -   右分支 (大): ${4, 4}$ -> 类别 ${0, 0}$。
            $ "Gini"_("right") = 0 $
        -   加权 Gini: $2/4 times 0.5 + 2/4 times 0 = 0.25$。
    
    虽然不完美，但这是我们唯一的选择（因为只选了 $x_2$）。
    -   左节点 (小) 包含样本 ${3, 1}$。我们需要继续分裂。

3.  *左子节点分裂*：
    -   数据: ${3(绿, 小, 0), 1(红, 小, 1)}$。
    -   *特征随机选择*：假设抽到了 *$x_1$ (颜色)*。
    -   分裂：
        -   红 -> 1 (苹果)
        -   绿 -> 0 (非苹果)
    -   完美分开。

*第三步：集成预测*

现在来了一个新水果：*样本 A (颜色=绿, 大小=小)*。它实际上是样本 3。

1.  *Tree 1 预测*：看颜色 -> 绿 -> *0 (非苹果)*。
2.  *Tree 2 预测*：
    -   看大小 -> 小 -> 进入左子树。
    -   看颜色 -> 绿 -> *0 (非苹果)*。
3.  *随机森林投票*：0 票 vs 0 票 -> 最终预测 *0*。

*任务二：回归问题 (Regression)*

我们使用 *均方误差 (MSE)* 或 *方差* 作为分裂标准。预测值为叶子节点均值。

*第一步：构建第一棵树 (Tree 1)*

1.  *Bootstrap 采样*：${1, 2, 2, 3}$。甜度值为 ${7, 9, 9, 3}$。
2.  *根节点分裂*：
    -   随机选特征 *$x_1$ (颜色)*。
    -   左 (红): ${7, 9, 9}$。均值 $bar(y)_L = 8.33$。
        $ "MSE"_L = 1/3 [(7-8.33)^2 + (9-8.33)^2 + (9-8.33)^2] approx 0.89 $
    -   右 (绿): ${3}$。均值 $bar(y)_R = 3$。$"MSE"_R = 0$。
    -   *Tree 1 结构*：如果是红，预测 8.33；如果是绿，预测 3。

*第二步：构建第二棵树 (Tree 2)*

1.  *Bootstrap 采样*：${3, 4, 1, 4}$。甜度值为 ${3, 1, 7, 1}$。
2.  *根节点分裂*：
    -   随机选特征 *$x_2$ (大小)*。
    -   左 (小): ${3, 7}$ (样本 3, 1)。均值 $bar(y)_L = 5$。
    -   右 (大): ${1, 1}$ (样本 4, 4)。均值 $bar(y)_R = 1$。
    -   *Tree 2 结构*：如果是小，预测 5；如果是大，预测 1。（假设停止分裂）。

*第三步：集成预测*

预测 *样本 2 (红, 大)*，真实甜度 9。

1.  *Tree 1 预测*：颜色是红 -> 预测 *8.33*。
2.  *Tree 2 预测*：大小是大 -> 预测 *1.0*。
    *(注意：这里 Tree 2 预测得很差，因为它训练数据里只有绿色的“大”水果，没见过红色的“大”水果，这就是欠拟合)*。
3.  *随机森林均值*：
    $ H(x) = (8.33 + 1.0) / 2 = 4.665 $

*任务三：袋外误差评估 (OOB Evaluation)*

我们不需要单独的测试集，直接用 OOB 样本评估模型的泛化能力。这里演示 *分类问题* 的 OOB 计算。

1.  *样本 1*：出现在 Tree 1, Tree 2 的训练集中。-> 没有 OOB 预测。
2.  *样本 2 (红, 大, 1)*：
    -   在 Tree 1 中是训练样本。
    -   在 Tree 2 中是 *OOB 样本* (没被抽中)。
    -   *OOB 预测*：仅用 Tree 2 预测样本 2。
        Tree 2 规则：看大小(大) -> 预测 *0*。
    -   *误差*：真实是 1，预测是 0。错！
3.  *样本 3*：出现在 Tree 1, Tree 2。-> 无 OOB。
4.  *样本 4 (绿, 大, 0)*：
    -   在 Tree 1 中是 *OOB 样本*。
    -   在 Tree 2 中是训练样本。
    -   *OOB 预测*：仅用 Tree 1 预测样本 4。
        Tree 1 规则：看颜色(绿) -> 预测 *0*。
    -   *误差*：真实是 0，预测是 0。对！

*OOB 错误率计算*：
我们只有样本 2 和样本 4 有 OOB 预测结果。
- 样本 2：错。
- 样本 4：对。
$ E_("OOB") = 1/2 = 50% $

#remark[
  因为数据太少，且模型非常简单，误差看起来很大，但逻辑是通用的。
]