#import "../../notes.typ":*
#show:notes

== Stacking (堆叠泛化)

*Stacking*（Stacked Generalization）是一种分层结构的集成学习框架。与 Bagging（并行、降低方差）和 Boosting（串行、降低偏差）不同，Stacking 通常是一种*异构集成*（Heterogeneous Ensemble）方法，它结合了多种*不同类型*的基学习器（如将 SVM、随机森林、KNN 集成在一起）。

Stacking 的核心思想是：“三个臭皮匠，顶个诸葛亮”。它不是简单地对结果进行投票，而是训练一个*元学习器*（Meta-Learner）去学习如何组合这些基学习器的预测结果，以达到最佳性能。

=== 基本架构

Stacking 模型通常包含两层：

1.  *Level-0（基学习器层）*：包含 $T$ 个不同的算法（如 $h_1$ 是 SVM，$h_2$ 是 RF，$h_3$ 是 XGBoost）。它们利用原始训练数据进行训练。
2.  *Level-1（元学习器层）*：包含 1 个元学习器（通常是简单的线性模型，如逻辑回归或线性回归）。它的*输入*是 Level-0 学习器的*输出*（预测值），*目标*是原始的真实标签。


=== Stacking 的训练流程

假设我们有数据集 $D = {(x_1, y_1), ..., (x_m, y_m)}$，基学习器算法集合 $L_1, ..., L_T$，元学习器算法 $L_"meta"$。我们采用 $k$-折交叉验证策略。

#algorithm[Stacking 算法流程][
 *过程*：

1. 将数据集 $D$ 随机划分为 $k$ 个大小相似的互斥子集 $D_1, D_2, dots, D_k$。
2. *生成 Level-1 数据集（元特征矩阵）*：
  - For $t = 1$ to $T$ (对每个基学习器):
    - For $j = 1$ to $k$ (对每一折):
      - 使用 $D minus D_j$ (除去第 $j$ 折的数据) 训练基模型 $h_t^j$。
      - 使用 $h_t^j$ 对 $D_j$ (验证集) 进行预测，得到预测结果 $hat(y)_(D_j)^t$。
    - 将 $k$ 次预测结果拼接起来，得到该基学习器对整个训练集的预测列向量 $z_t$。
3. *构建新数据集*：
  将所有基学习器的预测向量拼接，形成新的特征矩阵 $Z = (z_1, z_2, dots, z_T)$。$Z$ 的维度是 $m times T$。
4. *训练元学习器*：
  使用新的数据集 $(Z, y)$ 训练元学习器 $H$。
5. *最后重新训练基模型*：
  使用*全部*原始数据 $D$ 对每个算法 $L_t$ 重新训练，得到最终的基模型 $h_1, ..., h_T$。

*预测*：
对于新样本 $x$：
1. 让所有最终基模型预测：$z'_t = h_t(x)$。
2. 构建元特征向量：$z' = (z'_1, ..., z'_T)$。
3. 元模型输出：$y_"pred" = H(z')$。
]

#figure(
 table(
  columns: 3,
  stroke: none,
  align: center,
  [原始特征 $X$], [基模型预测 (CV)], [元特征 $Z$],
  $mat(delim: "[", x_11, dots; dots.v, dots.v; x_(m 1), dots)$,
  [$arrow.r$ $h_1, h_2$ $arrow.r$],
  $mat(delim: "[", hat(y)_1^((1)), hat(y)_1^((2)); dots.v, dots.v; hat(y)_m^((1)), hat(y)_m^((2)))$
 ),
 caption: "特征变换视角：Stacking 本质上是一种特征提取方法"
)

=== 手算实例：回归任务的 Stacking

为了彻底理解，我们手动模拟一个微型 Stacking 过程。

*任务*：预测房价 ($y$)。
*数据*：4 个样本。
*基模型*：
- Model A: 简单的均值模型（为了演示，假设它只是学局部均值）。
- Model B: 简单的规则模型。
*元模型*：线性回归 $w_1 z_A + w_2 z_B$。

#figure(
 table(
  columns: 4,
  inset: 8pt,
  align: center,
  [*ID*], [*特征 $x$*], [*真实值 $y$*], [*Fold 划分*],
  [1], [10], [100], [Fold 1],
  [2], [20], [200], [Fold 1],
  [3], [30], [300], [Fold 2],
  [4], [40], [400], [Fold 2]
 ),
 caption: "原始数据与 2-Fold 划分"
)

*Step 1: 生成 Model A 的元特征列 $z_A$*

- *第一轮 CV*：
 - 训练集：Fold 2 (样本 3, 4)。$y_{3,4} = {300, 400}$。
 - Model A 逻辑：输出训练集 $y$ 的平均值。
 - Model A 参数：$(300+400)/2 = 350$。
 - *预测验证集 (Fold 1)*：样本 1 预测 350，样本 2 预测 350。

- *第二轮 CV*：
 - 训练集：Fold 1 (样本 1, 2)。$y_{1,2} = {100, 200}$。
 - Model A 参数：$(100+200)/2 = 150$。
 - *预测验证集 (Fold 2)*：样本 3 预测 150，样本 4 预测 150。

- *结果*：$z_A = [350, 350, 150, 150]^T$。
 (注意：这个模型很烂，但这就是它的“特征”。)

*Step 2: 生成 Model B 的元特征列 $z_B$*

- *第一轮 CV* (用 Fold 2 训，测 Fold 1)：
 - Model B 逻辑：$y = 10x$。
 - 预测 Fold 1：
  - 样本 1 ($x=10$) -> 100。
  - 样本 2 ($x=20$) -> 200。

- *第二轮 CV* (用 Fold 1 训，测 Fold 2)：
 - Model B 逻辑：$y = 10x$ (规则没变)。
 - 预测 Fold 2：
  - 样本 3 ($x=30$) -> 300。
  - 样本 4 ($x=40$) -> 400。

- *结果*：$z_B = [100, 200, 300, 400]^T$。

*Step 3: 构建元数据集并训练元模型*

现在我们有了新的训练矩阵 $Z$ 和原始标签 $y$：

#figure(
 table(
  columns: 4,
  inset: 8pt,
  align: center,
  [*ID*], [*$z_A$ (Model A 预测)*], [*$z_B$ (Model B 预测)*], [*真实值 $y$*],
  [1], [350], [100], [100],
  [2], [350], [200], [200],
  [3], [150], [300], [300],
  [4], [150], [400], [400]
 )
)

我们需要训练元模型（线性回归）：$y = w_A dot z_A + w_B dot z_B$。

观察数据可以发现：
- Model B 的预测 $z_B$ 和真实值 $y$ 完全一致。
- Model A 的预测与 $y$ 负相关或无关。

元学习器会很容易学到：$w_A approx 0, w_B approx 1$。
这意味着 Stacked Model 自动学会了“Model B 是个好模型，信它的；Model A 是垃圾，忽略它”。

#remark[
 在实际复杂情况中，Model A 可能在某些样本上准，Model B 在另一些上准。元模型就是学习这种“在什么情况下该信谁”的权重分配机制（甚至是非线性组合）。
]

*Step 4: 最终测试*

假设来了一个测试样本 $x_{"test"} = 50$。
1. *全量重训*：用所有数据重新训练 Model A (均值=250) 和 Model B ($y=10x$)。
2. *基模型预测*：
 - Model A 输出：$250$。
 - Model B 输出：$500$。
3. *元模型预测*：
 $ H(x) = 0 dot 250 + 1 dot 500 = 500 $

=== 总结：Stacking 与其他方法的对比

#table(
 columns: (auto, auto, auto, auto),
 inset: 10pt,
 align: horizon,
 [*特性*], [*Bagging (RF)*], [*Boosting (GBDT)*], [*Stacking*],
 [基模型类型], [同质 (通常是树)], [同质 (通常是树)], [*异质* (SVM+RF+NN...)],
 [模型依赖关系], [独立并行], [串行依赖], [分层依赖],
 [偏差-方差], [主要降低方差], [主要降低偏差], [综合提升 (主要是偏差)],
 [核心逻辑], [少数服从多数], [知错能改], [*取长补短*]
)