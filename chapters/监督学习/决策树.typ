#import "@local/ysz_tools:0.1.0": *
#show: conf
#import "@preview/cuti:0.4.0":show-cn-fakebold
#show:show-cn-fakebold
#import "@preview/marginalia:0.3.1" as marginalia: note, notefigure, wideblock
#import "@preview/mitex:0.2.6": *
#show: marginalia.setup.with(
  inner: (far: 1mm, width: 1mm, sep: 1mm), //we dont use inner btw
  outer: (far: 5mm, width: 55mm, sep: 8mm),
  top: 2.5cm,
  bottom: 2.5cm,
  book: true,
  clearance: 20pt,
)
#show: marginalia.show-frame
#let a-note-counter = counter("a-note")
#let note = note.with(counter: a-note-counter, numbering: (..i) => text(
  weight: 500,
  font: "JetBrains Mono",
  size: 7pt,
  style: "normal",
  fill: rgb("#ff3a3a"),
  numbering("[a]", ..i),
))
#set page(header: context if here().page() > 0 {
  marginalia.header(
    text-style: (size: 11.5pt, number-type: "old-style"),
    [],
    [#smallcaps[Machine Learning] #text(fill: luma(60%))[_CHAPTER 4_]],
    [Page #counter(page).display("1 of 1", both: true)],
  )
})
#let remark = note
#let appendix(body) = {
  // 1. 重置标题计数器
  counter(heading).update(0)
  
  // 2. 针对附录内部的所有标题设置新格式
  set heading(numbering: "A.1")
  
  // 3. (可选) 如果你希望一级标题显示为 "附录 A. xxx"
  show heading.where(level: 1): it => {
    let nos = counter(heading).at(it.location())
    let letter = numbering("A", ..nos)
    block(sticky: true,[Appendix #letter #it.body])
  }

  body
}

= 决策树
本章主要讨论用于分类的决策树。其主要优点是模型具有可读性，分类速度快。决策树的学习通常包括3个步骤：*特征选择、决策树的生成和决策树的修剪。*

== 决策树模型与学习
分类决策树模型是一种描述对实例进行分类的属性结构。决策树由结点和有向边组成。结点由两种类型：内部结点和叶结点。*内部结点表示一个特征或属性，叶结点表示一个类。*

假设$X$为表示特征的随机变量，$Y$为表示类的随机变量，那么这个条件概率分布可以表示为$P(Y|X)$。各叶结点（单元）上的条件概率往往属于某一类的概率较大，决策树分类时将该结点的实例强行分到条件概率大的那一类去。

假设给定训练数据集
$
  D={(x_1,y_1),(x_2,y_2),dots,(x_N,y_N)}
$
其中，$x_i=(x_i^(\(1\)),x_i^(\(2\)),dots,x_i^(\(n\)))$为输入实例（特征向量），$n$为特征个数，$y_i in {1,2,dots,K}$为类标记，$i=1,2,dots,N$, $N$为样本容量。#note[
决策树学习本质上是*从训练数据集中归纳出一组分类规则*。能对训练数据正确分类的决策树可能有多个，也可能一个都没有。我们需要的是一个*与训练数据矛盾较小的决策树*，同时具有很好的泛化能力。从另一个角度看，*决策树学习是由训练数据集估计条件概率模型*。我们选择的条件概率模型应该不仅对训练数据有很好的拟合，而且对未知数据有很好的预测。
]


决策树学习的损失函数通常是正则化的极大似然函数。因为*从所有可能的决策树中选取最优决策树是NP完全问题*，所以现实中决策树学习算法通常采用*启发式方法，近似求解这一最优化问题*。#note[Trivial.]这样得到的决策树是次最优的。

决策树学习算法包括特征选择、决策树生成与决策树的剪枝过程。由于决策树表示一个条件概率分布，所以深浅不同的决策树对应不同复杂度的概率模型。决策树的生成对应于模型的局部选择，决策树的剪枝对应于模型的全局选择。决策树的生成只考虑局部最优，而决策树的剪枝则考虑全局最优。#remark[
决策树学习常见的算法有ID3、C4.5与CART。
]


== 特征选择
特征选择在于选取对训练数据具有分类能力的特征，这样可以提高决策树学习的效率。*如果利用一个特征进行分类的结果与随机分类的结果没有很大差别，则称这个特征是没有分类能力的*。经验上扔掉这样的特征对决策树学习的精度影响不大。通常特征选择的准则是*信息增益或信息增益比*。

#definition("随机变量的熵")[
在信息论与概率统计中，熵是表示随机变量不确定性的度量。设$X$是一个取有限个值的离散随机变量，其概率分布为
$
  P(X=x_i)=p_i , #h(2em)i=1,2,dots,n
$
则随机变量的熵定义为
$
  H(X)=-sum_(i=1)^n p_i log(p_i)
$
通常对数会以2或者$e$为底。熵只依赖于$X$的分布，而与$X$的取值无关，所以也可将$X$的熵记作$H(p)$，即：
$
  H(p)=-sum_(i=1)^n p_i log(p_i)
$
]

#theorem[
$
  0lt.eq.slant H(p)lt.eq.slant log n
$#note[
  Jensen不等式可以直接解决。考虑函数 $f(x) = x log x$，它在 $x > 0$ 上是凸函数。

那么：

#mitex(`
\sum_{i=1}^n \frac{1}{n} f(p_i) \ge f\left( \sum_{i=1}^n \frac{1}{n} p_i \right)\\

 = f\left( \frac{1}{n} \sum p_i \right) = f\left( \frac{1}{n} \right)
`)

即：

#mitex(`
\frac{1}{n} \sum p_i \log p_i \ge \frac{1}{n} \log \frac{1}{n}\\
\Rightarrow \sum p_i \log p_i \ge \log \frac{1}{n} = -\log n\\
\Rightarrow -\sum p_i \log p_i \le \log n
`)
]
]

#proof[
构造出以下优化问题：
$
  max_(p_i)-sum_(i=1)^n p_i log p_i #h(2em) s.t. sum_(i=1)^n p_i=1
$
引入拉格朗日乘子$lambda$，构造拉格朗日函数为
$
  L(p_i,lambda)=-sum_(i=1)^n p_i log p_i + lambda (sum_(i=1)^n p_i - 1)
$
求解：
$
  cases(
    frac(partial L(p_i,lambda), partial p_i)=0,
    ,
    sum_(i=1)^n p_i=1
  )
$
最终可知
$
  p_i=e^(lambda-1)=1/n
$
时，原式取到最大值$log n$.
]

#definition("条件熵")[
设有随机变量$(X,Y)$，其联合概率分布为
$
  P(X=x_i,Y=y_i)=p_(i j),#h(2em)i=1,2,dots,n#h(2em)j=1,2,dots,m
$
条件熵$H(Y|X)$表示在*已知随机变量$X$的条件下随机变量$Y$的不确定性*，定义为(条件概率分布的熵对$X$的数学期望)：
$
  H(Y|X)=sum_(i=1)^n p_i H(Y|X=x_i)
$
这里，$p_i=P(X=x_i),i=1,2,dots,n$.

当熵和条件熵中的概率由数据估计（特别是极大似然估计）得到时，所对应的熵与条件熵分别成为经验熵和经验条件熵。
]


信息增益表示得知特征$X$的信息而使得类$Y$的信息的不确定性减少的程度。

#definition("信息增益")[
特征$A$对训练数据集$D$的信息增益$g(D,A)$定义为：
$
  g(D,A)=H(D)-H(D|A)
$
]

#note[
*决策树学习中的信息增益等价于训练数据集中类与特征的互信息*。
]

决策树学习应用信息增益准则选择特征。给定训练数据集$D$和特征$A$，经验熵$H(D)$表示对数据集$D$进行分类的不确定性。而经验条件熵$H(D|A)$表示在特征$A$给定条件下，对数据集$D$进行分类的不确定性。它们的差即信息增益，就表示由于特征$A$而使得对数据集$D$的分类不确定性减少的程度。信息增益大的特征具有更强的分类能力。

设训练数据集为$D$，$|D|$表示样本个数。设有$K$个类$C_k,k=1,2,dots,K$,$|C_k|$为属于类$C_k$的样本个数，$sum_(k=1)^K|C_k|=|D|$。

设特征$A$有$n$个不同的取值${a_1,a_2,dots,a_n}$，根据特征$A$的取值将$D$划分为$n$个子集$D_1,D_2,dots,D_n$，$|D_i|$为$D_i$的样本个数，$sum_(i=1)^n|D_i|=|D|$。记自己$D_i$中属于类$C_k$的样本的集合为$D_("ik")$，即$D_("ik")=D_i inter C_k$, $|D_("ik")|$为$D_("ik")$的样本个数。

（说人话就是按类别分和按特征分，两个维度。）所以有信息增益算法如下：

#algorithm("计算信息增益")[
*输入：*训练数据集$D$和特征$A$.

*输出：*特征$A$对训练数据集$D$的信息增益$g(D,A)$

1. 计算数据集$D$的经验熵$H(D)$
$
  H(D)=-sum_(k=1)^K frac(|C_k|,|D|)log_2frac(|C_k|,|D|)
$
2. 计算特征$A$对数据集$D$的经验条件熵$H(D|A)$
$
  H(D|A)=sum_(i=1)^n frac(|D_i|,|D|)H(D_i)=-sum_(i=1)^n frac(|D_i|,|D|)sum_(k=1)^K frac(|D_("ik")|,|D_i|)log_2frac(|D_("ik")|,|D_i|)
$
3. 计算信息增益
$
  g(D,A)=H(D)-H(D|A)
$
]

以信息增益作为划分训练数据集的特征，存在偏向于选择取值较多的特征的问题，使用信息增益比可以对这个问题进行校正，也是特征选择的一个准则。

#definition("信息增益率")[
特征$A$对训练数据集$D$的信息增益比$g_R (D,A)$定义为其信息增益$g(D,A)$与训练数据集$D$关于特征$A$的值的熵$H_A (D)$之比，即
$
  g_R(D,A)=frac(g(D,A),H_A (D))
$
其中，$H_A (D)=-sum_(i=1)^n frac(|D_i|,|D|)log_2frac(|D_i|,|D|)$，$n$是特征$A$取值的个数。

$H_A (D)$是特征取值的分布熵。如果特征本身出现频率很均匀，$H_A (D)$就会很大。
]


== 决策树的生成


ID3算法的核心是选择信息增益最大的特征作为结点的特征不断的构造新节点.#note[选了信息增益最大的,剩下的熵就最小了.
*其实也相当于用极大似然法进行概率模型的选择*。]

#algorithm("ID3算法生成决策树")[
*输入：*训练数据集$D$，特征集$A$阈值$epsilon$.

*输出：*决策树$T$。

+ 若$D$中所有实例属于同一类$C_k$，则$T$为单结点树，并将类$C_k$作为该结点的类标记，返回$T$。
+ 若$A=nothing$，则$T$为单结点树，并将$D$中实例最大的类$C_k$作为该结点的类标记，返回$T$。
+ 否则，按照前述方法计算$A$中各特征对$D$的信息增益，选择信息增益最大的特征$A_g$；
+ 如果$A_g$的信息增益小于阈值$epsilon$，则置$T$为单结点树，并将$D$中实例数量最大的类$C_k$作为该结点的类标记，返回$T$(分不清楚就用频率最多的类糊弄)；
+ 否则，对$A_g$的每一可能值$a_i$, 依$A_g=a_i$将$D$分割成若干非空子集$D_i$，将$D_i$中实例数最大的类作为标记，构建子结点，由结点及其子结点构成树$T$，返回$T$；
+ 对第$i$个子结点，以$D_i$为训练集，以$A-{A_g}$为特征集，递归地调用步骤1-5，得到子树$T_i$，返回$T_i$；
#note[
*ID3算法只有树的生成，所以该算法生成的树容易产生过拟合。*C4.5算法与ID3算法相似，C4.5算法对ID3算法进行了改进，生成过程中选择信息增益比来选择特征。
]]
== C4.5算法
改进:$
G'(D,a) eq.delta g(D,a)/(I V(a)),I V(a) eq.delta - sum_(v=1)^V (abs(D^v)/(abs(D)) log_2 (abs(D^v))/(abs(D))
$
可以避免ID3算法偏爱那些待选数量多的特征(比如100个学号).

== 决策树的剪枝
#definition("Pruning")[
在决策树学习中将已生成的树进行简化的过程称为剪枝(pruning)。具体地，剪枝从已生成的树上裁掉一些子树或叶结点，并将其根结点或父结点作为新的叶结点，从而简化分类树模型。
]


决策树的剪枝往往通过极小化决策树整体的损失函数来实现。设树$T$的叶结点个数为$|T|$，$t$是树$T$的叶结点，该叶结点有$N_t$个样本点，$N_t k$是在这个叶子节点中，属于第 $k$ 类的样本有多少个。$k=1,2,dots,K$，$H_t (T)$为叶结点$t$上的经验熵，$alpha gt.eq.slant 0$为参数，则决策树学习的损失函数可以定义为：
$
  C_alpha (T)=sum_(t=1)^(|T|) N_t H_t (T)+alpha |T|
$
其中经验熵为
$
  H_t (T)=-sum_k frac(N_"tk",N_t)log frac(N_"tk",N_t)
$
在损失函数中，第一项：
$
  C(T)=sum_(t=1)^(|T|) N_t H_t (T)=-sum_(t=1)^(|T|)sum_(k=1)^K N_"tk"log frac(N_"tk",N_t)
$
$C(T)$表示模型对训练数据的预测误差，即模型对训练数据的拟合程度，$|T|$表示模型复杂度，参数$alpha gt.eq.slant 0$控制两者之间的影响。较大的$alpha$促使选择较简单的模型（树）。$alpha=0$则表示只考虑模型与训练数据的拟合程度，不考虑模型的复杂度。剪枝就是当$alpha$确定时，选择损失函数最小的模型。当$alpha$值确定时，子树越大，往往与训练数据的拟合越好，但模型复杂度就越高。利用损失函数的极小化等价于正则化的极大似然估计。所以利用损失函数最小化原则进行剪枝就是用正则化的极大似然估计进行模型选择。

设一组叶结点回缩到其父结点之前与之后的整体树分别是$T_B$与$T_A$，其对应的损失函数值分别是$C_alpha (T_B)$与$C_alpha (T_A)$，如果
$
  C_alpha (T_A) lt.eq.slant C_alpha (T_B)
$
则进行剪枝，将父结点变为新的叶结点。

#remark[
决策树算法只考虑两个数的损失函数的差，所以计算是可以在局部进行的，因而可以由一种动态规划算法实现。
]

#example[
  ID3 算法手算实例：贷款申请分类

我们使用一个简化的数据集，包含 5 个样本。目标是根据特征判断是否批准贷款（类别 $Y$）。

*1. 数据集准备*

#table(
  columns: (auto, auto, auto, auto, auto),
  inset: 8pt,
  align: center,
  table.header(
    [*ID*], [*年龄* ($A_1$)], [*有工作* ($A_2$)], [*有房子* ($A_3$)], [*类别: 批准?* ($Y$)]
  ),
  [1], [青年], [否], [否], [否],
  [2], [青年], [是], [否], [是],
  [3], [老年], [否], [是], [是],
  [4], [老年], [是], [是], [是],
  [5], [老年], [否], [否], [否],
)
]

*2. 计算根结点的经验熵 $H(D)$*

数据集 $D$ 中共有 5 个样本：
- 类别“是”（$Y=1$）：3 个样本
- 类别“否”（$Y=0$）：2 个样本

$
  H(D) = - sum_(k=1)^K p_k log_2 p_k = - (3/5 log_2 3/5 + 2/5 log_2 2/5) approx 0.971
$

*3. 计算各特征的信息增益*

我们要分别计算 $A_1$（年龄）、$A_2$（有工作）、$A_3$（有房子）的信息增益。

*(1) 特征 $A_1$：年龄*
- 取值“青年” ($D_1$): 2 个样本 (1否, 1是)。$H(D_1) = - (1/2 log_2 1/2 + 1/2 log_2 1/2) = 1.0$
- 取值“老年” ($D_2$): 3 个样本 (1否, 2是)。$H(D_2) = - (1/3 log_2 1/3 + 2/3 log_2 2/3) approx 0.918$

经验条件熵：
$
  H(D|A_1) = 2/5 H(D_1) + 3/5 H(D_2) = 0.4 times 1.0 + 0.6 times 0.918 = 0.951
$
信息增益：
$
  g(D, A_1) = H(D) - H(D|A_1) = 0.971 - 0.951 = 0.020
$

*(2) 特征 $A_2$：有工作*
- 取值“是” ($D_1$): 2 个样本 (2是)。纯度最高，$H(D_1) = 0$
- 取值“否” ($D_2$): 3 个样本 (2否, 1是)。$H(D_2) approx 0.918$

经验条件熵：
$
  H(D|A_2) = 2/5 times 0 + 3/5 times 0.918 = 0.551
$
信息增益：
$
  g(D, A_2) = H(D) - H(D|A_2) = 0.971 - 0.551 = 0.420
$

*(3) 特征 $A_3$：有房子*
- 取值“是” ($D_1$): 2 个样本 (2是)。$H(D_1) = 0$
- 取值“否” ($D_2$): 3 个样本 (2否, 1是)。$H(D_2) approx 0.918$

信息增益（计算过程同上）：
$
  g(D, A_3) = 0.971 - (0.4 times 0 + 0.6 times 0.918) = 0.420
$

*4. 构建决策树（第一层划分）*

比较信息增益：
$ g(D, A_2) = g(D, A_3) > g(D, A_1) $
我们发现“有工作”和“有房子”的信息增益相同且最大。ID3 算法可以任选一个。这里我们选择特征 *$A_3$ (有房子)* 作为根结点。

- *右分支（有房子=是）*：
  对应样本 {3, 4}，类别全为“是”。这是一个叶结点，标记为 *“是”*。

- *左分支（有房子=否）*：
  对应样本 $D'$ = {1, 2, 5}。
  - 样本 1: 青年, 没工作 -> 否
  - 样本 2: 青年, 有工作 -> 是
  - 样本 5: 老年, 没工作 -> 否
  
  此时 $D'$ 中还有混合类别，需要递归。
  计算 $D'$ 中剩余特征 ($A_1$ 年龄, $A_2$ 工作) 的增益：
  显然可以看出，在 $D'$ 中，只要“有工作”就是“是”，没工作就是“否”。
  $A_2$ (工作) 将把数据完美分开，增益最大。

#note[
  在这个例子中，因为数据量较小，第二步递归时特征“有工作”直接得到了纯度为 0 的子集，因此算法终止，生成了深度为 2 的树。
]

== CART算法
#remark[
ID3和C4.5算法，生成的决策树是多叉树，只能处理分类不能处理回归。而CART（classification and regression tree）分类回归树算法，既可用于分类也可用于回归。 分类树的输出是样本的类别， 回归树的输出是一个实数。
]

#definition[
CART是在给定输入随机变量$X$条件下输出随机变量$Y$的条件概率分布的学习方法。CART假设决策树是*二叉树*，内部结点特征的取值为“是”和“否”，左分支是取值为“是”的分支，右分支是取值为“否”的分支。这样的决策树等价于递归地二分每个特征，将输入空间即特征空间划分为有限个单元，并在这些单元上确定预测的概率分布，也就是在输入给定的条件下输出的条件的概率分布。

+ 决策树生成：基于训练数据集生成决策树，生成的决策树要尽量大。
+ 决策树剪枝：用验证数据集对已生成的树进行剪枝并选择最优子树，这时用损失函数最小作为剪枝的标准。
]


== CART生成
决策树的生成就是递归地构建二叉决策树的过程，对回归树用平方误差最小化准则，对分类树用基尼指数(Gini index)最小化准则，进行特征选择，生成二叉树。

*1. 回归树生成*

每次划分时逐一考察当前集合中所有特征的所有取值，根据平方误差最小化准则选择最优划分点。

#definition[
假设$X$与$Y$分别为输入和输出变量，并且$Y$是连续变量，给定数据集$D$，考虑如何生成回归树。一棵回归树对应着输入空间（即特征空间）的一个划分以及在划分的单元上的输出值。假设已将输入空间划分为$M$个单元$R_1,R_2,dots,R_M$，并且在每个单元$R_m$上有一个固定的输出值$c_m$，于是回归树模型可表示为：
$
  f(x)=sum_(m=1)^M c_m I(x in R_m)
$
]

当输入空间的划分确定时，可以用平方误差$sum_(x_i in R_m)(y_i - f(x_i))^2$来表示回归树对训练数据的预测误差，用平方误差最小的准则求解每个单元上的最优输出值。

#note[
易知(可以通过求导方法证明)，单元$R_m$上，$c_m$的最优值$hat(c)_m$是$R_m$上的所有输入实例$x_i$对应的输出$y_i$的均值，即：
$
  hat(c)_m="ave"(y_i|x_i in R_m)
$
]

问题是怎样对输入空间进行划分。采用启发式的方法，选择第$j$个变量$x^((j))$和它的取值$s$作为切分变量(splitting variable)和切分点(splitting point)，并定义两个区域：
$
  R_1(j,s)={x|x^((j))lt.eq.slant s},#h(2em)R_2(j,s)={x|x^((j))> s}
$
然后寻找最优切分变量$j$和最优切分点$s$。具体地，求解
$
  min_(j,s)[min_(c_1)sum_(x in R_1 (j,s)) (y_i-c_1)^2+min_(c_2) sum_(x_i in R_2 (j,s)) (y_i-c_2)^2]
$
对固定的输入变量$j$可以找到最优切分点$s$。
$
  hat(c)_1 = "ave"(y_1|x_i in R_1(j,s))#h(2em) hat(c)_2="ave"(y_i|x_i in R_2(j,s))
$
遍历所有输入变量，找到最优切分变量$j$，构成一个对$(j,s)$，依次将输入空间划分为两个区域。接着对每个区域重复上述划分过程，直到满足停止条件为止。这样就生成一颗回归树。这样的回归树通常称为*最小二乘树*。

*2. 分类树生成*

#definition[
分类树用基尼指数选择最优特征，同时决定该特征的最优二值切分点。

分类问题中，假设有$K$个类，样本点属于第$k$类的概率为$p_k$，则概率分布的基尼指数定义为
$
  "Gini"(p)=sum_(k=1)^K p_k (1-p_k)=1-sum_(k=1)^K p_k^2
$
]

#example[
对于二类分类问题，若样本点属于一个类的概率为$p$，那么概率分布的基尼指数为
$
  "Gini"(p)=2p(1-p)
$
]

对于给定的样本集合$D$，其基尼指数为
$
  "Gini"(D)=1-sum_(k=1)^K (frac(|C_k|,|D|))^2
$
这里，$C_k$是$D$中属于第$k$类的样本子集，$K$是类的个数。

如果样本集合$D$根据特征$A$是否取某一可能值$a$被分割成$D_1$和$D_2$两部分，即
$
  D_1={(x,y) in D| A(x) = a},#h(2em)D_2=D-D_1
$
则在特征$A$的条件下，集合$D$的基尼指数定义为
$
  "Gini"(D,A)=frac(|D_1|,|D|)"Gini"(D_1)+frac(|D_2|,|D|)"Gini"(D_2)
$
#note[基尼指数$"Gini"(D)$表示集合$D$的不确定性，基尼指数$"Gini"(D,A)$表示经$A=a$分割后集合$D$的不确定性。]

#remark[
CART生成算法停止计算的条件是*结点中样本个数小于预定阈值或样本集的基尼指数小于预定阈值（样本基本属于同一类）*，或者没有更多特征。
]

== CART剪枝
CART剪枝算法的目标是剪掉那些算了很多步骤但是对Gini的收益不是太大的分支.

由两步组成：首先从生成算法产生的决策树$T_0$底端开始不断剪枝，直到$T_0$的根节点，形成一个子树序列${T_0,T_1,dots,T_n}$；然后通过交叉验证法在独立的验证数据集上对子树序列进行测试，从中选择最优子树。#note[
  第一步是贪心地生成一系列候选树，第二步用交叉验证从候选树里挑最好的.
]

在剪枝过程中，计算子树的损失函数：
$
  C_alpha (T)=C(T)+alpha|T|
$
其中，$T$为任意子树，$C(T)$为对训练数据的预测误差（如基尼指数），$|T|$为子树的叶结点个数，$alpha gt.eq.slant 0$为参数，$C_alpha (T)$为参数是$alpha$时子树$T$的整体损失。参数$alpha$权衡训练数据的拟合程度与模型的复杂度。对固定的$alpha$，一定存在使损失函数$C_alpha (T)$最小的子树，将其表示为$T_alpha$。$T_alpha$在损失函数$C_alpha (T)$最小的意义下是最优的。

#theorem[
CART剪枝算法中，当$alpha$确定时，存在唯一的最小子树$T_alpha$使损失函数$C_alpha (T)$最小。
]

#proof[
因为$C_alpha (T)=C(T)+alpha|T|$，而$C(T)$岁节点数$|T|$增加而递减，$alpha|T|$随$|T|$增加而递增，因此$C_alpha (T)$是关于$|T|$的连续分段线性函数。在所有可能的子树中，存在一个最优子树$T_alpha$使得$C_alpha (T_alpha)$最小；*由于损失函数的单调性和连续性，最优子树是唯一的。*
]

Breiman等人证明：可以用递归的方法对树进行剪枝。将$alpha$从小增大，$0=alpha_0<alpha_1<dots<alpha_n<+infinity$, 产生一系列的区间$[alpha_i,alpha_(i+1)),i=0,1,dots,n$；剪枝得到的子树序列对应着区间的最优子树序列${T_0,T_1,dots,T_n}$，序列中的子树是嵌套的。

具体地，从整体树$T_0$开始剪枝，对$T_0$的任意内部结点$t$，以$t$为单结点的损失函数是
$
  C_alpha (t)=C(t) + alpha
$
以$t$为根结点的子树$T_t$的损失函数是
$
  C_alpha (T_t)=C(T_t)+alpha|T_t|
$
当$alpha=0$及$alpha$充分小的时候，有：
$
  C_alpha (T_t)<C_alpha (t)
$
当$alpha$增大时，在某一$alpha$有
$
  C_alpha (T_t)=C_alpha (t)
$
只要$alpha=frac(C(t)-C(T_t),|T_t|-1)$，$T_t$与$t$有相同的损失函数值，而$t$的结点少，要对$T_t$进行剪枝。为此，对$T_0$中每一内部结点$t$，计算
$
  g(t)=frac(C(t)-C(T_t),|T_t|-1)
$
他表示剪枝后整体损失函数减少的程度。在$T_0$中剪去$g(t)$最小的$T_t$，将得到的子树作为$T_1$，同时将最小的$g(t)$设为$alpha_1$。$T_1$为区间$[alpha_1,alpha_2)$的最优子树。
#theorem[
  CART剪枝算法中，求出的子树序列${T_0,T_1,dots,T_n}$分别是区间$alpha in [alpha_i,alpha_(i+1))$的最优子树$T_alpha$，这里$i=0,1,dots,n,0=alpha_0<alpha_1<dots<alpha_n<+infinity$
]

#proof[
已知$T_0$是完整的决策树，$T_i$是剪枝掉$T_(i+1)$的一个分支得到的子树。因为$T_0$在$alpha=0$时最优，假设$T_k$在$[alpha_k,alpha_(k+1))$内最优，由于剪枝操作连续，那么$T_(k+1)$在$[alpha_(k+1),alpha_(k+2))$内最优。
]

这种方法的优点是可以自动生成一系列复杂度不同的树，便于通过交叉验证选择最佳$alpha$和对应子树。
具体地，利用独立的验证数据集，测试子树序列$T_0,T_1,dots,T_n$中各棵子树的平方误差或基尼指数。平方误差或基尼指数最小的决策树被认为是最优的决策树。在子树序列中，每棵子树$T_1,T_2,dots,T_n$都对应一个参数$alpha_1,alpha_2,dots,alpha_n$。所以，当最优子树$T_k$确定时，对应的$alpha_k$也确定了，即得到最优决策树$T_alpha$.

#algorithm[CART剪枝算法][
*输入：*CART算法生成的决策树$T_0$

*输出：*最优决策树$T_alpha$
+ 设$k=0,T=T_0$
+ $alpha=+infinity$
+ 自下而上地计算各内部结点$t$的$C(T_t),|T_t|,g(t),alpha=min(alpha,g(t))$.
+ 对$g(t)=alpha$的内部结点$t$进行剪枝，并对叶结点$t$以多数表决法决定其类，得到树$T$。
+ 设$k=k+1,alpha_k = alpha, T_k = T$
+ 如果$T_k$不是由根结点及两个叶结点构成的树，回到步骤2; 否则令$T_k=T_n$
+ 采用交叉验证法在子树序列$T_0,T_1,dots,T_n$中选取最优子树$T_alpha$
]

#example[

#figure(
  table(
    columns: (auto, auto, auto, auto, auto),
    inset: 5pt,
    align: center,
    table.header(
      [*ID*], [*有房* ($A_1$)], [*婚姻* ($A_2$)], [*年薪 (万)* ($A_3$)], [*类别* ($Y$)]
    ),
    [1], [是], [单身], [12.5], [0 (正常)],
    [2], [否], [已婚], [10.0], [0 (正常)],
    [3], [否], [单身], [7.0], [1 (拖欠)],
    [4], [是], [已婚], [12.0], [0 (正常)],
    [5], [否], [离婚], [9.5], [1 (拖欠)],
  ),
  caption: [贷款申请数据集]
)
]

*数据摘要*：
- 总样本 $|D|=5$。
- 类别分布：$Y=0$ (3个), $Y=1$ (2个)。
- 根节点的基尼指数：
  $ "Gini"(D) = 1 - ((3/5)^2 + (2/5)^2) = 1 - (0.36 + 0.16) = 0.48 $


*(1) 特征 $A_1$：有房*
- *切分点*：{是} vs {否}
  - $D_1$ (有房): ID {1, 4} (全0)。$"Gini" = 0$。
  - $D_2$ (没房): ID {2, 3, 5} (0, 1, 1)。$"Gini" = 1 - ((1/3)^2 + (2/3)^2) approx 0.444$。
- *加权 Gini*：
  $ "Gini"(D, A_1) = 2/5(0) + 3/5(0.444) approx 0.267 $

*(2) 特征 $A_2$：婚姻 (多值离散)*
CART 需要将其转为二值逻辑。
- *切分 A*：{已婚} vs {单身, 离婚}
  - $D_1$ (已婚): ID {2, 4} (全0)。$"Gini" = 0$。
  - $D_2$ (其他): ID {1, 3, 5} (0, 1, 1)。$"Gini" = 0.444$。
  - *结果*：$2/5(0) + 3/5(0.444) approx 0.267$。
- *切分 B*：{单身} vs {已婚, 离婚}
  - $D_1$ (单身): ID {1, 3} (0, 1)。$"Gini" = 0.5$。
  - $D_2$ (其他): ID {2, 4, 5} (0, 0, 1)。$"Gini" = 0.444$。
  - *结果*：$2/5(0.5) + 3/5(0.444) = 0.2 + 0.267 = 0.467$。

*(3) 特征 $A_3$：年薪 (连续值)*
先排序：$7.0, 9.5, 10.0, 12.0, 12.5$。
取相邻中点作为候选切分点：$8.25, 9.75, 11.0, 12.25$。
我们测试切分点 *9.75*：
- $D_1$ (<= 9.75): ID {3, 5} (全1, 拖欠)。$"Gini" = 0$。
- $D_2$ (> 9.75): ID {1, 2, 4} (全0, 正常)。$"Gini" = 0$。
- *加权 Gini*：
  $ "Gini"(D, A_3=9.75) = 0 $ (完美划分)

比较发现，特征 *$A_3$ (年薪 <= 9.75)* 的基尼指数最小 (0.0)。因此将其作为根节点。由于划分后的子集纯度均为 0，树的生长停止。每一层节点都是一场独立的比赛，胜者通吃（成为切分点），败者只能等待下一轮（子节点）的机会。如果比赛在这一轮就彻底结束了（完美分类），败者就没有上场机会了。