#import "../../notes.typ":*
#show: notes

== 贝叶斯法
#definition[朴素贝叶斯法][
朴素贝叶斯法(naive Bayes)法是基于*贝叶斯定理与特征条件独立假设*的分类方法。对于给定的训练数据集，首先基于特征条件独立假设，学习输入输出的联合概率分布；然后基于此模型，对给定的输入$x$，利用贝叶斯定理求出后验概率最大的输出$y$。朴素贝叶斯法实现简单，学习与预测效率较高，是一种常用的方法。
]


=== 朴素贝叶斯法的学习与分类

设输入空间$cal(X)subset.eq R^n$为$n$维向量的集合，输出空间为类标记集合$cal(Y)={c_1,c_2,dots,c_K}$。输入为特征向量$x in cal(X)$，输出为类标记$y in cal(Y)$。$X$是定义在输入空间$cal(X)$上的随机向量，$Y$是定义在输出空间$cal(Y)$上的随机变量，$P(X,Y)$是$X$和$Y$的联合概率分布。训练数据集$T$由$P(X,Y)$独立同分布产生。

朴素贝叶斯法通过训练数据集学习联合概率分布$P(X,Y)$。具体地，先验概率分布
$
  P(Y=c_k),#h(3em)k=1,2,dots,K
$
条件概率分布
$
  P(X=x|Y=c_k)=P(X^(\(1\))=x^(\(1\)),dots,X^(\(n\))=x^(\(n\))|Y=c_k),#h(1em)k=1,2,dots,K
$
于是学习到了联合概率分布。而条件概率分布$P(X=x|Y=c_k)$有指数级数量的参数，其估计实际上是不可行的。

#note[
朴素贝叶斯法对条件概率分布作了*条件独立性的假设*。由于这是一个较强的假设，朴素贝叶斯法由此得名。参数数量从$S^d-1$降低到了$d(S-1)$，实现了从指数级到线性级的降维。
]

#remark[
  如果不做任何假设，我们需要计算的是所有特征联合起来的概率：
$ P(X^((1))=x^((1)), X^((2))=x^((2)), dots, X^((n))=x^((n)) | Y=c_k) $

这就相当于要填一张巨大的多维表格。
- 特征 1 有 $S$ 种取值。
- 特征 2 有 $S$ 种取值。
- ...
- 特征 $n$ 有 $S$ 种取值。

输入特征向量 $x$ 的**所有可能组合**总数为：
$ S times S times dots times S = S^n $

我们需要为每一种组合都分配一个概率值。由于所有概率之和必须为 1，只要知道了前 $S^n - 1$ 个概率，最后一个概率自然就确定了（$1 - sum$）。

*结论*：参数个数为 $S^n - 1$。
]

#remark[
  朴素贝叶斯假设特征之间相互独立：
$ P(X|Y=c_k) = P(X^((1))|Y=c_k) times P(X^((2))|Y=c_k) times dots times P(X^((n))|Y=c_k) $

现在，我们不需要把特征合在一起看，而是**单独**为每个特征建立概率表。

- *对于第 1 个特征* $X^((1))$：
  它有 $S$ 种取值。我们需要描述它在 $Y=c_k$ 下的分布，需要 $S-1$ 个参数。
- *对于第 2 个特征* $X^((2))$：
  同样有 $S$ 种取值，需要独立的一组 $S-1$ 个参数。
- ...
- *对于第 $n$ 个特征* $X^((n))$：
  需要 $S-1$ 个参数。

我们将所有特征需要的参数加起来（而不是乘起来）：
$ underbrace((S-1), X^((1))) + underbrace((S-1), X^((2))) + dots + underbrace((S-1), X^((n))) = n(S-1) $

*结论*：参数个数为 $n(S-1)$。
]
$
  P(X=x|Y=c_k)=P(X^(\(1\))=x^(\(1\)),dots,X^(\(n\))=x^(\(n\))|Y=c_k)=product_(j=1)^n P(X^(\(j\))=x^(\(j\))|Y=c_k)
$
朴素贝叶斯法实际上是*学习到生成数据*的机制，所以属于*生成模型*。条件独立假设等于是说用于分类的特征在类确定的条件下都是条件独立的。这一假设使朴素贝叶斯法变得简单，但有时会牺牲一定的分类准确率。朴素贝叶斯法分类时，对给定的输入$x$，通过学习到的模型计算后验概率分布$P(Y=c_k|X=x)$，将后验概率最大的类作为$x$的类输出。后验概率计算根据贝叶斯定理进行：
$
  P(Y=c_k|X=x)
  &=frac(P(X=x|Y=c_k)P(Y=c_k),sum_k P(X=x|Y=c_k)P(Y=c_k)) \
  &=frac(P(Y=c_k)product_j P(X^(\(j\))=x^(\(j\))|Y=c_k),sum_k P(Y=c_k)product_j P(X^(\(j\))=x^(\(j\))|Y=c_k)) 
$
所以朴素贝叶斯分类器可表示为：
$
  y=f(x)=arg max_(c_k)P(Y=c_k|X=x)=arg max_(c_k)frac(P(Y=c_k)product_j P(X^(\(j\))=x^(\(j\))|Y=c_k),sum_k P(Y=c_k)product_j P(X^(\(j\))=x^(\(j\))|Y=c_k)) 
$
而我们注意到，分母对所有$c_k$都是相同的，所以：
$
  y=arg max_(c_k)P(Y=c_k)product_j P(X^(\(j\))=x^(\(j\))|Y=c_k)
$

#theorem[朴素贝叶斯法将实例分到后验概率最大的类中，这等价于期望风险最小化。]

#proof[

假设选择0-1损失函数，$f(X)$表示分类决策函数，那么期望风险函数为：
$
  R_("exp")(f)=E[L(Y,f(X))]
$
期望是对联合分布$P(X,Y)$取的，由此取条件期望
$
  R_("exp")(f)=E_X sum_(k=1)^K [L(Y,f(X))]P(c_k|X)
$
为了使期望风险最小化，只需对$X=x$逐个极小化，由此得到：
$
  f(x)&=arg min_(y in cal(Y))sum_(k=1)^K L(c_k,y)P(c_k|X=x) \
  =&=arg min_(y in cal(Y)) sum_(k=1)^K P(y eq.not c_k|X=x) \
  &=arg max_(y in cal(Y)) P(y eq c_k|X=x)\
$
]

这样一来，根据期望风险最小化准则就得到了后验概率最大化准则，即朴素贝叶斯法所采用的原理。

#algorithm[朴素贝叶斯法][
  *输入*：训练数据集$T={(x_1,c_1), (x_2,c_2),dots, (x_N, c_N)}$, 平滑参数$alpha$，新样本$X_"new"$.

  *输出*: 新样本的预测类别$hat(y)=c_"new"$.

  1. *估计先验概率*：计算每个类别在训练集中出现的频率.
  $
    P(Y=c_k)=("类别为"c_k"的样本数量")/"训练集总样本数量"
  $
  2. *估计条件概率*：对每个特征$x_i$（假设为离散特征，有$S_i$个可能的取值
  $
    P(x_i="value"|Y=c_k)=("在类别"c_k"中"x_i="value的数量")/("类别"c_k"的总数")
  $
  3. *拉普拉斯平滑*：为了避免训练集中未出现的情况导致概率为0(使得整个连乘为0)，我们通常使用修正后的公式, 此时计算概率$P(Y=c_k)$时也同样需要修正: 
  $
    P(x_i="value"|Y=c_k)=("计数"+alpha)/("类别总数"+alpha times S_i)
  $
  #h(1em)其中，$alpha$是一个大于0的平滑参数，当$alpha=1$时，也称为*加一平滑*。
  4. *预测最终结果*：计算新样本$X_"new"=(x_1,x_2,dots,x_n)$对于每个可能的类别的得分（即后验概率的分子），从而确定得分最高的类别$c_k$为新样本$X_"new"$的预测类别。
  $
    hat(y)=arg max_(c_k)P(Y=c_k)product_j P(X^(\(j\))=x^(\(j\))|Y=c_k)
  $

]

=== 半朴素贝叶斯分类器

半朴素贝叶斯分类器的基本思想是适当考虑一部分属性间的相互依赖信息，避免完全联合概率计算的高复杂性，同时不彻底忽略较强的属性依赖关系。最常用的策略是独依赖估计（ODE）​，即假*设每个属性在类别之外最多仅依赖于一个其他属性*。其核心公式可表示为：
$
  P(c|x) prop P(c)product_(i=1)^d P(x_i | c, p a_i)
$
其中$p a_i$是属性$x_i$所依赖的父属性。

常见的半朴素贝叶斯分类器主要有以下几种：
1. *​SPODE（超父独依赖估计）*​：假设所有属性都依赖于同一个属性，该属性称为“超父”。通过交叉验证等模型选择方法来确定超父属性 。这种方法结构简单，但超父的选择至关重要，且假设所有属性共同依赖一个超父可能仍过于简化。

2. *​TAN（树增强朴素贝叶斯）​*：通过以下步骤构建属性间的依赖关系树形结构
  - 计算任意两个属性间的*条件互信息*，以刻画它们在已知类别下的相关性。
  $
    I(X,Y|Z)=sum_(z in Z)p_Z (z)sum_(y in Y)sum_(x in X) p_ (X,Y|Z) (x,y|z)log (p_(X,Y|Z) (x,y|z))/(p_(X|Z) (x|z) p_(Y|Z) (y|z))
  $
  - 以属性为结点，条件互信息为边权构建*完全图*。
  - 构建此完全图的*最大带权生成树*。
  - 选定根节点并方向，加入类别节点指向所有属性。
  #remark[
    TAN方法保留了强相关属性间的依赖性，通常能获得比朴素贝叶斯和SPODE更好的分类性能。构造出来的这个生成树，就表达着每个结点的依赖关系。
  ]
  - 最终仍然是计算联合概率取最大值确定具体的类别，只不过是要考虑到条件概率进行计算罢了。

3. *AODE（平均独依赖估计）*:与其寻找一个最优的"超父"特征（如SPODE），不如将每个特征都作为超父来构建SPODE模型，然后集成所有这些模型的结果。
  对于一个测试实例$x=(x_1,x_2,dots,x_d)$, AODE的分类决策公式为：
  $
    hat(y)=arg max_(c in C) sum_(i=1\ |D_(x_i)| gt.eq.slant m^prime) P(c,x_i) product_(j=1)^d P(x_j|c,x_i)
  $
  - $|D_(x_i)|$是在训练集中特征$i$取值为$x_i$的样本数量
  - $m^prime$是阈值(通常设置为30)，用于确保有足够的数据支持概率估计
  - $P(c,x_i)$是类别$c$和特征$x_i$的联合概率
  - $P(x_j|c,x_i)$是在给定类别$c$和超父特征$x_i$的条件下特征$x_j$的概率。
  #remark[
    AODE 是基于 SPODE，通过集成学习得到的一个独依赖分类器，与 SPODE 的不同在于，SPODE 是选择一个模型来进行预测，而在 AODE 中，每个模型都进行一次预测（设置了门槛），然后将预测结果进行平均后得到最终的预测结果。不难看出，AODE也是一种惰性学习方法，且易于实现增量学习。
  ]
  #note[
    这里对于概率的计算，也需要考虑用拉普拉斯平滑。
  ]

#remark[
  以上方法如果进一步地拓展到kDE, 随着k增大，虽然可以一定程度上增加泛化能力，但是还是会免不了陷入指数爆炸的泥淖中。
]

#example[TAN (Tree Augmented Naive Bayes) 计算实例][
  假设我们有一个贷款违约预测问题：
- *类别 $Y$*：是否违约 ($y=1$ 违约, $y=0$ 不违约)。
- *特征 $X$*：三个二值特征 $X_1$ (收入高), $X_2$ (资产多), $X_3$ (已婚)。

假设我们基于训练数据计算出的互信息值如下（单位：比特）：

#table(
  columns: (auto, auto, auto),
  inset: 8pt,
  align: center,
  table.header([*特征对*], [*条件互信息 $I(X_i, X_j | Y)$*], [*说明*]),
  [($X_1, X_2$)], [*0.52*], [强相关 (收入与资产通常挂钩)],
  [($X_1, X_3$)], [0.12], [弱相关],
  [($X_2, X_3$)], [0.08], [弱相关]
)
]

我们需要构建一个图，节点是 $X_1, X_2, X_3$，边权是上面的互信息值。
- *完全图*：所有节点两两相连。
- *生成树*：我们要选出边权最大的边，且不构成回路。

- 选定 $X_1$ 为树的根节点。
- 边的方向：$X_1 arrow X_2$, $X_1 arrow X_3$。
- 加入类别节点 $Y$，让 $Y$ 指向所有特征。

基于上图结构，联合概率分布 $P(X_1, X_2, X_3, Y)$ 分解为：
$
  P(y, x_1, x_2, x_3) = P(y) dot underbrace(P(x_1 | y), "根节点") dot underbrace(P(x_2 | x_1, y), "依赖父节点") dot underbrace(P(x_3 | x_1, y), "依赖父节点")
$
#remark[
  对比朴素贝叶斯 (NB)：NB 会假设 $P(x_2 | x_1, y) = P(x_2 | y)$。TAN 则利用 $P(x_2 | x_1, y)$ 捕捉了 $X_1$ 和 $X_2$ 的强相关性。
]

*计算示例*：
来了一个新样本 $x_("new") = {X_1=1, X_2=0, X_3=1}$，我们要判断它是否违约 ($Y=1$?)。

我们需要比较 $P(Y=1|x)$ 和 $P(Y=0|x)$。以 $Y=1$ 为例，计算分子的 Score：
$
  "Score"_1 = P(Y=1) dot P(X_1=1 | Y=1) dot P(X_2=0 | X_1=1, Y=1) dot P(X_3=1 | X_1=1, Y=1)
$

假设从训练数据查表得到的概率表如下：
- $P(Y=1) = 0.5$
- $P(X_1=1 | Y=1) = 0.8$ (违约的人通常收入看起来不低?)
- $P(X_2=0 | X_1=1, Y=1) = 0.1$ (*关键点*：收入高但资产少且违约的概率)
- $P(X_3=1 | X_1=1, Y=1) = 0.6$

代入计算：
$
  "Score"_1 &= 0.5 times 0.8 times 0.1 times 0.6 \
            &= 0.024
$

最后对 $Y=0$ 做同样的计算得到 $"Score"_0$，然后归一化即可分类：
$
  P(Y=1 | x) = ("Score"_1) / ("Score"_1 + "Score"_0)
$
=== 贝叶斯网络

*贝叶斯网络(Bayesian network)*，又称信念网络(Belief Network)，或有向无环图模型(directed acyclic graphical model)，是一种概率图模型，于1985年由Judea Pearl首先提出。它是一种模拟人类推理过程中因果关系的不确定性处理模型，其网络拓朴结构是一个有向无环图(DAG)。

贝叶斯网络的有向无环图中的节点表示随机变量，它们可以是可观察到的变量，或隐变量、未知参数等。认为有因果关系（或非条件独立）的变量或命题则用箭头来连接。若两个节点间以一个单箭头连接在一起，表示其中一个节点是“因(parents)”，另一个是“果(children)”，两节点就会产生一个条件概率值。

#example[
  假设节点E直接影响到节点H，即E→H，则用从E指向H的箭头建立结点E到结点H的有向弧(E,H)，权值(即连接强度)用条件概率P(H|E)来表示
]
贝叶斯网络的训练主要是*结构学习*和*参数学习*两部分。其中结构学习最为关键复杂，要确定好变量之间的依赖关系。

我们需要一个标准来给每个可能的结构打分，这就是评分函数。我们认为*一个好的模型应该能像高效的压缩软件一样，用最短的编码来“描述”你的训练数据*。而编码长度包含两部分：
- *描述模型本身的代价*​：存储这个贝叶斯网络的结构 G和参数 Θ需要多少比特？网络越复杂（节点和边越多），这部分代价就越高。
- *描述数据所需的代价*​：用这个贝叶斯网络的概率分布来编码（表示）整个训练数据集 D需要多少比特？如果网络能很好地拟合数据，那么数据在该网络下的概率就会很高，根据信息论，概率高的事件所需的编码长度就短。

评分函数：
$
  s(B|D)=f(theta)|B|-"LL"(B|D)
$
- $|B|$：网络$B$的参数综述，代表模型的复杂度。
- $f(theta)$: 描述每个参数需要的平均编码位数。
- $"LL"(B|D)$: 对数似然，衡量网络$B$对数据$D$的拟合程度。值越大，拟合效果越好。

网络结构的可能数量是随变量个数*指数级*增长的，我们不可能穷举所有结构。因此，我们需要使用搜索算法​（例如：爬山法、遗传算法等）在巨大的结构空间中进行*启发式搜索*，逐步调整网络结构（如增加、删除、反转边），朝着评分降低的方向前进。

接着我们进行参数学习，对于网络中的每一个节点（变量），我们直接统计训练数据 $D$中，在该节点的父节点取某种特定配置时，该节点取不同值的条件频率。用这个频率来估计条件概率。这里只需要进行最简单的计数统计就可以了。

确定了模型之后，接下来进行推断工作。如果是进行精密计算的话，那么在计算某个概率时，可能会因为全概率公式展开后求和项指数级增长，以至于非常难以计算。因此我们采用近似推断的方法，这里介绍的是*吉布斯采样*，这是一种马尔科夫链蒙特卡洛方法。

+ *初始化*：随机生成一个与证据 $E=e$一致的样本作为起点。例如，证据是“咳嗽=是，发烧=是”，那么初始样本中这些变量的值必须固定为此，其他未知变量的值可以随机初始化。
+ *迭代采样（随机漫步）​*: 从初始样本开始，进行多轮迭代。在每一轮中：
  + 选择一个非证据变量（比如“是否感染病毒A”）作为当前要更新的目标。
  + 保持所有其他变量的取值不变（包括证据变量和除目标外的其他非证据变量）。
  + 根据这个“局部环境”（即该变量在贝叶斯网络中的马尔可夫毯——包括其父节点、子节点、子节点的其他父节点），计算出该目标变量取各种值的条件概率。
  + 像扔骰子一样，按照这个计算出的概率分布，​随机地为该目标变量赋予一个新值。
+ 重复步骤二很多次（比如$T=10000$次）。在采样过程中，我们会记录下每次采样后，查询变量 $Q$的取值情况。例如，我们想知道 $Q=q$（比如“感染病毒A=是”）的概率。
  - 经过$T$次采样后，我们统计出查询变量的取值与 $q$一致的样本共有$n_q$个。
  - 根据大数定律，我们可以近似估算出后验概率：$P(Q=q|E=e) approx n_q / T$

#note[
  + 它是一个马尔可夫链​：每一步采样只依赖于前一步的状态，符合马尔可夫性质。
  + 收敛到目标分布​：在满足一定条件（如遍历性）时，无论从哪个初始状态开始，这个马尔可夫链在经过足够长的时间后，其状态分布会收敛到一个唯一的平稳分布。
  + 对于吉布斯采样，这个平稳分布恰好就是我们想求的后验分布$P(Q|E=e)$.
]