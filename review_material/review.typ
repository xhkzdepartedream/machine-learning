#set page(
  paper: "a4",
  margin: (x: 2cm, y: 2.5cm),
  header: align(center, text(fill: gray, size: 9pt)[机器学习与模式识别课程期末复习资料]),
  // footer: [
  //   #align(center, counter(page). display("1 / 1", both: true))
  // ]
)

#set text(
  font: ("Times New Roman", "Source Han Serif SC"),
  size: 11pt,
  lang: "zh"
)

#set par(justify: true, leading: 0.8em)
#show heading: set text(fill: rgb("#1a365d"))

// --- 试卷一卷头 ---
#align(center)[
  #text(size: 18pt, weight: "bold")[2024-2025 学年机器学习与模式识别期末试卷]
  #v(5pt)
  #text(size: 11pt, fill: gray)[满绩小铺搜集整理 | 规范化 Typst 排版]
]
#v(10pt)

= 一、 基础概念简答题

请简要解释监督学习（Supervised Learning）、无监督学习（Unsupervised Learning）及强化学习（Reinforcement Learning）之间的区别,并分别举一个应用实例.

= 二、 逻辑回归模型分析

逻辑回归模型（Logistic Regression）主要用于分类还是回归？是线性模型还是非线性模型？是生成式模型还是判别式模型？请给出逻辑回归的数学公式,并简要回答上述问题.

= 三、 随机森林集成算法

随机森林（Random Forest）是一种常用的集成学习算法.请回答以下问题：
1. 简要说明随机森林的工作原理.
2. 随机森林在特征选择方面有什么优势？

= 四、 最小风险贝叶斯决策

对一维模式的两分类问题,设两类模式均为正态分布,其均值和方差分别为：
$ mu_1 = 0, sigma_1^2 = 4 $
$ mu_2 = 2, sigma_2^2 = 4 $

这里采用 (0-1) 损失函数,$ P(omega_1) = e P(omega_2) $,这里 $e$ 是自然对数的底数.

1. 试大致绘出两类模式的密度函数曲线.
   #v(10pt)
   #align(center)[#rect(width: 80%, height: 120pt, stroke: 0.5pt + gray)[【两类模式的密度函数曲线绘图区域】]]
   #v(10pt)
2. 试写出基于自然对数的对数似然比的最小风险贝叶斯决策规则,并进行计算.
3. 判断两个样本 $x_1 = 2, x_2 = 4$ 分别属于哪一类.

= 五、 感知器算法判别

已知两类二维模式向量样本如下：
- $omega_1$: $X_1 = [0, 0]^T, X_2 = [0, 1]^T$
- $omega_2$: $X_3 = [1, 0]^T, X_4 = [1, 1]^T$

设增广权矢量初值为 $W(1) = [0, 1, 1]^T$.请使用感知器算法（Perceptron Algorithm）求解判别函数,并绘出其判别界面.

= 六、 主成分分析 (PCA)

有四个二维模式向量构成的样本集：
$ X_1 = [1, -1]^T, X_2 = [3, 3]^T, X_3 = [-1, 1]^T, X_4 = [-3, -3]^T $

1. 求该样本集的均值向量和协方差矩阵.
2. 求出协方差矩阵的全部特征值及对应的特征向量,并在图中标示出第一主元（First Principal Component）方向.

= 七、 最大最小距离聚类

设有 9 个一维模式样本：
$ X_1=0, X_2=1, X_3=2, X_4=6, X_5=7, X_6=8, X_7=10, X_8=11, X_9=12 $

现有一维数轴分布如下图所示：
#v(10pt)
#align(center)[#rect(width: 90%, height: 50pt, stroke: 0.5pt + black)[【数轴：0(X1) - 1(X2) - 2(X3) ------ 6(X4) - 7(X5) - 8(X6) ------ 10(X7) - 11(X8) - 12(X9)】]]
#v(10pt)

请分别在下面两种参数条件下,用最大最小距离法对这些样本进行聚类分析.
1. 初始聚类中心 $Z_1 = X_1$,阈值参数 $theta = 0.4$.
2. 初始聚类中心 $Z_1 = X_3$,阈值参数 $theta = 0.6$.

= 八、 综合应用场景分析题

假设你正在为一家金融公司开发一个客户贷款违约预测模型,用于预测客户是否会违约.数据集包含以下字段：
- `customer_id`: 客户 ID
- `age`: 客户年龄
- `income`: 年收入
- `loan_amount`: 贷款金额
- `loan_term`: 贷款期限（年）
- `credit_score`: 信用评分
- `default`: 是否违约（0：否,1：是）

公司希望你开发一个机器学习模型,根据客户的这些特征来预测客户是否会违约.试回答以下问题：
+ *a.* 根据上述数据,你需要对数据进行哪些预处理（Preprocessing）步骤？列出至少三个步骤并简述其原因.
+ *b.* 针对违约分类问题,常见的机器学习模型有哪些？请列出两种,并简述它们的基本原理.
+ *c.* 你会选择哪些评估指标（Evaluation Metrics）来评估分类模型的效果？列出两种并解释其含义.

#pagebreak()

// --- 试卷二卷头 ---
#align(center)[
  #text(size: 16pt, weight: "bold")[武汉大学计算机学院 2020-2021 学年\ 第 2 学期期末考试试卷（A卷）]
  #v(5pt)
  #text(size: 13pt, weight: "bold")[课程：机器学习]
]
#v(15pt)

= 一、 KNN 分类算法复杂度分析 （10分）

在 $K$ 最近邻（KNN）分类算法中,当参数 $K$ 取值较小的时候,分析此时模型的复杂度（高/低）、模型偏差（高/低）和方差（高/低）,并详细说明其背后的理由.

= 二、 多类模式判别区域绘制 （10分）

一个三类分类问题,其判别函数分别定义为：
$ d_1(x) = x_1 + x_2 - 4 $
$ d_2(x) = -x_1 + x_2 + 2 $
$ d_3(x) = -x_2 + 3 $

现有一模式向量 $x = [8, 5]^T$,试判断该样本应属于哪一类？并完整画出这三类模式在二维空间中的分布区域边界.

= 三、 贝叶斯最小风险与最小错误率决策 （15分）

进行一项疾病检查,两类状态分别表示：$omega_1$ 表示正常,$omega_2$ 表示异常.两类的先验概率分别为：
- 正常 $P(omega_1) = 0.99$
- 异常 $P(omega_2) = 0.01$

现有一个待识别的样本细胞,其观察值为 $x$.从类条件概率密度函数曲线上可查得：
$ P(x | omega_1) = 0.3, quad P(x | omega_2) = 0.5 $

并且假设将正常结果误判为异常的风险 $lambda_{21} = 1$,将异常结果误判为正常得风险 $lambda_{12} = 100$.而决策正确的风险（正常判为正常、异常判为异常）为 $lambda_{11} = lambda_{22} = 0$.

试用该观察值 $x$ 采用以下两种方法对其进行分类决策：
1. 基于最小错误率的贝叶斯决策；
2. 基于最小风险的贝叶斯决策；
3. 请对比分析两种决策结果的异同点,并剖析导致该系统偏差的深层次原因.

= 四、 决策树与信息增益 （15分）

考虑如下二分类问题的训练样本集：

#align(center)[
  #table(
    columns: (1.5cm, 1.5cm, 1.5cm),
    align: center + horizon,
    stroke: 0.5pt + black,
    fill: (x, y) => if y == 0 { rgb("#e2e8f0") } else { none },
    [#strong([样本序号])], [#strong([属性 A])], [#strong([属性 B])], [#strong([类别])],
    [1], [T], [T], [+],
    [2], [T], [T], [+],
    [3], [T], [F], [-],
    [4], [F], [F], [+],
    [5], [F], [T], [-],
    [6], [F], [T], [-],
    [7], [F], [F], [T],
    [8], [F], [F], [+],
    [9], [F], [T], [-],
  )
]

1. 计算整个训练样本集关于类别的信息熵 $H("Data")$.
2. 分别计算按照属性 A 和属性 B 划分数据集后的信息增益（Information Gain）.
3. 使用信息增益准则的决策树分类算法在根节点应该首先使用哪个属性？

= 五、 离散 K-L 变换（PCA） （15分）

已知有四个二维模式向量：
$ x_1 = [0.5, 0.5], quad x_2 = [1, 1], quad x_3 = [-0.5, -0.5], quad x_4 = [-1, -1] $

1. 求出这四个模式构成的样本集的自相关矩阵 $R$.
2. 求出自相关矩阵 $R$ 的两个特征值及其对应的特征向量,并在坐标系中画出这两个特征向量的方向.
3. 使用自相关矩阵 $R$ 的最大特征值 $lambda_1$ 所对应的特征向量 $mu_1$,对原样本集 ${x_i, i=1,2,3,4}$ 进行离散 K-L 变换.

= 六、 感知器算法求解判别函数 （15分）

已知以下两类训练样本：
- 第一类：$x_1 = [0, 0, 0]^T, quad x_2 = [0, 1, 1]^T$
- 第二类：$x_3 = [1, 0, 1]^T, quad x_4 = [1, 1, 0]^T$

请使用感知器算法求出分类器的判别函数.（已知初始化增广权矢量 $W_1 = [1, 1, 1, 1]^T$,校正增量系数 $c = 1$）.

= 七、 机器学习综合工程系统设计题 （20分）

#block(
  fill: rgb("#f7fafc"),
  inset: 12pt,
  radius: 4pt,
  stroke: 0.5pt + rgb("#cbd5e0")
)[
  *应用场景*：某公司现有一台自带可编程通信接口并能对三种水果进行分拣的水果分拣机.公司提出对其进行智能化升级改造,以现有分拣机为基础扩展 PC 机水果自动分类识别子系统和通信模块,从而构建一种能对*猕猴桃*、*草莓*和*山楂*进行分类识别的智能分拣系统.
]

1. 若采用多层感知机（MLP）网络对水果进行分类识别,试给出水果自动分类识别子系统总体设计方案及关键设计方法（*要求及说明*：叙述总字数在 150~400 字之间；关键设计方法可结合流程图或伪代码展示；必要时可借助图表辅助说明）.
2. 请给出采用 MLP 分类识别出三种不同水果的关键程序片段（Python / PyTorch 或伪代码均可）.


#align(center)[
  #text(size: 18pt, weight: "bold")[机器学习与模式识别期末复习题（三）]
  #v(5pt)
  #text(size: 11pt, fill: gray)[根据最新试卷图片精准重构 | Typst 规范化排版]
]
#v(10pt)

= 1. 欺诈检测模型（类别不平衡问题）

要建立一个欺诈检测模型,正常样本有 300,000 条（类别 0）,欺诈样本只有 500 条（类别 1）.用这些数据训练模型会面临什么问题？请简述该问题,并列举三种常见的解决方法.

= 2. 模型复杂度与泛化误差

1. 请画出示意图,横轴为模型复杂度,纵轴为误差.在图中绘出训练误差和测试误差的变化曲线,并标注出过拟合（Overfitting）和欠拟合（Underfitting）的区域.
   #v(10pt)
   #align(center)[#rect(width: 80%, height: 140pt, stroke: 0.5pt + gray)[【模型复杂度 vs 泛化误差曲线绘图区域】]]
   #v(10pt)
2. 简述过拟合和欠拟合的主要特征.
3. 列举五种防止模型过拟合的方法.

= 3. 贝叶斯分类

给定一个包含 10 个样本的数据集,每个样本包含 3 个特征（特征取值均为 `True` 或 `False`）以及一个对应的类别标签.
1. 数数,计算各类别的概率.
2. 计算各特征在给定类别下的条件概率.
3. 给定一个新的测试样本,使用朴素贝叶斯分类器计算其属于各个类别的概率,并给出分类结果.

= 4. 支持向量机（SVM）推导

考虑一种修改后的软间隔 SVM 原始形式,其正则化项采用 L2 视角,即目标函数中松弛变量的惩罚项为：
$ 1/2 C sum_(i=1)^n xi_i^2 $

1. 写出该问题的拉格朗日原始问题表达式.
2. 推导其对应的对偶问题方程.

= 5. 决策树

给定一个包含 15 个样本的数据集,有 3 个特征 $A, B, C$,对应一个分类目标 $D$（就是类别,还是二分类）.
1. 计算数据集类别的熵 $H(D)$.
2. 计算给定特征下的条件熵 $H(D|A)$,$H(D|B)$,$H(D|C)$.
3. 分别计算这 3 个特征的信息增益 $g(D, A)$,$g(D, B)$,$g(D, C)$.

= 6. 主成分分析（PCA）

给定 4 个二维样本点数据（数据很好算）：
1. 对样本数据进行中心化处理.
2. 计算样本的协方差矩阵.
3. 计算协方差矩阵的特征值和对应的特征向量.
4. 计算样本在第一主成分上的投影结果.

= 7. K-means 聚类

给定一维数轴上的 4 个数据点,好像是 $x_1=0, x_2=4, x_3=5, x_4=10$.
1. 给定初始化方案 A,$C_1=0, C_2=10$,计算聚类迭代后的最终结果.
2. 给定初始化方案 B,$C_1=4, C_2=5$,计算聚类迭代后的最终结果.
3. 定性比较上述两种方案,说明哪一种效果更好并阐述理由.

= 8. 强化学习

1. 请列出强化学习中马尔可夫决策过程（MDP）的四元组,并解释每个元素的作用,好像还有整体的作用,都解释一下吧.
2. 简述强化学习的主要目标是什么.
3. 对比强化学习与监督学习,说明二者在学习方式、应用场景及反馈机制上的区别.

= 9. 综合案例分析（医疗肿瘤风险识别）

某医院计划利用各种数据（包括病历文本、体检数据、化验指标等）开发一个机器学习模型,用于识别早期肿瘤风险.

1. 针对逻辑回归、支持向量机和随机森林三种模型,分析它们的偏差和方差,并分析：哪种模型更容易欠拟合？哪种模型更容易过拟合？请解释原因.
2. 在模型评估中发现,模型在不同群体上的表现差异显著：在年轻人群体中的准确率达到 90%,而在老年人群体中只有 72%.请问这是机器学习中的什么偏差问题？造成该现象的可能原因有哪些？
3. 深度学习在医疗领域常被认为是“不可解释的”.请解释为什么会这样？举例说明这种不可解释性在医疗诊断中会带来哪些潜在风险.