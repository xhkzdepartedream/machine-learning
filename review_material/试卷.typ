#set text(font :("Times New Roman","STSong"))
#import "@preview/cuti:0.4.0":show-cn-fakebold
#show: show-cn-fakebold
#show math.equation.where(block: false): it => math.display(it)
#show math.equation: set text(font: ("New Computer Modern Math",) , purple)
#set table(
  inset: 6pt,
  stroke: none
)

#show figure.where(
  kind: table
): set figure.caption(position: top)

#show figure.where(
  kind: image
): set figure.caption(position: bottom)
#let show_answer = true

#let answer(body) = {
  if show_answer {
    block(
      fill: gray.lighten(80%),   
      inset: 8pt,                
      radius: 4pt,               
      width: 100%,               
      stroke: 0.5pt + gray,      
    )[
        #set text(fill: blue,weight: "bold") // 这一行会让方框内所有的文字和公式都变成蓝色
  #show math.equation: set text(font: ("New Computer Modern Math",) , blue)
      #body
    ]
  } else {
    // 当开关为 false 时，什么都不渲染（返回空）
    none
  }
}

4567因为解答过程较长放在后面

== 一、
请简要解释监督学习、无监督学习及强化学习之间的区别，并分别举一个应用实例。

① 监督学习处理的对象是所谓的有标签训练数据，它利用有标签的训练数据来学习一个模型，它的目标是用学到的模型给无标签的测试数据打上标签。(决策树和随机森林)

② 无监督学习的训练数据没有标签，它自动从训练数据中学习知识，建立模型。 -> K-均值聚类 (K-means)

③ 强化学习是通过与环境的交互来学习策略，以最大化累积奖励。模型（或称为代理）通过试错过程，从环境中获取反馈来调整其行为策略。如标准的马尔可夫决策过程。

== 二、
逻辑回归模型主要用于分类还是回归？是线性模型还是非线性模型？是生成式模型还是判别式模型？请给出逻辑回归的数学公式，并简要回答上述问题。

- 逻辑回归主要用于分类任务。
- 逻辑回归模型本质上是一个线性模型，但由于其输入经过 sigmoid 函数进行变换，整体表现出非线性模型。
- 判别性模型。
- 公式：
  + 线性部分：$z = W^T x + b$
  + $ sigma(z) = frac(1, 1 + e^(-z)) $

== 三、
随机森林是一种常用的集成学习算法。请回答以下问题：

(1) 简要说明随机森林的工作原理。

*① 工作原理*
从训练数据集中有放回地随机抽取多个子集样本，形成多个不同的训练集。对每个训练集训练一个决策树。在训练决策树时，还在每个结点进行特征选择时，随机选择一部分特征，即特征子集选择。最后用所有训练好的决策树共同构成随机森林。对于分类任务，随机森林通过对每棵树的预测结果进行投票并决定最终结果。

(2) 随机森林在特征选择方面有什么优势？

*② 特征选择方面的优势*
+ * 减少过拟合。* 由于每棵树在训练时只使用了部分特征（通过特征子集选择），随机森林能够减少模型对特定特征的依赖，从而降低过拟合的风险。
+ * 能处理大量数据。* 随机森林能处理大量特征，并且在特征子集选择过程中，不显著增加计算复杂度。

== 四、
对一维模式的两分类问题，设两类模式均为正态分布，其均值和方差分别为
$ mu_1 = 0, sigma_1^2 = 4; quad mu_2 = 2, sigma_2^2 = 4 $
采用（0-1）损失函数，且 $P(omega_1) = e P(omega_2)$，这里 $e$ 是自然对数的底数。

(1) 试大致绘出两类模式的密度函数曲线。

(2) 试写出基于自然对数的对数似然比的最小风险贝叶斯决策规则，计算基于 $x$ 的判决阈值与规则。

(3) 判断两个样本：$x_1 = 2, x_2 = 4$，分别属于哪一类。

== 五、
已知两类训练样本为
$ omega_1: X_1 = [0, 0]^T, quad X_2 = [0, 1]^T; quad omega_2: X_3 = [1, 0]^T, quad X_4 = [1, 1]^T $
设增广权矢量初值为 $W(1) = [0, 1, 1]^T$，用感知器算法求解判别函数，并绘出判别界面。

== 六、
有四个二维模式向量构成的样本集：
$ X_1 = [1, -1]^T, quad X_2 = [3, 3]^T, quad X_3 = [-1, 1]^T, quad X_4 = [-3, -3]^T $

(1) 求该样本集的均值向量和协方差矩阵。

(2) 求出协方差矩阵的全部特征值及对应的特征向量，并在图中标示出第一主元方向。

== 七、
设有 9 个一维模式样本：$X_1=0, X_2=1, X_3=2, X_4=6, X_5=7, X_6=8, X_7=10, X_8=11, X_9=12$，分别在下面两种参数条件下，用最大最小距离法对这些样本进行聚类分析。

(1) $Z_1=X_1, theta=0.4$

(2) $Z_1=X_3, theta=0.6$

== 八、
假设你正在为一家金融公司开发一个客户分类系统，以预测客户是否会违约。数据集包含以下字段：
- customer_id：客户 ID
- age：客户年龄
- income：年收入
- loan_amount：贷款金额
- loan_term：贷款期限（年）
- credit_score：信用评分
- default：是否违约（0：否，1：是）

公司希望你开发一个机器学习模型，根据客户的这些特征来预测客户是否会违约。
试回答一下问题：

a. 根据上述数据，你需要对数据进行哪些预处理步骤？列出至少三个步骤并简述其原因。


+ *数据清理：*
  + *缺失值处理：* 检查数据集中是否有缺失值，并处理；使用均值、中位数或其他方法填补缺失值。
  + *异常值处理：* 检测并处理异常值，对于一些明显错误的值需要处理。
+ *数据标准化/归一化：* 将收入、贷款金额、贷款期限和信用评分等数值特征标准化或归一化到同一尺度，以确保不同特征在模型训练过程中具有相似的影响范围。
+ *特征工程：*
  + *特征选择：* 根据数据的相关性和特征重要性，选择对违约预测最有帮助的特征。
  + *特征转换：* 将特征转换为分类变量，或创建新的特征。

b. 针对分类问题，常见的机器学习模型有哪些？请列出两种，并简述它们的基本原理。


- *逻辑回归：* 通过学习输入特征线性组合，使用逻辑函数将线性组合的结果映射到 0 和 1 之间的概率值，用于预测某个样本属于某类的概率。
- *随机森林：* 构建多个决策树，在预测时综合这些树的结果来提高模型的准确性和稳定性。每棵树都是在随机子集上训练的，并且每次分裂时只考虑随机一部分特征，从而减少过拟合的风险。

c. 你会选择哪些评估指标来评估分类模型的效果？列出两种并解释其含义。


- *准确率：* 正确预测样本占样本总量比例。$\A c c = (T P + T N) / (T P + T N + F P + F N)$


= 解答四

*已知条件：*
- 类 $omega_1$: $mu_1 = 0$, $sigma_1^2 = 4$ (即 $sigma_1 = 2$)
- 类 $omega_2$: $mu_2 = 2$, $sigma_2^2 = 4$ (即 $sigma_2 = 2$)
- 先验概率关系: $P(omega_1) = e P(omega_2)$
- 损失函数: (0-1) 损失

== (1) 试大致绘出两类模式的密度函数曲线

正态分布的概率密度函数通式为：
$ p(x|omega_i) = 1/(sqrt(2 pi) sigma_i) exp( -(x - mu_i)^2 / (2 sigma_i^2) ) $

代入题目参数：
- 对于 $omega_1$：
  $ p(x|omega_1) = 1/(sqrt(8 pi)) exp( -x^2 / 8 ) $
  这是一个中心在 $x = 0$，峰值高度为 $1/sqrt(8 pi)$ 的钟形曲线。

- 对于 $omega_2$：
  $ p(x|omega_2) = 1/(sqrt(8 pi)) exp( -(x - 2)^2 / 8 ) $
  这是一个中心在 $x = 2$，峰值高度同为 $1/sqrt(8 pi)$ 的钟形曲线。

*绘图说明：*
两条曲线形状完全相同（因为方差相等），只是位置发生了平移。
- $p(x|omega_1)$ 的对称轴是 $x = 0$。
- $p(x|omega_2)$ 的对称轴是 $x = 2$。

== (2) 试写出基于自然对数的对数似然比的最小风险贝叶斯决策规则，计算基于 $x$ 的判决阈值与规则

*1. 贝叶斯决策规则*
在 (0-1) 损失函数下，最小风险贝叶斯决策等价于最大后验概率决策。
决策规则为：
若 $P(omega_1 | x) > P(omega_2 | x)$，则判为 $omega_1$ 类；否则判为 $omega_2$ 类。

根据贝叶斯公式，上述规则等价于比较似然与先验的乘积：
若 $p(x|omega_1) P(omega_1) > p(x|omega_2) P(omega_2)$，则判为 $omega_1$。

*2. 引入对数似然比*
将不等式变形为似然比的形式：
$ (p(x|omega_1)) / (p(x|omega_2)) > (P(omega_2)) / (P(omega_1)) $

两边取自然对数：
$ ln( (p(x|omega_1)) / (p(x|omega_2)) ) > ln( (P(omega_2)) / (P(omega_1)) ) $

*3. 代入数值计算*
- *右边（阈值项）：*
  已知 $P(omega_1) = e P(omega_2)$，即 $(P(omega_2)) / (P(omega_1)) = 1/e$。
  所以，$ln( (P(omega_2)) / (P(omega_1)) ) = ln(e^(-1)) = -1$。

- *左边（对数似然比）：*
  $ ln( (p(x|omega_1)) / (p(x|omega_2)) ) = ln(p(x|omega_1)) - ln(p(x|omega_2)) $
  
  代入密度函数公式（注意系数 $1/sqrt(8 pi)$ 相同，相除为 1，取对数为 0）：
  $ = ( -x^2/8 ) - ( -(x-2)^2/8 ) $
  $ = ( (x-2)^2 - x^2 ) / 8 $
  $ = ( (x^2 - 4x + 4) - x^2 ) / 8 $
  $ = ( -4x + 4 ) / 8 $
  $ = ( 1 - x ) / 2 $

*4. 综合不等式*
将左右两边代回决策不等式：
$ (1 - x) / 2 > -1 $
$ 1 - x > -2 $
$ -x > -3 $
$ x < 3 $

*结论：*
- *判决阈值：* $x_0 = 3$
- *决策规则：*
  - 若 $x < 3$，则判为 $omega_1$ 类；
  - 若 $x > 3$，则判为 $omega_2$ 类。

== (3) 判断两个样本：$x_1 = 2, x_2 = 4$，分别属于哪一类

根据第 (2) 问得出的决策规则（$x < 3$ 为 $omega_1$， $x > 3$ 为 $omega_2$）：

1. 对于样本 *$x_1 = 2$*：
   因为 $2 < 3$，满足 $omega_1$ 的条件。
   *所以 $x_1$ 属于 $omega_1$ 类。*

2. 对于样本 *$x_2 = 4$*：
   因为 $4 > 3$，满足 $omega_2$ 的条件。
   *所以 $x_2$ 属于 $omega_2$ 类。*

= 解答五

已知两类训练样本为：
$ omega_1: X_1 = [0, 0]^T, quad X_2 = [0, 1]^T $
$ omega_2: X_3 = [1, 0]^T, quad X_4 = [1, 1]^T $

设增广权矢量初值为 $W(1) = [0, 1, 1]^T$，用感知器算法求解判别函数，并绘出判别界面。

== 1. 样本增广与规范化

首先将样本向量增广（末尾加 1），并将属于 $omega_2$ 类的样本乘以 -1 进行规范化，使得所有样本期望满足 $W^T X > 0$。

- *$omega_1$ 类样本（保持不变）：*
  - $X_1 = [0, 0, 1]^T$
  - $X_2 = [0, 1, 1]^T$

- *$omega_2$ 类样本（乘以 -1）：*
  - $X_3' = -[1, 0, 1]^T = [-1, 0, -1]^T$
  - $X_4' = -[1, 1, 1]^T = [-1, -1, -1]^T$

== 2. 感知器算法迭代过程

设定初始权向量 $W(1) = [0, 1, 1]^T$，学习率 $rho = 1$。

*第 1 轮迭代：*
- $W(1)^T X_1 = 1 > 0$ (正确) $-> W(2) = W(1)$
- $W(2)^T X_2 = 2 > 0$ (正确) $-> W(3) = W(2)$
- $W(3)^T X_3' = -1 <= 0$ (错误) $-> W(4) = W(3) + X_3' = [-1, 1, 0]^T$
- $W(4)^T X_4' = 0 <= 0$ (错误) $-> W(5) = W(4) + X_4' = [-2, 0, -1]^T$

*第 2 轮迭代：*
- $W(5)^T X_1 = -1 <= 0$ (错误) $-> W(6) = W(5) + X_1 = [-2, 0, 0]^T$
- $W(6)^T X_2 = 0 <= 0$ (错误) $-> W(7) = W(6) + X_2 = [-2, 1, 1]^T$
- $W(7)^T X_3' = 1 > 0$ (正确) $-> W(8) = W(7)$
- $W(8)^T X_4' = 0 <= 0$ (错误) $-> W(9) = W(8) + X_4' = [-3, 0, 0]^T$

*第 3 轮迭代：*
- $W(9)^T X_1 = 0 <= 0$ (错误) $-> W(10) = W(9) + X_1 = [-3, 0, 1]^T$
- $W(10)^T X_2 = 1 > 0$ (正确) $-> W(11) = W(10)$
- $W(11)^T X_3' = 2 > 0$ (正确) $-> W(12) = W(11)$
- $W(12)^T X_4' = 2 > 0$ (正确) $-> W(13) = W(12)$

*第 4 轮迭代：*
- $W(13)^T X_1 = 1 > 0$ (正确)
- $W(13)^T X_2 = 1 > 0$ (正确)
- $W(13)^T X_3' = 2 > 0$ (正确)
- $W(13)^T X_4' = 2 > 0$ (正确)

所有样本分类正确，算法收敛。

== 3. 最终结果

*最终权向量：*
$$ W = [-3, 0, 1]^T $$

*判别函数：*
设原始输入向量为 $x = [x_1, x_2]^T$，增广后为 $X = [x_1, x_2, 1]^T$。
$$ d(x) = W^T X = [-3, 0, 1] [x_1, x_2, 1]^T = -3x_1 + 1 $$

*判别界面：*
令 $d(x) = 0$，即：
$$ -3x_1 + 1 = 0 implies x_1 = 1/3 $$

*判别界面图示说明：*
判别界面是一条垂直于 $x_1$ 轴（横轴）的直线，位置在 $x_1 = 1/3$ 处。
- 当 $x_1 < 1/3$ 时（如 $x_1=0$），$d(x) > 0$，判为 $omega_1$ 类。
- 当 $x_1 > 1/3$ 时（如 $x_1=1$），$d(x) < 0$，判为 $omega_2$ 类。

= 解答六、主成分分析计算

已知四个二维模式向量构成的样本集：
$ X_1 = [1, -1]^top, quad X_2 = [3, 3]^top, quad X_3 = [-1, 1]^top, quad X_4 = [-3, -3]^top $

== (1) 求该样本集的均值向量和协方差矩阵

=== 1. 均值向量 $M$
样本集包含 4 个向量，均值向量 $M$ 为所有样本向量的平均值：
$ M = 1/n sum_(i=1)^n X_i = 1/4 (X_1 + X_2 + X_3 + X_4) $
$ M = 1/4 mat(1+3-1-3; -1+3+1-3) = mat(0; 0) $
*均值向量 $M = [0, 0]^top$。*

=== 2. 协方差矩阵 $Sigma$
根据样本协方差矩阵公式（分母为 $n-1$）：
$ Sigma = 1/(n-1) sum_(i=1)^n (X_i - M)(X_i - M)^top $
这里 $n=4$，所以分母为 $3$。由于 $M=[0,0]^top$，则 $X_i - M = X_i$。

分别计算每一项 $(X_i - M)(X_i - M)^top$：

- $i=1$: $ [1, -1]^top [1, -1] = mat(1, -1; -1, 1) $
- $i=2$: $ [3, 3]^top [3, 3] = mat(9, 9; 9, 9) $
- $i=3$: $ [-1, 1]^top [-1, 1] = mat(1, -1; -1, 1) $
- $i=4$: $ [-3, -3]^top [-3, -3] = mat(9, 9; 9, 9) $

求和：
$ sum = mat(1+9+1+9, -1+9-1+9; -1+9-1+9, 1+9+1+9) = mat(20, 16; 16, 20) $

计算协方差矩阵：
$ Sigma = 1/3 mat(20, 16; 16, 20) = mat(20/3, 16/3; 16/3, 20/3) approx mat(6.67, 5.33; 5.33, 6.67) $

== (2) 求出协方差矩阵的全部特征值及对应的特征向量，并在图中标示出第一主元方向

=== 1. 求特征值
设协方差矩阵为 $A = mat(20/3, 16/3; 16/3, 20/3)$。

特征方程为 $det(lambda E - A) = 0$：
$ det(mat(lambda - 20/3, -16/3; -16/3, lambda - 20/3)) = 0 $
$ (lambda - 20/3)^2 - (-16/3)^2 = 0 $
$ (lambda - 20/3)^2 = (16/3)^2 $
$ lambda - 20/3 = plus.minus 16/3 $

解得两个特征值：
- $ lambda_1 = 20/3 + 16/3 = 36/3 = 12 $
- $ lambda_2 = 20/3 - 16/3 = 4/3 $

=== 2. 求特征向量
*对于特征值 $lambda_1 = 12$：*

代入 $(A - lambda_1 E)v = 0$：
$ mat(20/3 - 12, 16/3; 16/3, 20/3 - 12) mat(v_1; v_2) = mat(0; 0) $
$ mat(-16/3, 16/3; 16/3, -16/3) mat(v_1; v_2) = mat(0; 0) $

化简得 $-v_1 + v_2 = 0 -> v_1 = v_2$。

取特征向量 *$V_1 = [1, 1]^top$*。

*对于特征值 $lambda_2 = 4/3$：*

代入 $(A - lambda_2 E)v = 0$：
$ mat(20/3 - 4/3, 16/3; 16/3, 20/3 - 4/3) mat(v_1; v_2) = mat(0; 0) $
$ mat(16/3, 16/3; 16/3, 16/3) mat(v_1; v_2) = mat(0; 0) $

化简得 $v_1 + v_2 = 0 -> v_1 = -v_2$。

取特征向量 *$V_2 = [1, -1]^top$*（或 $[-1, 1]^top$）。

=== 3. 第一主元方向
第一主元方向对应于*最大特征值*的特征向量。

最大特征值为 $lambda_1 = 12$，对应的特征向量为 $V_1 = [1, 1]^top$。

= 七、

设有 9 个一维模式样本：$X_1=0, X_2=1, X_3=2, X_4=6, X_5=7, X_6=8, X_7=10, X_8=11, X_9=12$，分别在下面两种参数条件下，用最大最小距离法对这些样本进行聚类分析。

== (1) $Z_1=X_1, theta=0.4$

$
Z_1 = X_1 #h(1em) theta = 0.4 #h(1em)
Z_2 = X_9 #h(1em) theta = 0.4 #h(1em)
d(Z_1, Z_2) = 12 #h(1em) theta * d(Z_1, Z_2) = 12 * 0.4 = 4.8
$

$
  #table(
    columns: 4,
    align: left,
    [d_21 = 1], [d_22 = 11], [d_2 = min(1, 11) = 1], [],
    [d_31 = 2], [d_32 = 10], [d_3 = min(2, 10) = 2], [],
    [d_41 = 6], [d_42 = 6], [d_4 = min(6, 6) = 6], [> 4.8],
    [d_51 = 7], [d_52 = 5], [d_5 = min(7, 5) = 5], [> 4.8],
    [d_61 = 8], [d_62 = 4], [d_6 = min(8, 4) = 4], [],
  )
$

则建立第3个聚类中心 $underline(Z_3 = X_4 = 6)$.

$
  #table(
    columns: 5,
    align: left,
    [d_21 = 1], [d_22 = 11], [d_23 = 5], [d_2 = min = 1], [],
    [d_31 = 2], [d_32 = 10], [d_33 = 4], [d_3 = min = 2], [],
    [d_51 = 7], [d_52 = 5], [d_53 = 1], [d_5 = min = 1], [],
    [d_61 = 8], [d_62 = 4], [d_63 = 2], [d_6 = min = 2], [],
    [d_71 = 10], [d_72 = 2], [d_73 = 4], [d_7 = min = 2], [],
    [d_81 = 11], [d_82 = 1], [d_83 = 5], [d_8 = min = 1], [],
  )
$

$
  #table(
    columns: 2,
    align: left,
    [属于 X_1 类], [X_1, X_2, X_3, Z_1],
    [属于 X_4 类], [X_4, X_5, X_6, Z_3],
    [属于 X_9 类], [X_7, X_8, X_9, Z_2],
  )
$

== (2) $Z_1=X_3, Z_2=X_9$

$
Z_1 = X_3 #h(1em) Z_2 = X_9 #h(1em)
  d(Z_1, Z_2) = 10 #h(1em) theta * d = 6 #h(1em)
  #table(
    columns: 3,
    align: left,
    [d_11 = 2], [d_12 = 12], [d_1 = min = 2],
    [d_21 = 1], [d_22 = 10], [d_2 = min = 1],
    [d_41 = 4], [d_42 = 6], [d_4 = min = 4],
    [d_51 = 5], [d_52 = 5], [d_5 = min = 5],
    [d_61 = 6], [d_62 = 4], [d_6 = min = 4],
  )
$

因此分为2类。

$
  #table(
    columns: 2,
    align: left,
    [1, 2, 3, 4, 5], [一类],
    [6, 7, 8, 9], [二类],
  )
$