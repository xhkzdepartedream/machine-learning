=== AdaBoost算法

对于分类问题而言，给定一个训练样本集，求比较粗糙的分类规则（弱分类器）要比求精确的分类规则（强分类器）容易得多。Boosting就是从弱学习算法出发，反复学习，得到一系列弱分类器（又称为基本分类器），然后组合这些弱分类器，构成一个强分类器。

大多数的Boosting都是改变训练数据的概率分布（训练数据的权值分布)，针对不同的训练数据分布调用弱学习算法学习一系列弱分类器。

这样，对Boosting来说，有两个问题需要回答：一是在每一轮如何改变训练数据的权值或概率分布；二是如何将弱分类器组合成一个强分类器。

AdaBoost的做法是*提高那些被前一轮弱分类器错误分类样本的权值，而降低那些被正确分类样本的权值*,和采取*加权多数表决*的方法.具体地，加大分类误差率小的弱分类器的权值，使其在表决中起较大的作用：减小分类误差率大的弱分类器的权值，使其在表决中起较小的作用。#remark[
  Boosting算法必须在每一轮改变训练数据的权值，主要有以下三个核心原因：

  + 迫使模型“关注”难分类样本（核心机制）
  + 避免生成重复模型（保证多样性）
  + 数学本质：梯度下降
]

假定给定一个二类分类的训练数据集$T={(x_1,y_1),(x_2,y_2),dots,(x_N,y_N)}$, 其中每个样本点由实例与标记组成。实例$x_i in cal(X) subset.eq R^n$, 标记$y_i in cal(Y)={-1,+1}$, $cal(X)$是实例空间，$cal(Y)$是标记集合。AdaBoost 利用以下算法，从训练数据中学习一系列弱分类器或基本分类器，并将这些弱分类器线性组合成为一个强分类器。


#algorithm[AdaBoost算法][
  *输入：*训练数据集 $T = {(x_1, y_1), (x_2, y_2), dots, (x_N, y_N)}$，其中 $x_i in cal(X) subset.eq RR^n$，$y_i in cal(Y) = {-1, +1}$；弱学习算法。 \
  *输出：*最终分类器 $G(x)$。

  + *初始化训练数据的权值分布*。
    $ D_1 = (w_(11), dots, w_(1i), dots, w_(1N)), quad w_(1i) = 1/N, quad i = 1, 2, dots, N $
  + *对 $m = 1, 2, dots, M$*
    + 使用具有权值分布 $D_m$ 的训练数据集学习，得到基本分类器：
      $ G_m (x): cal(X) arrow {-1, +1} $
    + 计算 $G_m (x)$ 在训练数据集上的分类误差率：
      $ e_m = sum_(i=1)^N P(G_m (x_i) != y_i) = sum_(i=1)^N w_(m i) II(G_m (x_i) != y_i) $
    + 计算 $G_m (x)$ 的系数：
      $ alpha_m = 1/2 ln frac(1 - e_m, e_m) $
    + 更新训练数据集的权值分布：
      $ D_(m+1) = (w_(m+1,1), dots, w_(m+1,i), dots, w_(m+1,N)) $
      $ w_(m+1, i) = w_(m i) / Z_m exp(-alpha_m y_i G_m (x_i)), quad i = 1, 2, dots, N $
      这里 $Z_m$ 是归一化因子：
      $ Z_m = sum_(i=1)^N w_(m i) exp(-alpha_m y_i G_m (x_i)) $
  + *构建基本分类器的线性组合*
    $ f(x) = sum_(m=1)^M alpha_m G_m (x) $
  + *得到最终分类器*
    $ G(x) = "sign"(f(x)) = "sign"(sum_(m=1)^M alpha_m G_m (x)) $
]

+ 步骤 1 假设训练数据集具有均匀的权值分布，即每个训练样本在基本分类器的学习中作用相同，这一假设保证第 1 步能够在原始数据上学习基本分类器 $G_1(x)$。
+ 步骤2 AdaBoost 反复学习基本分类器：
  #set enum(numbering: "(a)")
  + 使用当前分布 $D_m$ 加权的训练数据集学习基本分类器 $G_m (x)$。
  + 计算基本分类器 $G_m (x)$ 在加权训练数据集上的分类误差率：
    $ e_m = sum_(i=1)^N P(G_m(x_i) != y_i) = sum_(G_m(x_i) != y_i) w_(m i) $ <eq:8.8>
    这里，$w_(m i)$ 表示第 $m$ 轮中第 $i$ 个实例的权值，$sum_(i=1)^N w_(m i) = 1$。这表明，$G_m (x)$ 在加权的训练数据集上的分类误差率是被 $G_m (x)$ 误分类样本的权值之和。
  + 计算基本分类器 $G_m (x)$ 的系数 $alpha_m$： $alpha_m$ 表示 $G_m (x)$ 在最终分类器中的重要性。当 $e_m <= 1/2$ 时，$alpha_m >= 0$，并且 $alpha_m$ 随着 $e_m$ 的减小而增大，所以分类误差率越小的基本分类器在最终分类器中的作用越大，这样对结果的影响也就越大。
  + 更新训练数据的权值分布为下一轮作准备。
    
    如果预测正确，$y_i$和$G_m(x_i)$同号，乘积为$+1$，指数部分$exp(-alpha_m)<1$，权重$>1$。被误分类的则$<1$。误分类样本的权值被放大 $e^(2 alpha_m) = (1 - e_m) / e_m$ 倍。因此，误分类样本在下一轮学习中起更大的作用。#remark[
  AdaBoost 的迭代停止主要取决于以下 3 个条件，只要满足其中任意一个，算法就会停止：
  + 达到最大迭代次数（人为设定）
  + 训练误差为 0（完美分类）
  + 弱分类器失效（误差 $gt.eq 0.5$）
]
+ 步骤 3 中线性组合 $f(x)$ 实现 $M$ 个基本分类器的加权表决。所有 $alpha_m$ 之和并不为 1。$f(x)$ 的符号决定实例 $x$ 的类，$f(x)$ 的绝对值表示分类的确信度。#note[
  不改变所给的训练数据，而不断改变训练数据权值的分布，使得训练数据在基本分类器的学习中起不同的作用，这是 AdaBoost 的一个特点。

  利用基本分类器的线性组合构建最终分类器是 AdaBoost 的另一特点。
]




=== AdaBoost算法的解释



*1. 核心视角：指数损失函数的最小化*

AdaBoost 的所有公式（系数 $alpha_m$ 的计算、权值 $w$ 的更新）并非凭空捏造，其本质是在进行 *前向分步加法建模*，旨在最小化 *指数损失函数*：
$ L(y, f(x)) = exp(-y f(x)) $

其中 $y in {-1, +1}$，$f(x)$ 是强分类器的输出值。

#remark[
+ 如果 $y$ 和 $f(x)$ 符号相同（分类正确）且 $f(x)$ 绝对值很大（置信度高），则 $-y f(x)$ 为很大的负数，损失趋近于 0。
+ 如果符号相反（分类错误），损失会呈指数级爆炸增长。
]

AdaBoost 的迭代过程，实际上就是在使用 *贪心算法*，一步步地寻找基本分类器 $G_m$ 和系数 $alpha_m$，使得整体的指数损失最小化。


*2. 为什么 $alpha_m$ 是那个特定的公式？*

我们来看看 $alpha_m = 1/2 ln((1-e_m)/e_m)$ 是怎么来的。这是全算法最精妙的地方：它是损失函数的 *解析解 (Analytical Solution)*。

假设在第 $m$ 轮，我们已经有了前 $m-1$ 轮的组合 $f_(m-1)(x)$，现在的目标是找到最佳的 $alpha$ 和 $G(x)$ 来更新模型：
$ f_m (x) = f_(m-1) (x) + alpha G(x) $

我们的目标是最小化当前的总损失：
$ L(alpha, G) &= sum_(i=1)^N exp(-y_i (f_(m-1) (x_i) + alpha G(x_i))) \
&= sum_(i=1)^N underbrace(exp(-y_i f_(m-1) (x_i)), "记为 " tilde(w)_(m i)) exp(-y_i alpha G(x_i)) $

注意：这里的 $tilde(w)_(m i)$ 正是算法中第 $m$ 轮样本权值的非归一化形式。假设我们已经找到了使误差最小的 $G_m (x)$，现在解出最优的 $alpha$。将样本分为“正确分类”和“错误分类”两组：

$ L(alpha) &= sum_(y_i = G_m (x_i)) w_(m i) e^(-alpha) + sum_(y_i != G_m (x_i)) w_(m i) e^(alpha) \
            &= e^(-alpha) sum_("correct") w_(m i) + e^(alpha) sum_("incorrect") w_(m i) $

代入误差率 $e_m = sum_("incorrect") w_(m i)$ （假设权重已归一化），则正确分类的权重和为 $1 - e_m$：
$ L(alpha) = (1 - e_m) e^(-alpha) + e_m e^(alpha) $

对 $alpha$ 求导并令其为 0：
$ frac(partial L, partial alpha) = -(1 - e_m) e^(-alpha) + e_m e^(alpha) = 0 \
arrow.r.double (1 - e_m) e^(-alpha) = e_m e^(alpha) \
arrow.r.double frac(1 - e_m, e_m) = e^(2 alpha) \
arrow.r.double 2 alpha = ln(frac(1 - e_m, e_m)) \
arrow.r.double alpha = 1/2 ln(frac(1 - e_m, e_m)) $

*结论*：这个公式保证了在当前 $G_m$ 选定后，$alpha_m$ 的取值能让指数损失函数达到该方向上的 *极小值*。

*3. 权值更新公式的本质：梯度下降*

算法中的权值更新公式为：
$ w_(m+1, i) = w_(m, i) exp(-alpha_m y_i G_m (x_i)) $

如果我们把这公式递归展开：
$ w_(m+1, i) &= exp(-y_i f_(m-1) (x_i)) dot exp(-y_i alpha_m G_m (x_i)) \
&= exp(-y_i (f_(m-1) (x_i) + alpha_m G_m (x_i))) \
&= exp(-y_i f_m (x_i)) $

*数学含义*：
+ 样本 $i$ 的权值 $w_(m+1, i)$，在数值上正比于该样本在当前模型 $f_m$ 下的 *指数损失*。
+ 损失越大（分得越错），权重越大。

这可以理解为一种 *函数空间里的梯度下降*。每一轮的弱分类器 $G_m$，实际上是在拟合损失函数对于当前模型 $f_(m-1)$ 的负梯度方向。算法告诉下一轮的分类器：“请重点关注那些梯度（误差）最大的区域。”

*4. 0/1 损失的上界*

为什么不直接优化 0/1 损失（也就是分类错误率）？
+ 0/1 损失是不连续、非凸的，极难优化 (NP-hard)。
+ 指数损失函数 $e^(-y f(x))$ 是 0/1 损失 $I(y != "sign"(f(x)))$ 的 *光滑凸上界*。

数学上可以证明，随着指数损失不断降低并趋于 0，0/1 损失也会被迫趋于 0。AdaBoost 通过不断压低这个“上界”，间接地实现了分类错误率的最小化。

AdaBoost 的本质并非凭空捏造的权重调整，而是在进行 *前向分步加法建模 (Forward Stagewise Additive Modeling, FSAM)*。其核心目标是最小化 *指数损失函数*：
$ L(y, f(x)) = exp(-y f(x)) $
其中 $y in {-1, +1}$，$f(x)$ 是强分类器的输出值。

#remark[
  *直观理解：* 如果 $y$ 和 $f(x)$ 符号相同（分类正确）且 $|f(x)|$ 很大（置信度高），则 $-y f(x)$ 为很大的负数，损失 $L -> 0$。反之，损失会呈指数级爆炸增长。
]

AdaBoost 的迭代过程，实际上就是在使用 *贪心算法*，一步步地寻找基本分类器 $G_m$ 和系数 $alpha_m$，使得整体的指数损失最小化。

*关于 $alpha_m$ 的解析解推导*

$alpha_m = 1/2 ln((1-e_m)/e_m)$ 是损失函数的解析解。假设在第 $m$ 轮，我们已有 $f_(m-1)(x)$，现在的目标是寻找最佳的 $alpha$ 和 $G(x)$ 以最小化当前总损失：
$ L(alpha, G) &= sum_(i=1)^N exp(-y_i (f_(m-1) (x_i) + alpha G(x_i))) \
&= sum_(i=1)^N underbrace(exp(-y_i f_(m-1) (x_i)), "记为 " tilde(w)_(m i)) exp(-y_i alpha G(x_i)) $

这里的 $tilde(w)_(m i)$ 即第 $m$ 轮的样本权重。假设已找到最优 $G_m(x)$，将样本分为“正确”和“错误”两组求解 $alpha$：
$ L(alpha) = (1 - e_m) e^(-alpha) + e_m e^(alpha) $

对 $alpha$ 求导并令其为 0，即可解得：
$ alpha = 1/2 ln(frac(1 - e_m, e_m)) $
这保证了在 $G_m$ 选定后，$alpha_m$ 能让指数损失沿该方向达到极小值。

*权值更新的本质：函数空间梯度下降*

权值更新公式 $w_(m+1, i) = w_(m, i) exp(-alpha_m y_i G_m (x_i))$ 递归展开后为：
$ w_(m+1, i) = exp(-y_i f_m (x_i)) $

这意味着样本权值在数值上正比于该样本在当前模型下的 *指数损失*。每一轮的弱分类器 $G_m$，实际上是在拟合损失函数对于当前模型 $f_(m-1)$ 的 *负梯度方向*。这是一种 *函数空间 (Function Space) 中的梯度下降*，而不仅仅是参数调整。

*0/1 损失的上界*

之所以优化指数损失而不是 0/1 损失（错误率），是因为 0/1 损失非凸且不连续（NP-hard）。指数损失 $e^(-y f(x))$ 是 0/1 损失的 *光滑凸上界*。通过不断压低这个“上界”，间接迫使分类错误率趋于 0。